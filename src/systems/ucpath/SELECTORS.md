# ucpath — Selector Catalog

_Auto-generated by `npm run selectors:catalog` from `src/systems/ucpath/selectors.ts`. Do not hand-edit — re-run the script after changing selectors.ts._

**218 selectors** · regenerated 2026-08-20

---

## `getContentFrame()` — verified 2026-03-16

Returns the PeopleSoft content iframe FrameLocator. UCPath wraps Classic content in #main_target_win0 (not #ptifrmtgtframe). Every form interaction after initial navigation must go through this frame.

**Tags:** iframe, frame, content, peoplesoft

**Source:** [`src/systems/ucpath/selectors.ts:41`](./selectors.ts#L41)

## `smartHR.sidebarNavigationToggle()` — verified 2026-03-16

Navigation Area button that collapses the sidebar so iframe buttons aren't blocked.

**Tags:** sidebar, collapse, navigation, button

**Source:** [`src/systems/ucpath/selectors.ts:52`](./selectors.ts#L52)

## `smartHR.templateInput()` — verified 2026-03-16

Template selection textbox in the Smart HR Transactions form.

**Tags:** template, smart-hr, transaction, textbox

**Source:** [`src/systems/ucpath/selectors.ts:59`](./selectors.ts#L59)

## `smartHR.effectiveDateInput()` — verified 2026-03-16

Effective Date textbox.

**Tags:** effective, date, textbox, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:66`](./selectors.ts#L66)

## `smartHR.createTransactionButton()` — verified 2026-03-16

Create Transaction button.

**Tags:** create, transaction, smart-hr, button

**Source:** [`src/systems/ucpath/selectors.ts:73`](./selectors.ts#L73)

## `smartHR.reasonCodeSelect()` — verified 2026-03-16

Reason Code dropdown.

**Tags:** reason, code, dropdown, transaction

**Source:** [`src/systems/ucpath/selectors.ts:80`](./selectors.ts#L80)

## `smartHR.continueButton()` — verified 2026-03-16

Continue button after reason code selection.

**Tags:** continue, button, reason, transaction

**Source:** [`src/systems/ucpath/selectors.ts:86`](./selectors.ts#L86)

## `smartHR.tab.personalData()`

Personal Data tab inside Smart HR transaction form.

**Tags:** tab, personal-data, transaction, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:95`](./selectors.ts#L95)

## `smartHR.tab.jobData()`

Job Data tab inside Smart HR transaction form.

**Tags:** tab, job-data, transaction, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:101`](./selectors.ts#L101)

## `smartHR.tab.earnsDist()`

Earns Dist tab inside Smart HR transaction form.

**Tags:** tab, earns, dist, transaction, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:107`](./selectors.ts#L107)

## `smartHR.tab.employeeExperience()`

Employee Experience tab inside Smart HR transaction form.

**Tags:** tab, employee-experience, transaction, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:113`](./selectors.ts#L113)

## `smartHR.saveAndSubmitButton()` — verified 2026-03-16

Save and Submit button (the first match — bottom of every tab).

**Tags:** save, submit, transaction, button

**Source:** [`src/systems/ucpath/selectors.ts:121`](./selectors.ts#L121)

## `smartHR.confirmationOkButton()` — verified 2026-04-01

OK button on the confirmation dialog after Save & Submit.

**Tags:** ok, confirmation, dialog, save, button

**Source:** [`src/systems/ucpath/selectors.ts:128`](./selectors.ts#L128)

## `smartHR.errorBanner()` — verified 2026-03-16

Error/alert region inside the transaction iframe.

**Tags:** error, alert, banner, transaction

**Source:** [`src/systems/ucpath/selectors.ts:135`](./selectors.ts#L135)

## `smartHR.personMatchFoundHeading()`

"Person Match Found" page heading — the DEFINITIVE signal that the submit landed on the Search/Match review page rather than the confirmation dialog. mapped 2026-08-20 (live failure screenshots, runs 99d5012c/ebd5d59e)

**Tags:** person-match, search-match, heading, submit, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:160`](./selectors.ts#L160)

## `smartHR.personMatchNotAMatchButton()`

"Not a Match - Continue with Hire" action on the Person Match Found page — proceeds with the hire as a NEW person. Only clicked when every listed candidate is excluded (hard-identifier mismatch or operator-reviewed EID); see `decidePersonMatchContinue`. mapped 2026-08-20 (live failure screenshots)

**Tags:** person-match, not-a-match, continue, button, submit, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:170`](./selectors.ts#L170)

## `smartHR.personMatchCandidateRows()`

"Possible Person Matches" grid rows on the Person Match Found page — every data row carries a "Select" button in its first cell; header cells name the columns (Person ID / Legal First Name / Legal Last Name / National ID / Date of Birth). `readPersonMatchCandidates` maps the cells by header text inside the frame. mapped 2026-08-20 (live failure screenshots)

**Tags:** person-match, candidates, grid, row, select, submit, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:181`](./selectors.ts#L181)

## `smartHR.deleteSelectedTransactionsButton()` — verified 2026-06-24

"Delete Selected Transactions" button under the Smart HR Transactions page's "Transactions in Progress" grid — deletes the rows whose Select checkbox is ticked (used to remove a pending termination before recreating it). Live-verified via playwright-cli against the real in-progress grid (a real stale duplicate row deleted, keeper preserved) — see the 2026-06-24 `deletePendingTransaction` lesson in src/systems/ucpath/CLAUDE.md.

**Tags:** delete, selected, transactions, in-progress, button, smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:194`](./selectors.ts#L194)

## `personalData.legalFirstName()`

Legal First Name textbox.

**Tags:** legal, first-name, name, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:205`](./selectors.ts#L205)

## `personalData.legalLastName()`

Legal Last Name textbox.

**Tags:** legal, last-name, name, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:211`](./selectors.ts#L211)

## `personalData.legalMiddleName()`

Legal Middle Name textbox.

**Tags:** legal, middle-name, name, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:217`](./selectors.ts#L217)

## `personalData.preferredFirstName()` — verified 2026-04-16

Preferred/Lived First Name — `exact: true` disambiguates from legal variants.

**Tags:** preferred, lived, first-name, name, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:224`](./selectors.ts#L224)

## `personalData.preferredLastName()` — verified 2026-04-16

Preferred/Lived Last Name — `exact: true` disambiguates from legal variants.

**Tags:** preferred, lived, last-name, name, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:230`](./selectors.ts#L230)

## `personalData.preferredMiddleName()` — verified 2026-04-16

Preferred/Lived Middle Name — `exact: true` disambiguates from legal variants.

**Tags:** preferred, lived, middle-name, name, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:236`](./selectors.ts#L236)

## `personalData.dateOfBirth()`

Date of Birth textbox.

**Tags:** dob, birth, date, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:243`](./selectors.ts#L243)

## `personalData.nationalId()` — verified 2026-03-16

SSN / National ID textbox. exact: true avoids matching "National ID Type" dropdown.

**Tags:** ssn, national-id, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:250`](./selectors.ts#L250)

## `personalData.addressLine1()`

Address Line 1 textbox.

**Tags:** address, line, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:257`](./selectors.ts#L257)

## `personalData.city()`

City textbox.

**Tags:** city, address, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:263`](./selectors.ts#L263)

## `personalData.state()`

State textbox.

**Tags:** state, address, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:268`](./selectors.ts#L268)

## `personalData.postalCode()`

Postal Code textbox.

**Tags:** postal, zip, code, address, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:274`](./selectors.ts#L274)

## `personalData.phoneTypeSelect()` — verified 2026-03-16

Phone Type dropdown for row index 6 (Mobile - Personal slot).

**Tags:** phone, type, dropdown, grid, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:282`](./selectors.ts#L282)

## `personalData.phoneNumberInput()` — verified 2026-03-16

Phone number textbox for row index 6.

**Tags:** phone, number, textbox, grid, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:290`](./selectors.ts#L290)

## `personalData.phonePreferredCheckbox()` — verified 2026-03-16

Preferred-phone checkbox for row index 6.

**Tags:** phone, preferred, checkbox, grid, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:298`](./selectors.ts#L298)

## `personalData.emailTypeSelect()` — verified 2026-03-16

Email Type dropdown for row index 7 (Home slot).

**Tags:** email, type, dropdown, grid, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:306`](./selectors.ts#L306)

## `personalData.emailAddressInput()` — verified 2026-03-16

Email address textbox for row index 7.

**Tags:** email, address, textbox, grid, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:314`](./selectors.ts#L314)

## `personalData.trackerProfileIdInput()` — verified 2026-03-16

Tracker profile ID textbox (I-9 linkage).

**Tags:** tracker, profile, i9, textbox, personal-data

**Source:** [`src/systems/ucpath/selectors.ts:321`](./selectors.ts#L321)

## `comments.commentsTextarea()` — verified 2026-03-16

Comments textarea — exact ID preserved from original.

**Tags:** comments, textarea, transaction

**Source:** [`src/systems/ucpath/selectors.ts:332`](./selectors.ts#L332)

## `comments.initiatorCommentsTextarea()` — verified 2026-03-16

Initiator Comments textarea — exact ID preserved from original.

**Tags:** initiator, comments, textarea, transaction

**Source:** [`src/systems/ucpath/selectors.ts:339`](./selectors.ts#L339)

## `termination.overrideLastDateWorkedCheckbox()` — verified 2026-07-28

"Override Last Date Worked" checkbox. The live PeopleSoft DOM rendered `HR_TBH_SCR_WRK_TBH_CHK2$3`; prefix matching preserves the row suffix.

**Tags:** termination, override, last-date-worked, checkbox, ucpath

**Source:** [`src/systems/ucpath/selectors.ts:360`](./selectors.ts#L360)

## `termination.lastDateWorkedInput()` — verified 2026-07-28

Editable "Last Date Worked" input. The live read-only transaction rendered the corresponding `HR_TBH_SCR_WRK_TBH_DATE$3` span; this selector is deliberately input-specific so a read-only/reused transaction fails closed instead of masquerading as an editable form. The sibling `$prompt$3` calendar anchor + its `$prompt$img$3` icon share the prefix but are `<a>` / `<img>`, so `input` keeps this at exactly 1 element.

**Tags:** termination, last-date-worked, date, input, ucpath

**Source:** [`src/systems/ucpath/selectors.ts:373`](./selectors.ts#L373)

## `jobData.positionNumberInput()` — verified 2026-03-16

Position Number textbox. exact: true avoids "Reports To Position Number".

**Tags:** position, number, textbox, job-data, paypath

**Source:** [`src/systems/ucpath/selectors.ts:384`](./selectors.ts#L384)

## `jobData.employeeClassificationInput()` — verified 2026-03-16

Employee Classification textbox.

**Tags:** employee, classification, textbox, job-data

**Source:** [`src/systems/ucpath/selectors.ts:391`](./selectors.ts#L391)

## `jobData.compRateCodeInput()` — verified 2026-04-16

Comp Rate Code input. Primary: accessible name (resilient to grid-index shifts). Fallbacks: 4 known grid-ID variants covering pre- and post-position-number-fill states.

**Tags:** comp, rate, code, compensation, paypath, dropdown, textbox, job-data

**Source:** [`src/systems/ucpath/selectors.ts:403`](./selectors.ts#L403)

## `jobData.compensationRateInput()` — verified 2026-04-16

Compensation Rate input. Same shape as Comp Rate Code.

**Tags:** compensation, rate, comp, paypath, textbox, job-data

**Source:** [`src/systems/ucpath/selectors.ts:415`](./selectors.ts#L415)

## `jobData.compensationFrequencyInput()` — verified 2026-04-16

Compensation Frequency textbox (accessible name — resilient).

**Tags:** compensation, frequency, comp, textbox, job-data

**Source:** [`src/systems/ucpath/selectors.ts:427`](./selectors.ts#L427)

## `jobData.expectedJobEndDateInput()` — verified 2026-03-16

Expected Job End Date textbox.

**Tags:** expected, job, end, date, textbox, job-data

**Source:** [`src/systems/ucpath/selectors.ts:434`](./selectors.ts#L434)

## `personSearch.searchTypeSelect()` — verified 2026-03-16

Search Type dropdown (P = Person).

**Tags:** search, type, person, dropdown

**Source:** [`src/systems/ucpath/selectors.ts:445`](./selectors.ts#L445)

## `personSearch.parameterCodeInput()` — verified 2026-03-16

Parameter code input ("PERSON_SEARCH").

**Tags:** parameter, code, person, search, input

**Source:** [`src/systems/ucpath/selectors.ts:452`](./selectors.ts#L452)

## `personSearch.loadFormButton()` — verified 2026-08-05

Search button on page 1 (loads the search form). The 2026-08 configurable- search shell moved it into the "Search Criteria" section with inline results below; the id is unchanged, but the #PT_SIDE activity-guide sidebar overlays it — searchPerson collapses the sidebar first.

**Tags:** search, load, form, button, person

**Source:** [`src/systems/ucpath/selectors.ts:462`](./selectors.ts#L462)

## `personSearch.resultCodeInput()` — verified 2026-04-01

Result code input ("PERSON_RESULTS").

**Tags:** result, code, person, search, input

**Source:** [`src/systems/ucpath/selectors.ts:469`](./selectors.ts#L469)

## `personSearch.ssnInput()` — verified 2026-04-01

SSN input (CHAR_INPUT$0).

**Tags:** ssn, person, search, input

**Source:** [`src/systems/ucpath/selectors.ts:476`](./selectors.ts#L476)

## `personSearch.firstNameInput()` — verified 2026-04-01

First name input (CHAR_INPUT$1).

**Tags:** first-name, name, person, search, input

**Source:** [`src/systems/ucpath/selectors.ts:483`](./selectors.ts#L483)

## `personSearch.lastNameInput()` — verified 2026-04-01

Last name input (CHAR_INPUT$2).

**Tags:** last-name, name, person, search, input

**Source:** [`src/systems/ucpath/selectors.ts:490`](./selectors.ts#L490)

## `personSearch.dobInput()` — verified 2026-04-01

DOB input (DATE_INPUT$3).

**Tags:** dob, birth, date, person, search, input

**Source:** [`src/systems/ucpath/selectors.ts:497`](./selectors.ts#L497)

## `personSearch.ssnLookupButton()` — verified 2026-04-01

National Id magnifying-glass lookup button (CHAR_INPUT$prompt$0).

**Tags:** ssn, national-id, lookup, button, person, search

**Source:** [`src/systems/ucpath/selectors.ts:504`](./selectors.ts#L504)

## `personSearch.searchSubmitButton()` — verified 2026-04-01

Search submit button.

**Tags:** search, submit, button, person

**Source:** [`src/systems/ucpath/selectors.ts:511`](./selectors.ts#L511)

## `personSearch.resultEmplIdCells()` — verified 2026-07-10

Results grid — one Empl ID cell (`span#EMPLID$<row>`) per matched person on the PERSON_RESULTS grid (`table#l0PERSON$0`). The real results page carries NO `SEARCH_RESULT` id and no `.PSLEVEL1GRID` class — the previous anchors matched 0 elements on a live found page, so every genuine match timed out as an ambiguous outcome (live-

**Tags:** results, grid, rows, person, search, emplid

**Source:** [`src/systems/ucpath/selectors.ts:525`](./selectors.ts#L525)

## `jobSummary.campusDiscoveryUcsdLink()` — verified 2026-04-01

Campus discovery page — UCSD link.

**Tags:** campus, discovery, ucsd, link, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:536`](./selectors.ts#L536)

## `jobSummary.emplIdInput()` — verified 2026-04-01

Empl ID textbox. `root` is either `page.locator("body")` (direct URL) or `page.frameLocator("#main_target_win0").locator("body")` (iframe case).

**Tags:** empl, id, employee, textbox, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:545`](./selectors.ts#L545)

## `jobSummary.searchButton()` — verified 2026-04-01

Search button (exact: true).

**Tags:** search, button, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:552`](./selectors.ts#L552)

## `jobSummary.workLocationTab()` — verified 2026-04-01

Work Location tab. PeopleSoft renders these Job-Information sub-tabs as anchor LINKS styled as tabs, not true `role="tab"` widgets — so a `getByRole("tab", …)`-only selector times out (live separations run 2026-06-24: `click failed after 15073ms … getByRole('tab', { name: 'Work Location' })`). Fall back through the link role and an anchor with the tab text.

**Tags:** work, location, tab, link, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:564`](./selectors.ts#L564)

## `jobSummary.jobInformationTab()` — verified 2026-04-01

Job Information tab. Same link-vs-tab caveat as `workLocationTab` — anchor link styled as a tab, so the role fallback chain is required.

**Tags:** job, information, tab, link, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:577`](./selectors.ts#L577)

## `jobSummary.personName()` — verified 2026-06-18

Employee display name on the Workforce Job Summary detail page. PeopleSoft renders the bold header name (e.g. "Jayden Balmaceda", First Last order) in a stable derived field `#DERIVED_NAME_DISPLAY_NAME`; the fallback is its bold-header CSS class. Read after a successful search to verify the EID resolves to the expected person (separations name↔EID check). `root` is the `getFormRoot(page)` body locator (iframe or direct-URL case).

**Tags:** employee, name, display, header, detail, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:594`](./selectors.ts#L594)

## `jobSummary.mainTargetIframeProbe()` — verified 2026-04-01

Iframe presence probe — when count > 0 we're in iframe mode.

**Tags:** iframe, probe, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:604`](./selectors.ts#L604)

## `jobSummary.searchResultsGrid()` — verified 2026-06-24

Multi-row search-results grid container on the modern PeopleSoft Fluid "Find an Existing Value" page. Zero count → PeopleSoft auto-redirected to the detail page (single-row case). Non-zero count → multiple rows returned; caller must drill into one before clicking detail-page tabs. The live grid is `divgbrPTS_CFG_CL_STD_RSL$<n>`; the old `[id*="SEARCH_RESULT"]` / `.PSLEVEL1GRID` guesses match ZERO on the Fluid layout (kept only as classic-grid fallbacks — see LESSONS 2026-06-24).

**Tags:** multi-row, grid, search, results, fluid, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:617`](./selectors.ts#L617)

## `jobSummary.searchResultRows()` — verified 2026-06-24

Data rows inside the multi-row search-results grid. On the live Fluid "Find an Existing Value" page each result row is `tr[id^="trPTS_CFG_CL_STD_RSL"]` (e.g. `trPTS_CFG_CL_STD_RSL$0_row1`) — a clickable row whose own `onclick` (`submitAction_win0(..,'#ICRow<n>')`) drills to the detail page; there is NO `<a>` drill-in link. The legacy `[id*="SEARCH_RESULT"] tr` / `.PSLEVEL1GRID tr` probes matched ZERO live rows — the direct cause of the 2026-06-24 "could not reach the detail page (scoped rows=0)" failures (see LESSONS). The 7+ digit filter keeps employee-ID rows and skips header / separator rows.

**Tags:** multi-row, rows, grid, search, results, fluid, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:635`](./selectors.ts#L635)

## `jobSummary.rowHrStatusCell()` — verified 2026-06-24

Payroll Status cell inside a single result row — the signal the caller tests (`/terminat/i`) to skip already-terminated employments. On the live Fluid grid this is the `PTS_CFG_CL_RSLT_NUI_SRCH13$…` span (column 13 = Payroll Status), rendering the literal "Terminated" / "Active". Targeting it by the stable `SRCH13` column prefix avoids the old `td:has-text("Active")` trap (it also substring-matched HR Status "Inactive") and the strict-mode multi-match when two cells both read "Active". `.first()` guarantees a single textContent read.

**Tags:** multi-row, hr-status, payroll-status, terminated, active, cell, fluid, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:651`](./selectors.ts#L651)

## `jobSummary.rowDrillInLink()` — verified 2026-06-24

Drill-in target that navigates from the grid to the detail page for the matching row. On the live Fluid grid the ROW ITSELF is the click target — its `onclick` (`submitAction_win0(..,'#ICRow<n>')`) drills in; there is no `<a>` drill-in link, so the chain falls through to the row locator (`.or(row)` —

**Tags:** multi-row, drill-in, select, row, fluid, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:668`](./selectors.ts#L668)

## `jobSummary.resultDrillLinks()` — verified 2026-06-24

GRID-INDEPENDENT drill-in targets for the Workforce Job Summary results page, a fallback for when the scoped `searchResultRows` probe somehow misses. On the live Fluid page the clickable unit is the result ROW (`tr[id^="trPTS_CFG_CL_STD_RSL"]`, which drills via its own onclick); the caller clicks `.first()`. The legacy EMPLID/SRCH anchor probes — which the Fluid layout never emits, leaving us stuck on a tab-less results list (EID 10641172 / 2026-06-24) — stay as classic-grid fallbacks.

**Tags:** multi-row, drill-in, row, emplid, root, fallback, fluid, job-summary

**Source:** [`src/systems/ucpath/selectors.ts:684`](./selectors.ts#L684)

## `hrTasks.tile()` — verified 2026-03-16

HR Tasks tile / link.

**Tags:** hr-tasks, tile, link, navigation

**Source:** [`src/systems/ucpath/selectors.ts:709`](./selectors.ts#L709)

## `hrTasks.itemList()` — verified 2026-04-24

Sidebar `<navigation>` region containing all HR Tasks nav items. Useful as a scoping root for sidebar-only locators.

**Tags:** sidebar, navigation, region, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:717`](./selectors.ts#L717)

## `hrTasks.searchPersonLink()` — verified 2026-04-24

Sidebar top-level: Search Person (Search/Match form). Top-level links have duplicated accessible names ("X X"), so anchor with `^X`.

**Tags:** sidebar, search, person, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:726`](./selectors.ts#L726)

## `hrTasks.personOrgSummaryLink()` — verified 2026-04-24

Sidebar top-level: Person Organizational Summary.

**Tags:** sidebar, person-org-summary, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:733`](./selectors.ts#L733)

## `hrTasks.contractPayLink()` — verified 2026-04-24

Sidebar category toggle: Contract Pay (expand/collapse). The "(select to expand or collapse child steps)" suffix disambiguates the category header from any child link starting with the same prefix.

**Tags:** sidebar, contract-pay, category, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:742`](./selectors.ts#L742)

## `hrTasks.contractPaymentDetailsLink()` — verified 2026-04-24

Sidebar leaf under Contract Pay: Contract Payment Details.

**Tags:** sidebar, contract-pay, payment-details, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:751`](./selectors.ts#L751)

## `hrTasks.updateContractPayNaLink()` — verified 2026-04-24

Sidebar leaf under Contract Pay: Update Contract Pay NA.

**Tags:** sidebar, contract-pay, update, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:758`](./selectors.ts#L758)

## `hrTasks.payPathLink()` — verified 2026-04-24

Sidebar category toggle: PayPath/Additional Pay.

**Tags:** sidebar, paypath, additional-pay, category, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:765`](./selectors.ts#L765)

## `hrTasks.createAdditionalPayLink()` — verified 2026-04-24

Sidebar leaf under PayPath: Create Additional Pay.

**Tags:** sidebar, paypath, additional-pay, create, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:774`](./selectors.ts#L774)

## `hrTasks.selfServiceAdditionalPayLink()` — verified 2026-04-24

Sidebar leaf under PayPath: Self Service Additional Pay.

**Tags:** sidebar, paypath, self-service, additional-pay, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:781`](./selectors.ts#L781)

## `hrTasks.payPathActionsLink()` — verified 2026-04-24

Sidebar leaf under PayPath: PayPath Actions (combined staff+academic entry).

**Tags:** sidebar, paypath, actions, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:792`](./selectors.ts#L792)

## `hrTasks.payPathActionsAcadLink()` — verified 2026-04-24

Sidebar leaf under PayPath: PayPath Actions ACAD.

**Tags:** sidebar, paypath, actions, academic, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:799`](./selectors.ts#L799)

## `hrTasks.payPathActionsStaffLink()` — verified 2026-04-24

Sidebar leaf under PayPath: PayPath Actions STAFF.

**Tags:** sidebar, paypath, actions, staff, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:806`](./selectors.ts#L806)

## `hrTasks.jobDataRelatedLink()` — verified 2026-04-24

Sidebar category toggle: Job Data Related.

**Tags:** sidebar, job-data, category, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:813`](./selectors.ts#L813)

## `hrTasks.jobDataLink()` — verified 2026-04-24

Sidebar leaf under Job Data Related: Job Data.

**Tags:** sidebar, job-data, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:822`](./selectors.ts#L822)

## `hrTasks.ucEmployeeReviewLink()` — verified 2026-04-24

Sidebar leaf under Job Data Related: UC Employee Review.

**Tags:** sidebar, job-data, uc-employee-review, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:829`](./selectors.ts#L829)

## `hrTasks.workforceJobSummaryLink()` — verified 2026-04-24

Sidebar leaf under Job Data Related: Workforce Job Summary (separations + emergency-contact entry point).

**Tags:** sidebar, job-data, workforce, job-summary, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:837`](./selectors.ts#L837)

## `hrTasks.personalDataRelatedLink()` — verified 2026-04-24

Sidebar category toggle: Personal Data Related.

**Tags:** sidebar, personal-data, category, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:844`](./selectors.ts#L844)

## `hrTasks.activitiesLink()` — verified 2026-04-24

Sidebar leaf under Personal Data Related: Activities.

**Tags:** sidebar, personal-data, activities, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:853`](./selectors.ts#L853)

## `hrTasks.emergencyContactLink()` — verified 2026-04-24

Sidebar leaf under Personal Data Related: Emergency Contact.

**Tags:** sidebar, personal-data, emergency-contact, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:860`](./selectors.ts#L860)

## `hrTasks.identificationDataLink()` — verified 2026-04-24

Sidebar leaf under Personal Data Related: Identification Data.

**Tags:** sidebar, personal-data, identification, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:867`](./selectors.ts#L867)

## `hrTasks.modifyAPersonLink()` — verified 2026-04-24

Sidebar leaf under Personal Data Related: Modify a Person.

**Tags:** sidebar, personal-data, modify, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:874`](./selectors.ts#L874)

## `hrTasks.personChecklistLink()` — verified 2026-04-24

Sidebar leaf under Personal Data Related: Person Checklist.

**Tags:** sidebar, personal-data, checklist, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:881`](./selectors.ts#L881)

## `hrTasks.personProfilesLink()` — verified 2026-04-24

Sidebar leaf under Personal Data Related: Person Profiles (oath-signature entry point).

**Tags:** sidebar, personal-data, person-profiles, oath, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:889`](./selectors.ts#L889)

## `hrTasks.securityClearanceLink()` — verified 2026-04-24

Sidebar leaf under Personal Data Related: Security Clearance.

**Tags:** sidebar, personal-data, security-clearance, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:896`](./selectors.ts#L896)

## `hrTasks.ucExternalSystemIdsLink()` — verified 2026-04-24

Sidebar leaf under Personal Data Related: UC External System IDs.

**Tags:** sidebar, personal-data, external-system-ids, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:904`](./selectors.ts#L904)

## `hrTasks.smartHRTemplatesLink()` — verified 2026-04-24

Sidebar category toggle: Smart HR Templates. The legacy `getByText` variant from before 2026-04-24 still matched this same node loosely; the precise role+regex form here distinguishes the toggle from any descendant "Smart HR Templates" text.

**Tags:** sidebar, smart-hr, templates, category, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:914`](./selectors.ts#L914)

## `hrTasks.smartHRTransactionsLink()` — verified 2026-05-27

Sidebar leaf under Smart HR Templates: Smart HR Transactions; exact link role avoids matching "SS Smart HR Transactions".

**Tags:** sidebar, smart-hr, transactions, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:921`](./selectors.ts#L921)

## `hrTasks.ssSmartHRTransactionsLink()` — verified 2026-04-24

Sidebar leaf under Smart HR Templates: SS Smart HR Transactions (self-service variant).

**Tags:** sidebar, smart-hr, self-service, transactions, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:929`](./selectors.ts#L929)

## `hrTasks.smartHRTransactionStatusLink()` — verified 2026-04-24

Sidebar leaf under Smart HR Templates: Smart HR Transaction Status.

**Tags:** sidebar, smart-hr, status, transactions, link, hr-tasks

**Source:** [`src/systems/ucpath/selectors.ts:937`](./selectors.ts#L937)

## `personOrgSummary.emplIdInput()` — verified 2026-08-04

Empl ID textbox.

**Tags:** empl, id, employee, textbox, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:969`](./selectors.ts#L969)

## `personOrgSummary.lastNameInput()` — verified 2026-08-04

Last Name textbox.

**Tags:** last-name, name, textbox, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:977`](./selectors.ts#L977)

## `personOrgSummary.nameInput()` — verified 2026-08-04

Name (first/middle) textbox.

**Tags:** name, first-name, middle-name, textbox, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:987`](./selectors.ts#L987)

## `personOrgSummary.caseSensitiveCheckbox()` — verified 2026-04-24

Case Sensitive checkbox — toggles case-aware name matching.

**Tags:** case, sensitive, checkbox, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:996`](./selectors.ts#L996)

## `personOrgSummary.searchButton()` — verified 2026-04-24

Search submit button. The `#PTS_CFG_CL_WRK_PTS_SRCH_BTN` ID is the stable PeopleSoft Find-an-Existing-Value search trigger and is shared with the Search Person / Search/Match form.

**Tags:** search, submit, button, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1005`](./selectors.ts#L1005)

## `personOrgSummary.clearButton()` — verified 2026-04-24

Clear search criteria button. Used between iterations of name-strategy fallbacks (Last + First → Last + Middle, etc.) so stale values don't leak across attempts.

**Tags:** clear, button, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1014`](./selectors.ts#L1014)

## `personOrgSummary.personIdValue()` — verified 2026-07-08

Person ID value on the single-result detail page. PeopleSoft renders the header EID as `PERSON_NPC_VW_EMPLID` (no grid suffix); older deployments / variant code paths also expose `PER_INST_EMP_VW_OPRID$0` and `PER_INST_EMP_VW_EMPLID$0`.

**Tags:** person-id, emplid, detail, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1025`](./selectors.ts#L1025)

## `personOrgSummary.personNameValue()` — verified 2026-07-08

Employee display name on the detail page header. Primary anchor is `#PERSON_NAME_NAME` (the bold PABOLD11TEXT header span) — a live DOM probe of a real detail page (EID 10618178) found NO element for any `PERSON_NPC_VW_*NAME*` variant while `#PERSON_NAME_NAME` held the name, i.e. the old chain missed entirely and the body-scan heuristic in person-org-summary.ts was silently carrying production. The NPC arms are kept only as legacy fallbacks for older renderings.

**Tags:** name, display, header, detail, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1043`](./selectors.ts#L1043)

## `personOrgSummary.body()` — verified 2026-04-24

Body of the Person Org Summary iframe, used for DOM extraction where the page renders values as label-adjacent text nodes.

**Tags:** body, evaluate, detail, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1055`](./selectors.ts#L1055)

## `personOrgSummary.lastHireDate()` — verified 2026-04-24

Last Hire Date on the detail page's ORG Instance section.

**Tags:** hire-date, start-date, detail, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1062`](./selectors.ts#L1062)

## `personOrgSummary.terminationDate()` — verified 2026-04-24

Termination Date on the detail page's ORG Instance section. Empty means active employment.

**Tags:** termination-date, end-date, detail, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1070`](./selectors.ts#L1070)

## `personOrgSummary.terminationReason()` — verified 2026-06-05

Termination Reason (PeopleSoft action-reason description) on the detail page's ORG Instance section, rendered next to the Termination Date — e.g. "Resign - Personal Reasons". Empty for active employment.

**Tags:** termination-reason, action-reason, end-date, detail, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1079`](./selectors.ts#L1079)

## `personOrgSummary.nothingYetText()` — verified 2026-04-24

Empty-state copy shown before any search has run.

**Tags:** empty-state, text, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1086`](./selectors.ts#L1086)

## `personOrgSummary.noMatchingValuesText()` — verified 2026-04-24

No-results copy shown after PeopleSoft finds no matching values.

**Tags:** no-results, text, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1094`](./selectors.ts#L1094)

## `personOrgSummary.viewAllLink()` — verified 2026-04-24

View All link on paginated search results.

**Tags:** view-all, pagination, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1101`](./selectors.ts#L1101)

## `personOrgSummary.resultsTable()` — verified 2026-04-24

PeopleSoft configurable search results grid.

**Tags:** results, table, grid, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1108`](./selectors.ts#L1108)

## `personOrgSummary.drillInButton()` — verified 2026-04-24

Row drill-in icon in the search results grid.

**Tags:** drill-in, results, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1115`](./selectors.ts#L1115)

## `personOrgSummary.returnToSearchButton()` — verified 2026-04-24

Return to Search button on the Person Org Summary detail page.

**Tags:** return, search, detail, person-org-summary

**Source:** [`src/systems/ucpath/selectors.ts:1123`](./selectors.ts#L1123)

## `payPathActions.emplIdInput()` — verified 2026-04-24

Empl ID textbox. Most common search key.

**Tags:** empl, id, employee, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1146`](./selectors.ts#L1146)

## `payPathActions.emplRecordInput()` — verified 2026-04-24

Empl Record textbox. Disambiguates between concurrent jobs for the same employee.

**Tags:** empl, record, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1154`](./selectors.ts#L1154)

## `payPathActions.nameInput()` — verified 2026-04-24

Name textbox. exact: true required because "Last Name" and other cells contain "Name".

**Tags:** name, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1162`](./selectors.ts#L1162)

## `payPathActions.businessUnitInput()` — verified 2026-04-24

Business Unit textbox. exact: true avoids matching "Look up Business Unit".

**Tags:** business-unit, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1170`](./selectors.ts#L1170)

## `payPathActions.businessUnitLookupButton()` — verified 2026-04-24

Business Unit magnifying-glass lookup button.

**Tags:** business-unit, lookup, button, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1177`](./selectors.ts#L1177)

## `payPathActions.positionNumberInput()` — verified 2026-04-24

Position Number textbox.

**Tags:** position, number, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1184`](./selectors.ts#L1184)

## `payPathActions.positionNumberLookupButton()` — verified 2026-04-24

Position Number lookup button.

**Tags:** position, number, lookup, button, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1191`](./selectors.ts#L1191)

## `payPathActions.departmentInput()` — verified 2026-04-24

Department textbox.

**Tags:** department, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1198`](./selectors.ts#L1198)

## `payPathActions.departmentLookupButton()` — verified 2026-04-24

Department lookup button.

**Tags:** department, lookup, button, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1205`](./selectors.ts#L1205)

## `payPathActions.jobCodeInput()` — verified 2026-04-24

Job Code textbox.

**Tags:** job-code, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1212`](./selectors.ts#L1212)

## `payPathActions.jobCodeLookupButton()` — verified 2026-04-24

Job Code lookup button.

**Tags:** job-code, lookup, button, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1219`](./selectors.ts#L1219)

## `payPathActions.employeeClassificationInput()` — verified 2026-04-24

Employee Classification textbox.

**Tags:** employee, classification, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1226`](./selectors.ts#L1226)

## `payPathActions.employeeClassificationLookupButton()` — verified 2026-04-24

Employee Classification lookup button.

**Tags:** employee, classification, lookup, button, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1233`](./selectors.ts#L1233)

## `payPathActions.employeeStatusSelect()` — verified 2026-04-24

Employee Status combobox. Options: Active, Leave With Pay, Leave of Absence, Short Work Break.

**Tags:** employee, status, combobox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1241`](./selectors.ts#L1241)

## `payPathActions.caseSensitiveCheckbox()` — verified 2026-04-24

Case Sensitive checkbox.

**Tags:** case, sensitive, checkbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1248`](./selectors.ts#L1248)

## `payPathActions.searchButton()` — verified 2026-04-24

Search submit button. Shared `#PTS_CFG_CL_WRK_PTS_SRCH_BTN` ID with other Find-an-Existing-Value forms.

**Tags:** search, submit, button, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1256`](./selectors.ts#L1256)

## `payPathActions.clearButton()` — verified 2026-04-24

Clear search criteria button.

**Tags:** clear, button, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1263`](./selectors.ts#L1263)

## `payPathActions.saveAndSubmitButton()`

Save and Submit button on the post-search PayPath edit page. The `UC_E102_PP_WRK_SUBMIT_BTN` ID is unique to PayPath Actions (distinct from Smart HR's `smartHR.saveAndSubmitButton`). Verified in `src/workflows/work-study/enter.ts`.

**Tags:** save, submit, button, paypath-actions, edit

**Source:** [`src/systems/ucpath/selectors.ts:1273`](./selectors.ts#L1273)

## `payPathActions.alertOkButton()` — verified 2026-03-17

PeopleSoft alert dialog OK button (e.g. "payroll in progress" warning).

**Tags:** alert, dialog, ok, button, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1284`](./selectors.ts#L1284)

## `payPathActions.employeeNameDisplay()` — verified 2026-03-17

Employee name display span in the Position Data header (after search). Falls back to any element whose ID contains "NAME_DISPLAY".

**Tags:** employee, name, display, position-data, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1292`](./selectors.ts#L1292)

## `payPathActions.effectiveDateInput()` — verified 2026-03-17

Effective Date textbox on the Position Data tab.

**Tags:** effective-date, textbox, position-data, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1300`](./selectors.ts#L1300)

## `payPathActions.positionChangeReasonInput()` — verified 2026-03-17

Position Change Reason textbox on the Position Data tab.

**Tags:** position, change-reason, textbox, position-data, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1307`](./selectors.ts#L1307)

## `payPathActions.positionPoolInput()` — verified 2026-03-17

Position Pool textbox on the Position Data tab.

**Tags:** position, pool, textbox, position-data, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1314`](./selectors.ts#L1314)

## `payPathActions.jobDataTab()` — verified 2026-03-17

Job Data tab on the PayPath edit page.

**Tags:** job-data, tab, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1321`](./selectors.ts#L1321)

## `payPathActions.jobDataCommentsInput()` — verified 2026-03-17

Job Data Comments textbox.

**Tags:** job-data, comments, textbox, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1328`](./selectors.ts#L1328)

## `payPathActions.additionalPayDataTab()` — verified 2026-03-17

Additional Pay Data tab on the PayPath edit page.

**Tags:** additional-pay, tab, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1335`](./selectors.ts#L1335)

## `payPathActions.initiatorsCommentsInput()` — verified 2026-03-17

Initiator's Comments textbox on the Additional Pay Data tab.

**Tags:** initiators-comments, textbox, additional-pay, paypath-actions

**Source:** [`src/systems/ucpath/selectors.ts:1342`](./selectors.ts#L1342)

## `ssSmartHRTransactions.txnNumberTextbox()` — verified 2026-08-05

Transaction ID textbox (accessible name "Transaction ID begins with").

**Tags:** transaction, id, textbox, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1371`](./selectors.ts#L1371)

## `ssSmartHRTransactions.nameInput()` — verified 2026-08-05

Name textbox (accessible name "Name begins with").

**Tags:** name, textbox, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1381`](./selectors.ts#L1381)

## `ssSmartHRTransactions.emplIdInput()` — verified 2026-08-05

Empl ID textbox (accessible name "Empl ID begins with").

**Tags:** empl, id, employee, textbox, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1391`](./selectors.ts#L1391)

## `ssSmartHRTransactions.actionInput()` — verified 2026-08-05

Action textbox (PeopleSoft action code, e.g. HIR, REH; accessible name "Action begins with").

**Tags:** action, code, textbox, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1402`](./selectors.ts#L1402)

## `ssSmartHRTransactions.actionLookupButton()` — verified 2026-04-24

Action lookup button.

**Tags:** action, lookup, button, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1411`](./selectors.ts#L1411)

## `ssSmartHRTransactions.approvalStatusSelect()` — verified 2026-08-05

Approval Status combobox (accessible name "Approval Status =" — the `=` operator select is folded in). Options: Approved, Denied, Error, Manually Processed, Pending, Pushed Back.

**Tags:** approval, status, combobox, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1421`](./selectors.ts#L1421)

## `ssSmartHRTransactions.businessUnitInput()` — verified 2026-08-05

Business Unit textbox (accessible name "Business Unit begins with").

**Tags:** business-unit, textbox, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1431`](./selectors.ts#L1431)

## `ssSmartHRTransactions.businessUnitLookupButton()` — verified 2026-04-24

Business Unit lookup button.

**Tags:** business-unit, lookup, button, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1440`](./selectors.ts#L1440)

## `ssSmartHRTransactions.caseSensitiveCheckbox()` — verified 2026-04-24

Case Sensitive checkbox.

**Tags:** case, sensitive, checkbox, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1447`](./selectors.ts#L1447)

## `ssSmartHRTransactions.searchButton()` — verified 2026-04-24

Search submit button (`#PTS_CFG_CL_WRK_PTS_SRCH_BTN`).

**Tags:** search, submit, button, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1454`](./selectors.ts#L1454)

## `ssSmartHRTransactions.clearButton()` — verified 2026-04-24

Clear search criteria button.

**Tags:** clear, button, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1461`](./selectors.ts#L1461)

## `ssSmartHRTransactions.transactionResultRow()` — verified 2026-06-24

Drill into a specific transaction from the results grid by its Transaction ID — opens the Transaction Details page (where the effective date / "Effdt:" is shown). The Transaction ID column is a DISPLAY-ONLY `<span>` (`PSEDITBOX_DISPONLY`), NOT a hyperlink — the clickable element is the result ROW `<tr id^="trPTS_CFG_CL_STD_RSL">`, which carries the row's onclick. Filter the row by the (per-row unique) Transaction ID text. Falls back to a link named after the id for any PeopleSoft view that does render the id as a hyperlink.

**Tags:** transaction, drill-in, row, result, detail, effdt, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1476`](./selectors.ts#L1476)

## `ssSmartHRTransactions.transactionDetailTxnId()` — verified 2026-08-04

Transaction DETAIL page — the receipt's Transaction ID display span (`#UC_SS_TRANSACT_UC_TRANSACT_ID`). Half of the post-submit receipt PAIR; read together with .

**Tags:** transaction, id, receipt, detail, span, record-field, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1508`](./selectors.ts#L1508)

## `ssSmartHRTransactions.transactionDetailApprovalStatus()` — verified 2026-08-04

Transaction DETAIL page — the receipt's Approval Status display span (`#UC_SS_TRANSACT_APPR_STATUS`), e.g. `Approved` / `Denied` / `Pending`. The other half of the post-submit receipt PAIR.

**Tags:** approval, status, receipt, detail, span, record-field, ss-smart-hr

**Source:** [`src/systems/ucpath/selectors.ts:1523`](./selectors.ts#L1523)

## `smartHRTransactionStatus.hrReviewStatusSelect()` — verified 2026-04-24

HR Review Status combobox — top filter. Options: All, Cancelled, My Transactions, Pending, Processed.

**Tags:** hr-review, status, combobox, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1544`](./selectors.ts#L1544)

## `smartHRTransactionStatus.businessUnitInput()` — verified 2026-04-24

Business Unit textbox filter.

**Tags:** business-unit, textbox, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1551`](./selectors.ts#L1551)

## `smartHRTransactionStatus.businessUnitLookupButton()` — verified 2026-04-24

Business Unit lookup button.

**Tags:** business-unit, lookup, button, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1558`](./selectors.ts#L1558)

## `smartHRTransactionStatus.transactionTypeSelect()` — verified 2026-04-24

Transaction Type combobox. Options: All, Change Job Data, Change Job and Profile Data, Change Personal Data, Change Personal and Job Data, Change Personal and Profile Data, Change Personal/Job and Profile Data, Change Profile Data, Hire/Rehire, Hire/Rehire and Profile Data, RecruitingHire/Rehire/Transfer.

**Tags:** transaction-type, combobox, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1569`](./selectors.ts#L1569)

## `smartHRTransactionStatus.emplIdInput()` — verified 2026-04-24

Empl ID textbox filter.

**Tags:** empl, id, employee, textbox, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1576`](./selectors.ts#L1576)

## `smartHRTransactionStatus.transactionStatusSelect()` — verified 2026-04-24

Transaction Status combobox. Options: Action Required, All, Cancel, Completed, Denied, Error, Hired/Added, Requested.

**Tags:** transaction-status, combobox, status, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1584`](./selectors.ts#L1584)

## `smartHRTransactionStatus.firstNameInput()` — verified 2026-04-24

First Name textbox filter.

**Tags:** first-name, name, textbox, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1591`](./selectors.ts#L1591)

## `smartHRTransactionStatus.startDateFromInput()` — verified 2026-04-24

Start Date From textbox (MM/DD/YYYY). Defaults to ~10 days before today.

**Tags:** start-date, from, date, textbox, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1599`](./selectors.ts#L1599)

## `smartHRTransactionStatus.startDateFromCalendarButton()` — verified 2026-04-24

Calendar picker for Start Date From.

**Tags:** start-date, from, calendar, button, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1606`](./selectors.ts#L1606)

## `smartHRTransactionStatus.toDateInput()` — verified 2026-04-24

"To" date textbox (MM/DD/YYYY) — paired with Start Date From. Defaults to ~10 days after today.

**Tags:** to-date, end-date, date, textbox, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1614`](./selectors.ts#L1614)

## `smartHRTransactionStatus.toDateCalendarButton()` — verified 2026-04-24

Calendar picker for the To date.

**Tags:** to-date, end-date, calendar, button, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1621`](./selectors.ts#L1621)

## `smartHRTransactionStatus.lastNameInput()` — verified 2026-04-24

Last Name textbox filter.

**Tags:** last-name, name, textbox, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1628`](./selectors.ts#L1628)

## `smartHRTransactionStatus.downloadButton()` — verified 2026-04-24

Download button — stays disabled until a Refresh has populated rows.

**Tags:** download, button, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1636`](./selectors.ts#L1636)

## `smartHRTransactionStatus.refreshButton()` — verified 2026-04-24

Refresh button — runs the filter and populates the results grid.

**Tags:** refresh, button, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1644`](./selectors.ts#L1644)

## `smartHRTransactionStatus.clearButton()` — verified 2026-04-24

Clear button — resets all filter fields to defaults.

**Tags:** clear, button, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1651`](./selectors.ts#L1651)

## `smartHRTransactionStatus.downloadToExcelButton()` — verified 2026-04-24

Toolbar button: "Download Transaction Status Table to Excel" — exports the results grid as XLSX.

**Tags:** download, excel, export, button, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1659`](./selectors.ts#L1659)

## `smartHRTransactionStatus.returnToSmartHRLink()` — verified 2026-04-24

"Smart HR Transactions" return link at the bottom of the form. Submits a JS action that navigates back to the Smart HR Transactions landing.

**Tags:** return, smart-hr, link, transaction-status

**Source:** [`src/systems/ucpath/selectors.ts:1668`](./selectors.ts#L1668)

## `emergencyContact.emplIdInput()` — verified 2026-04-14

Empl ID textbox at page top level.

**Tags:** empl, id, employee, textbox, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1679`](./selectors.ts#L1679)

## `emergencyContact.searchButton()` — verified 2026-04-14

Search button (exact: true).

**Tags:** search, button, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1686`](./selectors.ts#L1686)

## `emergencyContact.noMatchMessage()` — verified 2026-04-14

"No matching values were found." message.

**Tags:** no-match, message, emergency-contact, search

**Source:** [`src/systems/ucpath/selectors.ts:1693`](./selectors.ts#L1693)

## `emergencyContact.drillInLink()` — verified 2026-04-14

Drill-in link in multi-result grid.

**Tags:** drill-in, link, results, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1700`](./selectors.ts#L1700)

## `emergencyContact.contactNameInputs()` — verified 2026-04-14

Every Contact Name textbox on the editor (for duplicate checking).

**Tags:** contact, name, textbox, emergency-contact, duplicate

**Source:** [`src/systems/ucpath/selectors.ts:1708`](./selectors.ts#L1708)

## `emergencyContact.primaryContactCheckboxes()` — verified 2026-04-28

Every Primary Contact checkbox on the editor — one per row, in document order, parallel to `contactNameInputs`. Used by `demoteExistingContact` to uncheck the primary on a fuzzy-matched historical contact (e.g. "Tomako Langley" demoted in favor of the correctly-spelled "Tomoko Longley").

**Tags:** primary, contact, checkbox, emergency-contact, demote

**Source:** [`src/systems/ucpath/selectors.ts:1719`](./selectors.ts#L1719)

## `emergencyContact.saveButton()` — verified 2026-04-14

Save button at the bottom of the editor.

**Tags:** save, button, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1726`](./selectors.ts#L1726)

## `emergencyContact.messageDialog()` — verified 2026-07-08

PeopleSoft "Message" alert dialog raised after a failed editor save (e.g. the (1000,110) only-one-primary-contact conflict). NEEDS LIVE VERIFY — factored out of the emergency-contact primary-conflict recovery WIP, not yet confirmed via playwright-cli.

**Tags:** message, dialog, alert, error, save, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1737`](./selectors.ts#L1737)

## `emergencyContact.messageDialogOkButton()` — verified 2026-07-08

OK button inside the "Message" alert dialog — dismisses the save error. NEEDS LIVE VERIFY — factored out of the emergency-contact primary-conflict recovery WIP, not yet confirmed via playwright-cli.

**Tags:** ok, button, message, dialog, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1747`](./selectors.ts#L1747)

## `emergencyContact.saveErrorBanner()` — verified 2026-07-08

Page-level PeopleSoft error banner on the emergency contact editor (same class/id family as the frame-scoped `smartHR.errorBanner`). NEEDS LIVE VERIFY — factored out of the emergency-contact primary-conflict recovery WIP, not yet confirmed via playwright-cli.

**Tags:** error, banner, alert, save, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1760`](./selectors.ts#L1760)

## `emergencyContact.addNewRowButton()` — verified 2026-07-08

"Add a new row at row 1" button on the editor grid — inserts a blank contact row as row 1.

**Tags:** add, new, row, button, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1769`](./selectors.ts#L1769)

## `emergencyContact.viewAllLink()` — verified 2026-08-05

"View All" toggle link on the contact scroll. Reads "View All" in paged mode and "View 1" once expanded; absent on some single-row renders. Click only when it reads "View All".

**Tags:** view-all, scroll, link, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1791`](./selectors.ts#L1791)

## `emergencyContact.contactNameAt()` — verified 2026-08-05

Contact Name textbox at a specific scroll row.

**Tags:** contact, name, textbox, row, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1798`](./selectors.ts#L1798)

## `emergencyContact.primaryContactAt()` — verified 2026-08-05

Primary Contact checkbox at a specific scroll row. `input[type=checkbox]` keeps PeopleSoft's hidden `$chk$` twin out (2026-07-28 lesson).

**Tags:** primary, contact, checkbox, row, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1807`](./selectors.ts#L1807)

## `emergencyContact.relationshipAt()` — verified 2026-08-05

Relationship dropdown at a specific scroll row. Note a blank row DEFAULTS to value "O", whose label is literally "(Invalid Value)".

**Tags:** relationship, select, row, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1815`](./selectors.ts#L1815)

## `emergencyContact.sameAddressAt()` — verified 2026-08-05

Same Address as Employee checkbox at a specific scroll row.

**Tags:** same, address, checkbox, row, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1823`](./selectors.ts#L1823)

## `emergencyContact.editAddressButtonAt()` — verified 2026-08-05

Edit Address button at a specific scroll row (opens the shared DERIVED_ADDRESS modal).

**Tags:** edit, address, button, row, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1832`](./selectors.ts#L1832)

## `emergencyContact.phoneAt()` — verified 2026-08-05

Phone textbox at a specific scroll row.

**Tags:** phone, textbox, row, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1839`](./selectors.ts#L1839)

## `emergencyContact.relationshipComboBox()` — verified 2026-07-08

"Relationship to Employee" dropdown on the editor's newly-added row.

**Tags:** relationship, combobox, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1848`](./selectors.ts#L1848)

## `emergencyContact.sameAddressAsEmployeeCheckbox()` — verified 2026-07-08

"Same Address as Employee" checkbox on the editor's newly-added row.

**Tags:** same, address, employee, checkbox, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1857`](./selectors.ts#L1857)

## `emergencyContact.editAddressButton()` — verified 2026-07-08

"Edit Address" button, shown after unchecking Same Address as Employee.

**Tags:** edit, address, button, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1866`](./selectors.ts#L1866)

## `emergencyContact.address1Input()` — verified 2026-07-08

Address 1 textbox inside the Edit Address modal.

**Tags:** address, street, textbox, modal, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1874`](./selectors.ts#L1874)

## `emergencyContact.cityInput()` — verified 2026-07-08

City textbox inside the Edit Address modal.

**Tags:** address, city, textbox, modal, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1882`](./selectors.ts#L1882)

## `emergencyContact.stateInput()` — verified 2026-07-08

State textbox inside the Edit Address modal.

**Tags:** address, state, textbox, modal, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1890`](./selectors.ts#L1890)

## `emergencyContact.postalInput()` — verified 2026-07-08

Postal textbox inside the Edit Address modal.

**Tags:** address, postal, zip, textbox, modal, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1898`](./selectors.ts#L1898)

## `emergencyContact.editAddressOkButton()` — verified 2026-07-08

OK button inside the Edit Address modal (exact match — disambiguates from other OK-labeled buttons on the page).

**Tags:** ok, button, modal, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1907`](./selectors.ts#L1907)

## `emergencyContact.phoneInput()` — verified 2026-07-08

Phone textbox (exact match) on the Contact Address/Phone tab.

**Tags:** phone, textbox, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1916`](./selectors.ts#L1916)

## `emergencyContact.personIdValue()` — verified 2026-08-05

Employee ID VALUE in the editor header ("Person ID <emplId>"). The `.or()` arm is PeopleSoft's wrapper div around the same span; both were read live and both return the bare EID.

**Tags:** person, id, emplid, value, header, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1936`](./selectors.ts#L1936)

## `emergencyContact.personNameValue()` — verified 2026-08-05

Employee NAME VALUE in the editor header. Note the record differs from the EID's (`PERSON_NAME` vs `PERSON_NPC_VW`) — do not derive one id from the other.

**Tags:** person, name, value, header, emergency-contact

**Source:** [`src/systems/ucpath/selectors.ts:1947`](./selectors.ts#L1947)

## `oathSignature.getPersonProfileFrame()` — verified 2026-04-22

Person Profiles content iframe FrameLocator. Distinct from Smart HR's `#main_target_win0` — this component mounts inside `#ptifrmtgtframe` (name="TargetContent").

**Tags:** iframe, frame, person-profile, oath

**Source:** [`src/systems/ucpath/selectors.ts:1968`](./selectors.ts#L1968)

## `oathSignature.emplIdInput()` — verified 2026-04-22

Empl ID textbox on the Find-an-Existing-Value search form.

**Tags:** empl, id, employee, textbox, person-profile, search

**Source:** [`src/systems/ucpath/selectors.ts:1975`](./selectors.ts#L1975)

## `oathSignature.searchButton()` — verified 2026-04-22

Search button (exact: true disambiguates from "Save Search").

**Tags:** search, button, person-profile

**Source:** [`src/systems/ucpath/selectors.ts:1982`](./selectors.ts#L1982)

## `oathSignature.clearSearchButton()` — verified 2026-04-22

Clear button on the search form — clears Empl ID between iterations.

**Tags:** clear, button, person-profile, search

**Source:** [`src/systems/ucpath/selectors.ts:1989`](./selectors.ts#L1989)

## `oathSignature.addNewOathLink()` — verified 2026-04-22

"Add New Oath Signature Date" link on the loaded Person Profile. The primary anchor is the PeopleSoft action id; accessible-name fallback covers rename-proof backups. Two links share the same name (icon + text) — `.first()` picks the icon/primary anchor.

**Tags:** add, new, oath, signature, link, person-profile

**Source:** [`src/systems/ucpath/selectors.ts:2000`](./selectors.ts#L2000)

## `oathSignature.noOathSentinel()` — verified 2026-04-22

"There are currently no Oath Signature Date for this profile..." sentinel. When visible on the loaded profile, the employee has no existing oath — safe to add. When absent, an oath already exists (idempotency skip).

**Tags:** no, existing, oath, sentinel, text, person-profile

**Source:** [`src/systems/ucpath/selectors.ts:2012`](./selectors.ts#L2012)

## `oathSignature.oathDateInput()` — verified 2026-07-01

Oath Signature Date textbox inside the "Add New Oath Signature Date" sub-form. Defaults to today's date on open. It is a PeopleSoft effective-date input (`id="EFFDT$0"`) with NO accessible name, so the old `getByRole("textbox", { name: "Oath Signature Date" })` matched nothing — the editable `EFFDT` input is the real anchor (only the oath row is in add-mode during this flow, so it's the sole editable EFFDT). Role selector kept as a rename-proof fallback.

**Tags:** oath, signature, date, textbox, person-profile, effdt

**Source:** [`src/systems/ucpath/selectors.ts:2025`](./selectors.ts#L2025)

## `oathSignature.oathOkButton()` — verified 2026-04-22

OK button on the oath-detail sub-form — applies the row and returns to the profile.

**Tags:** ok, button, oath, sub-form, person-profile

**Source:** [`src/systems/ucpath/selectors.ts:2036`](./selectors.ts#L2036)

## `oathSignature.oathCancelButton()` — verified 2026-04-22

Cancel button on the oath-detail sub-form — used by test/dry paths.

**Tags:** cancel, button, oath, sub-form, person-profile

**Source:** [`src/systems/ucpath/selectors.ts:2043`](./selectors.ts#L2043)

## `oathSignature.saveButton()` — verified 2026-04-22

Save button at the bottom of the Person Profile form — commits the staged oath row to the database.

**Tags:** save, button, person-profile

**Source:** [`src/systems/ucpath/selectors.ts:2051`](./selectors.ts#L2051)

## `oathSignature.returnToSearchButton()` — verified 2026-04-22

Return to Search button shown after save — clears the profile and returns to the empty search form for the next EID.

**Tags:** return, search, button, person-profile

**Source:** [`src/systems/ucpath/selectors.ts:2059`](./selectors.ts#L2059)

## `oathSignature.employeeNameDisplay()` — verified 2026-07-01

Employee name display on the loaded Person Profile — visible near the Empl ID header. Extracted for tracker rows + dashboard. The live element is `span#PERSON_NAME_NAME_DISPLAY` (class `PABOLD11TEXT`); the previous `UC_JPM_PRS_I_PERSON_NAME` / `PSXLATITEM_XLATLONGNAME` anchors matched NOTHING, so the employee name was never recorded. Kept as trailing fallbacks.

**Tags:** employee, name, display, person-profile

**Source:** [`src/systems/ucpath/selectors.ts:2071`](./selectors.ts#L2071)

## `oathSignature.existingOathDate()` — verified 2026-07-02

The EXISTING (already-saved) Oath Signature Date on the loaded profile — a display-only span `#EFFDT$0.PSEDITBOX_DISPONLY` shown when an oath row is already present. DISTINCT from `oathDateInput` (the editable `input[id^=EFFDT]` on the Add-New sub-form): before Add is clicked, only this display span exists. Read on the skip-existing path so the tracker records the signature date even when someone else entered the oath (dashboard showed "—" before).

**Tags:** existing, oath, signature, date, display, person-profile, effdt

**Source:** [`src/systems/ucpath/selectors.ts:2088`](./selectors.ts#L2088)

## `oathSignature.emplIdDisplay()` — verified 2026-07-08

The Empl ID displayed on the loaded Person Profile header — display-only span `#JPM_PROFILE_EMPLID$1$` (class `PSEDITBOX_DISPONLY`); prefix-matched because the `$1$` suffix is a PeopleSoft row index. Exactly ONE match on a loaded profile (probed live). Read by the pre-add identity gate (`assertDisplayedIdentity`) to confirm the profile belongs to the searched EID before add+save. Do NOT use `LASTUPDOPRID$…` (last-updated-by column — a different person's EID).

**Tags:** empl-id, display, person-profile, identity, gate

**Source:** [`src/systems/ucpath/selectors.ts:2101`](./selectors.ts#L2101)
