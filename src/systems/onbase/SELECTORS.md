# onbase — Selector Catalog

_Auto-generated by `npm run selectors:catalog` from `src/systems/onbase/selectors.ts`. Do not hand-edit — re-run the script after changing selectors.ts._

**28 selectors** · regenerated 2026-07-10

---

## `nav.mainMenuButton()` — verified 2026-06-22

Nine-squares "Main Menu" launcher in the top banner.

**Tags:** onbase, menu, main-menu, nine-squares, launcher, nav

**Source:** [`src/systems/onbase/selectors.ts:31`](./selectors.ts#L31)

## `nav.importDocumentMenuItem()` — verified 2026-06-22

"Import Document" item in the Main Menu Document group.

**Tags:** onbase, menu, import, import-document, menuitem, nav

**Source:** [`src/systems/onbase/selectors.ts:38`](./selectors.ts#L38)

## `nav.documentRetrievalMenuItem()` — verified 2026-06-22

"Document Retrieval" item in the Main Menu Document group (search screen).

**Tags:** onbase, menu, document-retrieval, search, menuitem, nav

**Source:** [`src/systems/onbase/selectors.ts:46`](./selectors.ts#L46)

## `importForm.frame()` — verified 2026-06-22

The Import Document iframe (`FileUploadEnhanced.aspx`). Resolve once, then pass the FrameLocator to the field selectors below.

**Tags:** onbase, import, iframe, frame, navpanel

**Source:** [`src/systems/onbase/selectors.ts:58`](./selectors.ts#L58)

## `importForm.fileInput()` — verified 2026-06-22

Hidden `<input type=file>` behind the "Choose File" button. Drive with `.setInputFiles(path)`.

**Tags:** onbase, import, file, upload, choose-file

**Source:** [`src/systems/onbase/selectors.ts:66`](./selectors.ts#L66)

## `importForm.documentTypeGroupsSelect()` — verified 2026-06-22

"Document Type Groups" `<select>` (`<All>` / Payroll / Personnel).

**Tags:** onbase, import, document-type-groups, select, dropdown

**Source:** [`src/systems/onbase/selectors.ts:74`](./selectors.ts#L74)

## `importForm.documentTypesSelect()` — verified 2026-06-22

"Document Types" `<select>` (X_HR_* types incl. X_HR_Emergency Contact).

**Tags:** onbase, import, document-types, select, dropdown, emergency-contact

**Source:** [`src/systems/onbase/selectors.ts:82`](./selectors.ts#L82)

## `importForm.fileTypeSelect()` — verified 2026-06-22

"File Type" `<select>` (pick `PDF (.pdf)`; defaults to Image File Format).

**Tags:** onbase, import, file-type, select, dropdown, pdf

**Source:** [`src/systems/onbase/selectors.ts:90`](./selectors.ts#L90)

## `importForm.keysetLookupSelect()` — verified 2026-06-22

"Keyset Lookup" `<select>` (`Employee Lookup` is the autofill keyset).

**Tags:** onbase, import, keyset, employee-lookup, select, dropdown

**Source:** [`src/systems/onbase/selectors.ts:98`](./selectors.ts#L98)

## `importForm.keysetApplyButton()` — verified 2026-06-22

Apply-keyset (key icon) button beside the Keyset Lookup select.

**Tags:** onbase, import, keyset, apply, button

**Source:** [`src/systems/onbase/selectors.ts:106`](./selectors.ts#L106)

## `importForm.keywordInput()` — verified 2026-06-22

A keyword textbox by its OnBase label (e.g. "UCPath ID", "Last Name", "First Name", "Middle Name", "Suffix", "Document Name").

**Tags:** onbase, import, keyword, textbox, generic

**Source:** [`src/systems/onbase/selectors.ts:114`](./selectors.ts#L114)

## `importForm.keywordCombobox()` — verified 2026-06-22

A keyword combobox by its OnBase label (dataset-backed fields: "Department Name", "Department Code", "Vice Chancellor", "Vice Chancellor Code").

**Tags:** onbase, import, keyword, combobox, dataset, department, vice-chancellor

**Source:** [`src/systems/onbase/selectors.ts:123`](./selectors.ts#L123)

## `importForm.ucpathIdInput()` — verified 2026-07-02

The UCPath ID keyword textbox — the primary key. Autofill runs through the keyset MODAL (`keysetLookup.*`), not by typing here + Tab.

**Tags:** onbase, import, ucpath-id, keyword, primary, autofill

**Source:** [`src/systems/onbase/selectors.ts:131`](./selectors.ts#L131)

## `importForm.documentNameInput()` — verified 2026-06-22

The Document Name keyword textbox — the one required field the keyset does NOT autofill (set the per-doc-type constant).

**Tags:** onbase, import, document-name, keyword, constant

**Source:** [`src/systems/onbase/selectors.ts:139`](./selectors.ts#L139)

## `importForm.importButton()` — verified 2026-06-22

The "Import" submit button (disabled until a file + required keywords are present).

**Tags:** onbase, import, submit, button

**Source:** [`src/systems/onbase/selectors.ts:147`](./selectors.ts#L147)

## `importForm.clearKeywordsButton()` — verified 2026-06-22

"Clear Keywords" button — reset keyword fields without losing the file.

**Tags:** onbase, import, clear, keywords, button

**Source:** [`src/systems/onbase/selectors.ts:155`](./selectors.ts#L155)

## `importForm.clearAllButton()` — verified 2026-06-22

"Clear All" button — reset the whole import form.

**Tags:** onbase, import, clear, all, button, reset

**Source:** [`src/systems/onbase/selectors.ts:162`](./selectors.ts#L162)

## `keysetLookup.dialog()` — verified 2026-07-02

The "Employee Lookup" modal dialog (top page), opened by the key-icon.

**Tags:** onbase, keyset, employee-lookup, modal, dialog

**Source:** [`src/systems/onbase/selectors.ts:182`](./selectors.ts#L182)

## `keysetLookup.frame()` — verified 2026-07-02

The modal's search-form iframe (`ReverseKeysetLookup.aspx`). The OBToken in the src varies, so match on the stable page path.

**Tags:** onbase, keyset, employee-lookup, modal, iframe, frame

**Source:** [`src/systems/onbase/selectors.ts:189`](./selectors.ts#L189)

## `keysetLookup.ucpathIdInput()` — verified 2026-07-02

The modal's OWN UCPath ID search field (distinct from the import form's).

**Tags:** onbase, keyset, employee-lookup, modal, ucpath-id, search, textbox

**Source:** [`src/systems/onbase/selectors.ts:197`](./selectors.ts#L197)

## `keysetLookup.findButton()` — verified 2026-07-02

The modal's Find button (runs the employee search).

**Tags:** onbase, keyset, employee-lookup, modal, find, search, button

**Source:** [`src/systems/onbase/selectors.ts:204`](./selectors.ts#L204)

## `keysetLookup.selectEmployeeButton()` — verified 2026-07-02

"Select Employee" — enabled once a result row is selected; clicking it autofills the import form and closes the dialog.

**Tags:** onbase, keyset, employee-lookup, modal, select, button

**Source:** [`src/systems/onbase/selectors.ts:212`](./selectors.ts#L212)

## `keysetLookup.closeButton()` — verified 2026-07-02

"Close Dialog" — dismiss the Employee Lookup modal without selecting.

**Tags:** onbase, keyset, employee-lookup, modal, close, cancel, button

**Source:** [`src/systems/onbase/selectors.ts:222`](./selectors.ts#L222)

## `keysetLookup.noMatchMessage()` — verified 2026-07-02

The "No matching records were found" message (search returned nothing = bad/unknown UCPath ID).

**Tags:** onbase, keyset, employee-lookup, modal, no-match, message

**Source:** [`src/systems/onbase/selectors.ts:232`](./selectors.ts#L232)

## `keysetLookup.noMatchOkButton()` — verified 2026-07-02

OK button on the no-match alert dialog.

**Tags:** onbase, keyset, employee-lookup, modal, no-match, ok, button

**Source:** [`src/systems/onbase/selectors.ts:239`](./selectors.ts#L239)

## `documentQueue.frame()` — verified 2026-07-02

The Document Queue iframe (`frmViewer`, src `FileUploadEnhancedRightPage.aspx`) — a top-page sibling of NavPanelIFrame.

**Tags:** onbase, import, document-queue, iframe, frame, frmviewer

**Source:** [`src/systems/onbase/selectors.ts:259`](./selectors.ts#L259)

## `documentQueue.queuedRow()` — verified 2026-07-02

A queued document's row by filename (cell text). Appearing with status "Pending Import" confirms the attach landed.

**Tags:** onbase, import, document-queue, row, pending-import, attach, confirm

**Source:** [`src/systems/onbase/selectors.ts:267`](./selectors.ts#L267)

## `documentQueue.removeButtons()` — verified 2026-07-02

Every "Remove" button in the queue — used to clear leftover rows from a failed prior attempt before attaching (duplicate-import defense).

**Tags:** onbase, import, document-queue, remove, clear, button

**Source:** [`src/systems/onbase/selectors.ts:276`](./selectors.ts#L276)
