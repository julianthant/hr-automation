# crm — Selector Catalog

_Auto-generated by `npm run selectors:catalog` from `src/systems/crm/selectors.ts`. Do not hand-edit — re-run the script after changing selectors.ts._

**8 selectors** · regenerated 2026-04-28

---

## `search.resultRows()` — verified 2026-04-14

All result rows in the search-results table.

**Tags:** search, results, rows, table, crm

**Source:** [`src/systems/crm/selectors.ts:18`](./selectors.ts#L18)

## `search.nthResultRow()` — verified 2026-04-14

Nth result row. Use: `search.nthResultRow(page, i).locator("td").nth(1)` for the "Offer Sent On" column (index 1).

**Tags:** search, result, row, nth, table, crm

**Source:** [`src/systems/crm/selectors.ts:25`](./selectors.ts#L25)

## `record.thLabelFollowingTd()` — verified 2026-04-14

Visualforce label → value locator strategy 1: label in <th>, value in the next sibling <td>.

**Tags:** visualforce, label, value, th, extract, record, crm

**Source:** [`src/systems/crm/selectors.ts:37`](./selectors.ts#L37)

## `record.tdLabelFollowingTd()` — verified 2026-04-14

Visualforce label → value locator strategy 2 (fallback): label in <td>, value in the next sibling <td>.

**Tags:** visualforce, label, value, td, extract, record, fallback, crm

**Source:** [`src/systems/crm/selectors.ts:47`](./selectors.ts#L47)

## `sectionNav.byName()` — verified 2026-04-14

Fallback chain for "click a section by name" when direct URL mapping isn't available in CRM_SECTION_URLS. Tries link, then text, then tab.

**Tags:** section, navigation, name, fallback, link, tab, crm

**Source:** [`src/systems/crm/selectors.ts:62`](./selectors.ts#L62)

## `onboardingHistory.historyRows()` — verified 2026-04-28

Every history row in the audit-log table on the ONB_ShowOnboardingHistory page.

**Tags:** onboarding, history, audit, log, row, crm

**Source:** [`src/systems/crm/selectors.ts:97`](./selectors.ts#L97)

## `onboardingHistory.rowCells()` — verified 2026-04-28

The cells inside a single history row — 5 columns: Date, Created By, Field, Old Value, New Value.

**Tags:** onboarding, history, audit, cell, td, crm

**Source:** [`src/systems/crm/selectors.ts:104`](./selectors.ts#L104)

## `onboardingHistory.firstResultLink()` — verified 2026-04-28

The single result row on the search page after navigating to `/hr/ONB_SearchOnboardings?q=<EID>` — its first `<a>` link has the record-page URL `/hr/ONB_ViewOnboarding?id=<RECORD_ID>` we need to reach the history page.

**Tags:** onboarding, search, result, link, record-id, crm

**Source:** [`src/systems/crm/selectors.ts:113`](./selectors.ts#L113)
