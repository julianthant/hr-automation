# sharepoint — Selector Catalog

_Auto-generated by `npm run selectors:catalog` from `src/systems/sharepoint/selectors.ts`. Do not hand-edit — re-run the script after changing selectors.ts._

**18 selectors** · regenerated 2026-04-24

---

## `microsoft.emailInput()` — verified 2026-04-22

Email textbox on the Microsoft sign-in page. Primary is the CSS fallback (`input[name="loginfmt"]` / `input[type="email"]`) because the accessible name varies by tenant ("Enter your email, phone, or Skype.").

**Tags:** microsoft, login, email, textbox

**Source:** [`src/systems/sharepoint/selectors.ts:40`](./selectors.ts#L40)

## `microsoft.nextButton()` — verified 2026-04-22

"Next" button on the Microsoft email step. Falls back to a generic submit input since some tenants render the button as `<input type="submit">`.

**Tags:** microsoft, login, next, button

**Source:** [`src/systems/sharepoint/selectors.ts:51`](./selectors.ts#L51)

## `adfs.usernameInput()` — verified 2026-04-22

"User Account" textbox on the ADFS sign-in page. Usually pre-populated by Microsoft via the `?username=` URL parameter; we still explicitly re-fill it defensively in case the hand-off ever misses. Primary is the stable name attribute; fallbacks are the DOM id and the accessible name.

**Tags:** adfs, ucsd, sharepoint, username, textbox, login

**Source:** [`src/systems/sharepoint/selectors.ts:81`](./selectors.ts#L81)

## `adfs.passwordInput()` — verified 2026-04-22

"Password" textbox on the ADFS sign-in page.

**Tags:** adfs, ucsd, sharepoint, password, textbox, login

**Source:** [`src/systems/sharepoint/selectors.ts:92`](./selectors.ts#L92)

## `adfs.submitButton()` — verified 2026-04-22

"Sign in" submit button on the ADFS sign-in page.

**Tags:** adfs, ucsd, sharepoint, submit, sign-in, button, login

**Source:** [`src/systems/sharepoint/selectors.ts:103`](./selectors.ts#L103)

## `kmsi.noButton()` — verified 2026-04-22

"No" button on the "Stay signed in?" / KMSI prompt (login.srf). We always click No so the persistent context decides what to remember, not AAD.

**Tags:** microsoft, kmsi, stay-signed-in, no, button

**Source:** [`src/systems/sharepoint/selectors.ts:118`](./selectors.ts#L118)

## `kmsi.yesButton()` — verified 2026-04-22

"Yes" button on the KMSI prompt. Not currently clicked by any workflow — kept for parity + future reuse.

**Tags:** microsoft, kmsi, stay-signed-in, yes, button

**Source:** [`src/systems/sharepoint/selectors.ts:130`](./selectors.ts#L130)

## `kmsi.dontShowAgainCheckbox()` — verified 2026-04-22

"Don't show this again" checkbox on the KMSI prompt. Unchecked by default — kept for parity + future reuse.

**Tags:** microsoft, kmsi, dont-show-again, checkbox

**Source:** [`src/systems/sharepoint/selectors.ts:139`](./selectors.ts#L139)

## `getExcelFrame()` — verified 2026-04-22

Returns the Excel Online FrameLocator. The ribbon, file menu, and workbook body all live inside `iframe[name="WacFrame_Excel_0"]` on doc2.aspx — every post-auth interaction MUST go through this frame or selectors resolve to nothing.

**Tags:** excel, iframe, frame, wac

**Source:** [`src/systems/sharepoint/selectors.ts:152`](./selectors.ts#L152)

## `excelOnline.fileButton()` — verified 2026-04-22

File button on the Excel Online ribbon (opens the backstage menu). `exact: true` avoids matching "File menu", "File tab", etc.

**Tags:** excel, ribbon, file, button

**Source:** [`src/systems/sharepoint/selectors.ts:163`](./selectors.ts#L163)

## `excelOnline.coEditingBanner()` — verified 2026-04-22

Co-editing banner ("X is now editing the workbook"). Used only as a readiness probe — when visible, the ribbon has finished hydrating.

**Tags:** excel, coedit, banner, readiness

**Source:** [`src/systems/sharepoint/selectors.ts:172`](./selectors.ts#L172)

## `fileMenu.createACopy()` — verified 2026-04-22

"Create a Copy" menuitem in the File backstage. Must be HOVERED (not clicked) to open the submenu containing "Download a Copy". Clicking navigates to "Create a copy online" instead.

**Tags:** excel, file-menu, create-a-copy, menuitem, backstage

**Source:** [`src/systems/sharepoint/selectors.ts:186`](./selectors.ts#L186)

## `fileMenu.downloadACopy()` — verified 2026-04-22

"Download a Copy" menuitem — in the "Create a Copy" flyout. THIS is the xlsx download path. Triggering it fires the browser's `download` event.

**Tags:** excel, file-menu, download, xlsx, workbook, menuitem

**Source:** [`src/systems/sharepoint/selectors.ts:195`](./selectors.ts#L195)

## `fileMenu.export()` — verified 2026-04-22

"Export" menuitem — opens a flyout with non-xlsx download options only (PDF / CSV / CSV UTF-8 / ODS). The xlsx download is NOT here despite the name — that lives under "Create a Copy → Download a Copy".

**Tags:** excel, file-menu, export, menuitem, backstage

**Source:** [`src/systems/sharepoint/selectors.ts:205`](./selectors.ts#L205)

## `fileMenu.downloadAsPdf()` — verified 2026-04-22

"Download as PDF" — inside the Export flyout.

**Tags:** excel, export, download, pdf, menuitem

**Source:** [`src/systems/sharepoint/selectors.ts:213`](./selectors.ts#L213)

## `fileMenu.downloadAsCsvUtf8()` — verified 2026-04-22

"Download as CSV UTF-8" — inside the Export flyout.

**Tags:** excel, export, download, csv, utf8, menuitem

**Source:** [`src/systems/sharepoint/selectors.ts:221`](./selectors.ts#L221)

## `fileMenu.downloadAsCsv()` — verified 2026-04-22

"Download as CSV" — inside the Export flyout.

**Tags:** excel, export, download, csv, menuitem

**Source:** [`src/systems/sharepoint/selectors.ts:229`](./selectors.ts#L229)

## `fileMenu.downloadAsOds()` — verified 2026-04-22

"Download as ODS" — inside the Export flyout.

**Tags:** excel, export, download, ods, menuitem

**Source:** [`src/systems/sharepoint/selectors.ts:237`](./selectors.ts#L237)
