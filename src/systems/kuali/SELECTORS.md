# kuali — Selector Catalog

_Auto-generated by `npm run selectors:catalog` from `src/systems/kuali/selectors.ts`. Do not hand-edit — re-run the script after changing selectors.ts._

**20 selectors** · regenerated 2026-06-16

---

## `actionList.menuItem()` — verified 2026-03-16

"Action List" menu item.

**Tags:** action, list, menu, kuali

**Source:** [`src/systems/kuali/selectors.ts:18`](./selectors.ts#L18)

## `actionList.docLink()` — verified 2026-03-16

Document link matching a doc number regex.

**Tags:** document, doc, link, action-list, kuali

**Source:** [`src/systems/kuali/selectors.ts:25`](./selectors.ts#L25)

## `actionList.docLinks()`

All pending-document links in the Action List, one per row. Each Kuali Build Action List row exposes its document number as the accessible name of a link (the same anchor `docLink` matches by an exact number). This enumerates every such link so a caller can read the full set of pending document numbers without knowing them ahead of time. The accessible-name filter `/^\s*\d+\s*$/` keeps only links whose text is a bare document number, excluding the page's nav / action / pagination links (which carry word labels).

**Tags:** document, doc, links, action-list, pending, enumerate, kuali

**Source:** [`src/systems/kuali/selectors.ts:46`](./selectors.ts#L46)

## `separationForm.employeeName()`

Employee name textbox (last, first format).

**Tags:** employee, name, last, first, textbox, separation, kuali

**Source:** [`src/systems/kuali/selectors.ts:57`](./selectors.ts#L57)

## `separationForm.eid()`

EID textbox.

**Tags:** eid, employee, id, textbox, separation, kuali

**Source:** [`src/systems/kuali/selectors.ts:63`](./selectors.ts#L63)

## `separationForm.lastDayWorked()`

Last Day Worked date textbox.

**Tags:** last-day, worked, date, textbox, separation, kuali

**Source:** [`src/systems/kuali/selectors.ts:69`](./selectors.ts#L69)

## `separationForm.separationDate()`

Separation Date textbox.

**Tags:** separation, date, textbox, kuali

**Source:** [`src/systems/kuali/selectors.ts:75`](./selectors.ts#L75)

## `separationForm.terminationType()`

Type of Termination combobox.

**Tags:** type, termination, combobox, separation, kuali

**Source:** [`src/systems/kuali/selectors.ts:81`](./selectors.ts#L81)

## `separationForm.location()`

Location textbox (optional field).

**Tags:** location, textbox, separation, kuali

**Source:** [`src/systems/kuali/selectors.ts:87`](./selectors.ts#L87)

## `timekeeperTasks.requestAcknowledgedCheckbox()`

Request Acknowledged checkbox.

**Tags:** request, acknowledged, checkbox, timekeeper, kuali

**Source:** [`src/systems/kuali/selectors.ts:98`](./selectors.ts#L98)

## `timekeeperTasks.timekeeperName()`

Timekeeper Name textbox.

**Tags:** timekeeper, name, textbox, kuali

**Source:** [`src/systems/kuali/selectors.ts:105`](./selectors.ts#L105)

## `timekeeperTasks.timekeeperComments()`

Timekeeper / Approver Comments textbox.

**Tags:** timekeeper, approver, comments, textbox, kuali

**Source:** [`src/systems/kuali/selectors.ts:112`](./selectors.ts#L112)

## `finalTransactions.terminationEffDate()`

Termination Effective Date textbox.

**Tags:** termination, effective, date, textbox, final, kuali

**Source:** [`src/systems/kuali/selectors.ts:123`](./selectors.ts#L123)

## `finalTransactions.department()`

Department combobox.

**Tags:** department, combobox, final, kuali

**Source:** [`src/systems/kuali/selectors.ts:130`](./selectors.ts#L130)

## `finalTransactions.payrollTitleCode()`

Payroll Title Code textbox.

**Tags:** payroll, title, code, textbox, final, kuali

**Source:** [`src/systems/kuali/selectors.ts:137`](./selectors.ts#L137)

## `finalTransactions.payrollTitle()`

Payroll Title textbox.

**Tags:** payroll, title, textbox, final, kuali

**Source:** [`src/systems/kuali/selectors.ts:144`](./selectors.ts#L144)

## `transactionResults.submittedTemplateCheckbox()`

Submitted Termination Template checkbox.

**Tags:** submitted, template, termination, checkbox, transaction, kuali

**Source:** [`src/systems/kuali/selectors.ts:155`](./selectors.ts#L155)

## `transactionResults.transactionNumber()`

Transaction Number textbox.

**Tags:** transaction, number, textbox, kuali

**Source:** [`src/systems/kuali/selectors.ts:162`](./selectors.ts#L162)

## `transactionResults.doesNotNeedFinalPayRadio()`

"Does not need Final Pay (student employee)" radio.

**Tags:** final, pay, student, radio, transaction, kuali

**Source:** [`src/systems/kuali/selectors.ts:169`](./selectors.ts#L169)

## `save.navbarSaveButton()`

Navbar Save button (3-deep fallback chain).

**Tags:** save, navbar, button, fallback, kuali

**Source:** [`src/systems/kuali/selectors.ts:190`](./selectors.ts#L190)
