# new-kronos — Selector Catalog

_Auto-generated by `npm run selectors:catalog` from `src/systems/new-kronos/selectors.ts`. Do not hand-edit — re-run the script after changing selectors.ts._

**23 selectors** · regenerated 2026-06-24

---

## `searchFrame()` — verified 2026-04-06

Grab the Employee Search sidebar iframe (dynamic name).

**Tags:** iframe, frame, search, portal, dayforce, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:18`](./selectors.ts#L18)

## `loadingOverlay.overlay()`

WFD full-page loading overlay — wait for it to be hidden before clicking. Common selectors for Dayforce loading masks; at least one fires on the employee-search button click. // NEEDS LIVE RE-VERIFY 2026-06-17

**Tags:** loading, overlay, spinner, busy, wfd, dayforce, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:37`](./selectors.ts#L37)

## `navbar.employeeSearchButton()` — verified 2026-04-06

Open the Employee Search sidebar.

**Tags:** employee, search, button, sidebar, navbar, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:48`](./selectors.ts#L48)

## `search.searchInput()` — verified 2026-06-18

Search textbox (resolved iframe-or-top-level).

**Tags:** search, input, textbox, employee, name, id, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:69`](./selectors.ts#L69)

## `search.searchSubmitButton()` — verified 2026-06-18

Search submit (exact name to distinguish from other Search buttons).

**Tags:** search, submit, button, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:76`](./selectors.ts#L76)

## `search.noResultsText()` — verified 2026-06-18

"There are no items to display" text — no-results probe.

**Tags:** no-results, empty, text, probe, search, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:83`](./selectors.ts#L83)

## `search.firstResultCheckbox()` — verified 2026-06-18

First result's "Select Item" checkbox — checking it SELECTS the employee, which is what ENABLES the Go To button (it is `ng-disabled` until a slat is selected). Target it PRECISELY by accessible name "Select Item" — do NOT union with a bare `input[type=checkbox]`, which would also match the "Select All" header checkbox and pick the wrong control (leaving Selected[0]). The result control is a native input with role=checkbox, so getByRole still matches it.

**Tags:** first, result, checkbox, select-item, search, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:96`](./selectors.ts#L96)

## `search.firstResultRow()` — verified 2026-06-18

First result row fallback — the result renders as a `menuitemradio` ("Item Name <name> not checked"); clicking it also selects the employee.

**Tags:** first, result, row, menuitemradio, fallback, search, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:105`](./selectors.ts#L105)

## `search.firstResultSlat()`

First result SLAT — a "somebody showed up" signal for found-detection. The live WFD result renders as a `menuitemradio` ("Item Name <name> not checked"); its presence means the search returned an employee. Kept TIGHT (no `[role=row]` fallback, unlike `firstResultRow`) so a header/placeholder row can never false-positive a not-found search into found. Used to detect a present result INDEPENDENTLY of the "Select Item" checkbox, whose backing native input is visually hidden — see `searchEmployee`.

**Tags:** first, result, slat, menuitemradio, found, probe, search, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:119`](./selectors.ts#L119)

## `search.closeButton()` — verified 2026-06-18

Close the sidebar.

**Tags:** close, sidebar, button, search, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:126`](./selectors.ts#L126)

## `goToMenu.goToButtonOnPage()`

Employee Search panel's "Go To" dropdown (top-level page render). MUST be scoped to the Quick Find slide-out — a bare page-wide `/go to/i` ALSO matches the timecard TOOLBAR "Go to" button (`aria-label="Go to"`, `class="btn widget-button-icon"`), which is always enabled and sits BEHIND the Employee Search slide-out's `slideout__mask`, so clicking it dies with "Another element intercepted the click (modal/overlay)" (live log 2026-06-22: doc reached "Employee checkbox checked" then 5s mask-interception timeout → empty timecard). Primary = the panel's stable `#goToDropdownButton` (`ng-disabled="!quickFind.slatOptions.selectedslats.length"`); fallback = the `/go to/i` role match SCOPED to the `.quick-find-content` slide-out container (verified present in the live log) so it can never resolve the toolbar button. // NEEDS LIVE RE-VERIFY 2026-06-22 (derived from live error log + the 2026-06-18 id, not a fresh snapshot)

**Tags:** go-to, button, page, quick-find, slideout, dropdown, navigation, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:148`](./selectors.ts#L148)

## `goToMenu.goToButtonInFrame()`

Employee Search panel's "Go To" dropdown (portal-frame iframe render). Same slide-out scoping as `goToButtonOnPage` — never the timecard toolbar "Go to". // NEEDS LIVE RE-VERIFY 2026-06-22

**Tags:** go-to, button, frame, quick-find, slideout, dropdown, navigation, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:160`](./selectors.ts#L160)

## `goToMenu.timecardItem()` — verified 2026-06-18

Timecard menu item in the open Go To dropdown — role-scoped to the option ITSELF. The live Dayforce Go To menu renders Timecard as role="option" (menuitem kept as a fallback); both frame- and page-level renderings plus the "Timecards" plural variant are covered.

**Tags:** timecard, menu, item, option, navigation, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:183`](./selectors.ts#L183)

## `timecard.payPeriodTriggerButton()` — verified 2026-06-18

Pay-period trigger button — text varies ("Current Pay Period", "Previous Pay Period", or a date range). Match all three.

**Tags:** pay, period, trigger, button, timecard, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:203`](./selectors.ts#L203)

## `timecard.previousPayPeriodOption()` — verified 2026-06-18

Previous Pay Period option (inside an open period dropdown).

**Tags:** previous, pay, period, option, timecard, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:214`](./selectors.ts#L214)

## `timecard.selectRangeButton()` — verified 2026-06-18

"Select range" button to switch to custom date range.

**Tags:** select, range, button, custom, date, timecard, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:221`](./selectors.ts#L221)

## `timecard.startDateInput()` — verified 2026-06-22

Start date input (custom range) — NATIVE `<input type=date>`, value held as ISO `YYYY-MM-DD` (NOT a masked text field). Targeted by id.

**Tags:** start, date, input, range, timecard, native, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:229`](./selectors.ts#L229)

## `timecard.endDateInput()` — verified 2026-06-22

End date input (custom range) — NATIVE `<input type=date>` (ISO value). Targeted by id.

**Tags:** end, date, input, range, timecard, native, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:236`](./selectors.ts#L236)

## `timecard.calendarMonthHeader()` — verified 2026-06-22

Moment-picker month/year header (e.g. "Jun 2026") — read to know which way to step the calendar.

**Tags:** calendar, month, header, range, timecard, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:243`](./selectors.ts#L243)

## `timecard.calendarPrevMonth()` — verified 2026-06-22

Moment-picker "Previous month" arrow.

**Tags:** calendar, previous, month, arrow, range, timecard, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:250`](./selectors.ts#L250)

## `timecard.calendarNextMonth()` — verified 2026-06-22

Moment-picker "Next month" arrow.

**Tags:** calendar, next, month, arrow, range, timecard, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:257`](./selectors.ts#L257)

## `timecard.calendarDayCell()` — verified 2026-06-22

Moment-picker day cell for a specific date. `name` matches the cell's full-date aria-label ("Monday, June 11, 2026"); `:not(.out-of-month)` keeps an adjacent-month trailing day from being hit.

**Tags:** calendar, day, cell, gridcell, range, timecard, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:266`](./selectors.ts#L266)

## `timecard.applyButton()` — verified 2026-06-18

Apply button (custom range).

**Tags:** apply, button, range, timecard, new-kronos

**Source:** [`src/systems/new-kronos/selectors.ts:275`](./selectors.ts#L275)
