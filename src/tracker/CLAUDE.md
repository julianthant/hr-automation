# Tracker Module

Two-tier tracking: JSONL for live dashboard streaming, Excel for persistent historical records.

> **Kernel-internal.** `withTrackedWorkflow`, `appendLogEntry`, and the SIGINT handler are invoked by `src/core/runWorkflow` / `runWorkflowBatch` / `runWorkflowPool` / daemon `runOneItem` — workflow handlers never call them directly. Use `ctx.step(...)` / `ctx.updateData(...)` inside the handler.

## Files

- `jsonl.ts` — JSONL append-only tracker + `withTrackedWorkflow` lifecycle wrapper, `cleanOldTrackerFiles`/`cleanOldScreenshots`, PII-aware `serializeValue` + `toTypedValue`
- `dashboard/server.ts` — creates the HTTP server (port 3838): JSONL-only startup prune (unless `noClean`), projection rebuild, periodic sweeps. Serves `/api/*`, multiplexed `GET /events/hub`, and static prod assets when configured — routing lives under `dashboard/hono/`
- `dashboard.ts` — barrel re-export of `startDashboard` / `createDashboardServer` / `stopDashboard` plus session-state helpers (`filterEventsForRun`, `rebuildSessionState`, …) for tests and imports
- `session-events.ts` — `emitWorkflowStart` / `emitWorkflowEnd` / `emitSessionCreate` / `emitBrowserLaunch` / `emitAuthStart` / `emitAuthComplete` / `emitItemStart` / etc. Append `SessionEvent` lines to rotated `sessions-*.jsonl` (and legacy `sessions.jsonl`). `rebuildSessionState` in `src/tracker/dashboard/session-state.ts` reduces them into a live `SessionState` (re-exported from `dashboard.ts` for tests)
- `sessions/duo-queue.ts` — `requestDuoApproval(page, options)` — wraps `pollDuoApproval` with queue semantics (emit `duo_waiting` browser overlay, register in the global Duo queue, swap to `duo_active` when this request becomes head-of-line). Used by every login flow in `src/infra/auth/login.ts`
- `sessions/auth-observer.ts` — builds a `SessionObserver` that turns kernel auth lifecycle callbacks into tracker step events and failure screenshots.
- `files/files.ts` — SQLite-backed file registry helpers (`registerLocalFile`, `getRegisteredFile`) for PDFs, screenshots, page images, and related dashboard downloads.
- `files/pdf-cache.ts` — renders and registers per-page PNG cache records for uploaded PDFs.
- `files/multipart-helper.ts` — small multipart/form-data parser shared by dashboard upload routes.
- `state/screenshot-sweep.ts` — `sweepStaleRunScreenshots(dir, screenshotsDir, maxAgeDays = 30)` — lifecycle-tied screenshot cleanup driven by `runs.terminal_at`. Wired into `createDashboardServer` (startup + 6h interval). See "Cleaning Old Tracker Files".
- `exports/export-excel.ts` — On-demand Excel export from JSONL data
- `exports/spreadsheet.ts` — `appendRow(filePath, columns, data)` and `parseDepartmentNumber(deptText)`
- `alerts/failure-detector.ts` — `detectFailurePattern(entries, opts)` — pure function that groups failed tracker entries by (workflow, error), returns patterns that cross `thresholdN` inside `windowMs`. Caller-owned `cooldownState: Map<string, number>` suppresses re-alerts for `cooldownMs`. Defaults: 3 / 10min / 1h.
- `alerts/notify.ts` — `notify(title, body)` — best-effort macOS desktop notification via `osascript display notification`. No native deps. On non-darwin or osascript failure, logs a warn and returns without throwing.
- `delegation/watch-child-runs.ts` — `watchChildRuns(opts)` — generic watcher: polls a workflow's JSONL until N expected `itemId`s reach terminal status. Used by non-migrated waits such as SharePoint delegation and OCR fallback/force-research paths. Supports custom `isTerminal` predicate, `onProgress` callback, and 200ms polling fallback for filesystems where `fs.watch` is unavailable.
- `dashboard/oath-upload/http.ts` — HTTP handlers for `/api/oath-upload/*` endpoints and restart sweep; kept under `dashboard/` because its imports are dashboard route/server-only.
- `locked.ts` — Generic mutex-locked write wrapper for parallel Excel access
- `dashboard/hono/routes/ocr.ts` — HTTP handlers for `/api/ocr/*` endpoints: `buildOcrPrepareHandler`, `buildOcrApproveHandler`, `buildOcrDiscardHandler`, `buildOcrForceResearchHandler`, `buildOcrFormsHandler`, `sweepStuckOcrRows`. Per-sessionId in-memory lock (`_resetSessionLockForTests` for tests).
- `dashboard/hono/routes/entries-payload.ts` — JSONL fallback builder for the `entries` hub topic. It returns the selected workflow's tracker rows plus policy-declared persistent-root descendants (currently Oath Upload OCR/signature child context), then enriches rows for queue rendering.
- `dashboard/actions/` — central workflow action engine. `perform-workflow-action.ts` (`performWorkflowAction` dispatcher), `resolve-targets.ts` (scope → blast radius), `types.ts` (`WorkflowActionRequest` / `WorkflowActionResult`). Routes operator cancel/retry/delete/bump to the low-level `dashboard/ops/*` handlers.
- `index.ts` — Barrel re-exports

## Observability conventions

Tracker/log/session rows should preserve readable messages and carry structured fields when available. Operator-facing rows should prefer `data.__subject`; raw run ids/session ids are debug identifiers.

Tracker rows may include:
- `data.__subject` / `data.__subjectKind` from `WorkflowConfig.operatorSubject`.
- Task display fields such as `taskRole`, `originWorkflow`, and `taskGroupId`.
- Structured log fields such as `category`, `occasion`, `subject`, `system`, `step`, `attempt`, `childWorkflow`, or `durationMs`.

## `TrackerEntry.parentRunId`

Optional field added 2026-05-01. When set, the entry is a child run delegated by the parent. Used purely for dashboard visualization (parent→child pills in `EntryItem`, "Delegated runs" section in `LogPanel`). Watching logic is itemId-based (`delegation/watch-child-runs.ts`), not parentRunId-based. Thread through `withTrackedWorkflow` via `opts.parentRunId` (also available in `RunOpts`).

## SQLite Task Dependencies And Control State

- `src/tracker/tasks/store.ts` owns durable task/dependency rows for the Phase 2 OCR → EID lookup cutover.
- `src/tracker/tasks/scheduler.ts` is idempotent. It may run repeatedly; terminal dependencies are not re-applied.
- Phase 3 moved live queue/control authority into SQLite via `src/core/task-store/index.ts` and `src/core/daemon/worker-store.ts`: task attempts, claims, worker heartbeats, `worker_commands`, and scoped `browser_processes` are coordination state.
- JSONL remains audit/history output and dashboard visibility during transition. Do not remove `watchChildRuns`; it remains the fallback for legacy rows with no SQLite task/dependency records.
- Queue authority is SQLite. The `.queue.jsonl` file in `.tracker/daemons/` is an append-only audit trail only — readers must not consume it as state.
- New dependency kinds should not be added until a second real workflow needs them.
- **2026-05-20 retry rule:** retrying an OCR dependency child by failed `runId` must reopen the matching `task_dependencies` row (`status='pending'`, clear `terminal_at` / `result_json`) because the child task id is reused with a new attempt/run id. Without that reset, the scheduler will not patch the OCR preview after the retry and the operator could approve stale data.

## Failure-Pattern Alerts

`createDashboardServer` schedules `scanFailurePatterns()` on a **~15s** `setInterval` (see `dashboard/server.ts`) — not tied to SSE polling. Each run walks today's tracker entries across workflows through `detectFailurePattern`. Any pattern that crosses threshold (and isn't in cooldown) fires a macOS desktop notification + `log.warn`. The cooldown map is module-level so it persists for the lifetime of the dashboard process. Tests can call `__resetFailureAlertCooldown()` to clear it. Scan errors are swallowed — a notification glitch must never stall the dashboard process.

## Cleaning Old Tracker Files

- `cleanOldTrackerFiles(maxAgeDays, dir)` — deletes JSONL files whose filename date (YYYY-MM-DD) is older than `maxAgeDays`. Returns count deleted.
- `cleanOldScreenshots(maxAgeDays, dir)` — still available for **manual**/`npm run clean:tracker` use — deletes PNGs in `.screenshots/` whose filename-embedded ms timestamp is older than `maxAgeDays`. Not invoked on dashboard startup or `GET /api/preflight` (the dashboard uses `sweepStaleRunScreenshots` instead — see below).
- `sweepStaleRunScreenshots(dir, screenshotsDir, maxAgeDays = 30)` (`src/tracker/state/screenshot-sweep.ts`) — **lifecycle-tied** screenshot cleanup. Two passes: (1) joins `runs` (where `terminal_at` is older than `maxAgeDays`) with `files` (`kind='screenshot'`) and deletes both the PNGs and the rows in one transaction, zeroing `runs.screenshot_count`; (2) file-age backstop that deletes orphan PNGs older than `maxAgeDays` (parsed from the ms-embedded filename, mtime fallback) whose path is not registered in `files`. Per-file delete failures are logged via `log.warn` and the sweep continues. No-op when the projection isn't ready. `runs.terminal_at` is stamped by `applyTrackerEntry` the first time a run reaches a terminal status (`done` / `failed` / `skipped`).
- `npm run clean:tracker` — CLI wrapper in `src/scripts/ops/clean-tracker.ts`. By default cleans tracker JSONL + screenshots. Accepts `--days N` (default 7), `--dir PATH`, `--screenshots-dir PATH`, `--no-screenshots`, `--screenshots-only`.
- **Dashboard cadence:** `createDashboardServer` runs three startup operations: a one-time `cleanOldTrackerFiles` prune at **30 days** (JSONL only), a single `sweepStaleRunScreenshots` pass, then a low-frequency `setInterval` (**6 hours**, `.unref()`) that re-runs the screenshot sweep so long-lived dashboards keep up. `GET /api/preflight` re-runs the JSONL prune at most once per **60s** (throttled in `hono/routes/base.ts`); it does NOT re-trigger the screenshot sweep. Pass `{ noClean: true }` or `--no-clean` to skip startup operations.

## `withTrackedWorkflow(workflow, id, data, fn)`

Lifecycle wrapper for all workflows. Auto-emits JSONL events:
- **pending** — immediately on start
- **running** — via `setStep(step)` callback at phase transitions
- **done** — automatically on successful return
- **failed** — automatically on thrown error (with error message)

```ts
await withTrackedWorkflow("separations", docId, {}, async (setStep, updateData) => {
  setStep("authenticating");
  // ... auth ...
  updateData({ name: employeeName });
  setStep("extraction");
  // ... extract ...
}); // auto-emits done or failed
```

- `setStep(step)` — emits a `running` event with the step name
- `updateData(d)` — merges data into the entry (e.g. employee name discovered mid-workflow)
- The kernel nests this wrapper inside `withLogContext` for every production run path
- Tracker functions (`updateOnboardingTracker`, etc.) are Excel-only — they no longer call `trackEvent()`
- `opts.onCleanup` — callback for resource teardown (e.g. closing browsers) on both success and failure
- `opts.preAssignedRunId` — pre-assigned runId for batch mode (caller pre-emits pending for all items, then processes sequentially)
- `opts.preAssignedInstance` — pre-assigned workflow instance name (e.g. `"EID Lookup 1"`) for batch runners. When present, `withTrackedWorkflow` **skips its own `workflow_start` / `workflow_end` emits and skips calling `generateInstanceName`** — the caller (`withBatchLifecycle` in `src/core/batch-lifecycle.ts`) owns the batch-level lifecycle. The value is also stamped into each tracker row's `data.instance` so the dashboard session drawer can join per-item rows back to the batch instance.
- Calls `setLogRunId(runId)` to inject `runId` into the `AsyncLocalStorage` log context so log entries include it
- **SIGINT handler**: Registers a `process.on("SIGINT")` handler that writes a `failed` tracker entry and log entry synchronously via `fs.appendFileSync` before calling `process.exit`. Also kills Playwright Chrome via `wmic` on Windows.

## Dashboard SSE Server

`startDashboard(opts?)` starts an HTTP server (default port 3838 — see `StartDashboardOptions`). Endpoints:

- `GET /api/workflows` — list all workflows with JSONL data
- `GET /api/workflow-definitions` — kernel registry payload (label, steps, detailFields, getName/getId)
- `GET /api/dates?workflow=X` — list available dates for a workflow
- `GET /api/entries?workflow=X` — return all tracker entries (JSON)
- `GET /api/logs?workflow=X&id=Y[&runId=Z]` — return log entries (JSON)
- `GET /api/runs?workflow=X&id=Y[&date=D]` — past runs for an itemId
- `GET /api/screenshots?workflow=X&itemId=Y` — list `.screenshots/<workflow>-<itemId>-...png` for a failed entry
- `GET /screenshots/<filename>` — stream a PNG with path-traversal guard (`resolveScreenshotPath`)
- `GET /api/search?q=Q[&days=N]` — cross-workflow tracker entry search (`buildSearchHandler`)
- `GET /api/selector-warnings?days=N` — aggregated selector-fallback warns across N days (default 7)
- `GET /api/failures` — `FailureRow[]` for failed entries on a given date across all workflows (`buildFailuresHandler`). Same latest-run-per-`(workflow,id)` dedup pattern; takes optional `?date=YYYY-MM-DD` (default today).
- `GET /api/preflight` — startup checks + cleanedFiles count
- `GET /api/rosters` — list xlsx rosters in `.tracker/rosters/` + `src/data/`, newest first (consumer: `RunModal`)
- `POST /api/ocr/prepare` — multipart/form-data; fire-and-forgets `runWorkflow(ocrWorkflow)` and returns `{ok, sessionId, pdfPath}`. Body cap: 50MB.
- `POST /api/ocr/approve-batch` — JSON `{sessionId, records[]}`; expands to N kernel queue items via `enqueueFromHttp` for the downstream form-type daemon.
- `POST /api/ocr/discard` — JSON `{sessionId, reason?}`; emits `failed` step `discarded`.
- `POST /api/ocr/reocr-whole-pdf` — JSON `{sessionId}`; re-runs OCR for every page.
- `POST /api/ocr/retry-page` — JSON `{sessionId, pageNumber}`; retries one OCR page.
- `POST /api/ocr/force-research` — JSON `{sessionId, records[]}`; re-dispatches eid-lookup for a subset of records flagged for forced research.

Implementation: `src/tracker/dashboard/hono/routes/ocr.ts`.
- **`GET /events/hub?subs=<urlencoded JSON array>`** — single multiplexed SSE connection. Each element is `{ id, topic, params }`; every `data:` line is a `HubEnvelope` `{ sub, data, event? }` for the matching subscription id (`src/tracker/dashboard/hono/routes/hub.ts`). **Legacy per-topic URLs** (`/events`, `/events/logs`, …) were removed (2026-05-08); `registerEventRoutes` is intentionally empty (`hono/routes/events.ts`).
- **Topic registry** — `src/tracker/dashboard/hono/topics.ts` (`topicRegistry`, `parseSubsQuery`). **Emitters** register in `topics-emitters.ts`. Registered topics include: `entries` (payload includes enriched rows + `wfCounts` + `failureCounts` from `computeFailureCounts` — drives `FailureBell` without an extra HTTP fetch for counts), `logs`, `sessions`, `runEvents`, `captureSessions`, `telegram`.
- **Run events filtering** — events lacking `runId` (batch-scope `Session.launch`) are attributed via `workflowInstance` + per-run time window inside `filterEventsForRun` (`src/tracker/dashboard/session-state.ts`), **re-exported** from `src/tracker/dashboard.ts` for unit tests and hub code.

In dev, the React dashboard is served by Vite (port 5173) and proxies API calls to 3838. In prod, `dashboard --prod` serves `dist/dashboard/index.html` from the Hono dashboard server.

## JSONL File Format

Two file types per workflow per day in `.tracker/`:

- **Entries**: `.tracker/{workflow}-{YYYY-MM-DD}.jsonl` — one JSON line per `trackEvent()` call
- **Logs**: `.tracker/{workflow}-{YYYY-MM-DD}-logs.jsonl` — one JSON line per `log.step/success/error/waiting` call (via `withLogContext`)

## `appendRow(filePath, columns, data)`

Appends a single row to an `.xlsx` file. Creates the file and/or worksheet if missing. Worksheet name is today's date as `YYYY-MM-DD`.

## Gotchas

- **Critical ExcelJS quirk**: After `readFile()`, ExcelJS loses column key mappings. Code re-applies keys in a loop — without this, `addRow(data)` won't map object keys correctly.
- **Excel daily worksheet name** — `exports/spreadsheet.ts` uses `dateLocal()` from `jsonl.ts` for the tab name (`YYYY-MM-DD`), matching tracker JSONL filename dates (local calendar day, not UTC `toISOString()` slice).
- Tracker `.xlsx` files belong inside their workflow folder, never in project root
- Dashboard port 3838 conflict: logs and skips if port in use (another instance running)
- `withTrackedWorkflow` does NOT call `withLogContext` — use both: `withLogContext` wraps `withTrackedWorkflow` to get both log streaming and entry tracking
- **Do NOT use `markStaleRunningEntries`** — was removed because it falsely marked running entries as "failed" with fake "Process interrupted — no heartbeat" messages. Use SIGINT handler in `withTrackedWorkflow` instead for proper cleanup on Ctrl+C.
- **SIGINT writes must be synchronous** — `process.on("SIGINT")` handler cannot await async functions (process exits before they complete). Use `fs.appendFileSync` directly when writing final tracker/log entries.
- **`trackEvent` / `appendLogEntry` are synchronous** — do NOT wrap `appendFileSync` in a mutex. POSIX `write(2)` with `O_APPEND` is atomic at the OS level, and Node is single-threaded within a process. An `async-mutex` wrapper makes the call fire-and-forget (returns a `Promise` but signature is `void`), causing reads-after-write to miss data.
- **Queue/control state has two outputs** — when changing queue/control behavior, update SQLite state and JSONL audit together. SQLite is live truth; JSONL is audit/history. Never add a dashboard control that only mutates process-local state.

## Adding Tracking for a New Workflow

Kernel workflows get tracking for free — `defineWorkflow({ ... })` registers dashboard metadata and `runWorkflow` (or batch/pool/daemon runners) wraps each run in `withLogContext` + `withTrackedWorkflow`. Do NOT call `withTrackedWorkflow`, `trackEvent`, or `setStep` from a handler; use `ctx.step(...)` / `ctx.markStep(...)` / `ctx.updateData(...)` instead.

If you ever add a **non-kernel** one-off workflow, it would need an explicit `register(metadata)` call (from `src/core/kernel/registry.ts`) and a custom outer wrapper — no such workflows ship under `src/workflows/*` today; use `defineWorkflow` for all new work.

## Lessons Learned

- **2026-05-22: Recent JSONL latest-row walkers should use `findLatestEntryForPredicate`.** The helper is now the canonical newest-first, malformed-line-tolerant scan for recent workflow tracker JSONL. `findLatestTrackerDateForRun`, restart-sweep latest-by-run maps, OCR session metadata readers, OCR approval data lookup, and dashboard cancel instance lookup all route through it with explicit lookback windows where needed. Data-field predicates stay JSONL-only because no SQLite index covers them; callers preserve their public no-match values (`undefined`, `null`, or `{}`) at their own boundary.
- **2026-05-22: SSE hot-path caches and sweep cost fixed (5 performance defects).** Five defects caused measurable event-loop stalls and per-client cache thrashing:
  - **Fix 1 — `ttlMemoize` single-slot thrash.** `ttlMemoize` stored exactly one `{ key, computedAt, value }` entry, so two SSE clients viewing different workflows/dates alternated keys and never hit the cache — `queryEntriesPayload` ran per-tick-per-client. Fix: replaced the single slot with a keyed LRU `Map` (cap 16, delete-on-hit + re-set insertion-order LRU). Same fix applied to `getCrossWorkflowCounts` in `entries-payload.ts` (also single-slot, key `dir::date`). Public signature of `ttlMemoize` unchanged; both `reset()` hooks preserved. Tests: `tests/unit/tracker/perf-hot-path-caches.test.ts`.
  - **Fix 2 — `state/queries.ts` re-prepared every SQL statement on every call.** All 8+ exported query functions called `db.prepare(...)` inline on each invocation, paying full SQL re-parse + re-plan cost. Fix: mirrored `apply.ts`'s `WeakMap<Database, CachedStatements>` pattern — `readStmts(db)` prepares all statements once per `Database` handle and caches them. `selectRunEventsForRun` (which had a dynamic `itemId` filter) now uses two pre-prepared variants (`WithItem` / `NoItem`) selected at call time. `querySessionEventsForRun` (which built a dynamic `WHERE` clause) likewise has two variants. All function signatures and return types unchanged.
  - **Fix 3 — `sessionEventsCache` was unbounded.** `const sessionEventsCache = new Map<string, SessionFileCache>()` had no eviction; `cleanOldSessionFiles` deleted the files but never the cache entries, which pinned parsed event arrays in memory one entry per calendar day forever. Fix: cap at 64 entries with delete-on-hit + re-set LRU, same pattern as `parseCache`. `__resetSessionEventsCacheForTests` kept.
  - **Fix 4 — `sweepInterval` was not `.unref()`'d.** `setInterval(() => { void scanFailurePatterns(); ... }, 15_000)` in `server.ts` would prevent the Node process from exiting if the server was closed but the interval not explicitly cleared. The sibling `screenshotSweepInterval` was already `.unref()`'d. Fix: one-line `sweepInterval.unref()`.
  - **Fix 5 — `scanFailurePatterns` did an O(all-workflows × full-file) JSONL scan every 15s.** `workflows.flatMap((w) => readEntries(w))` ran every 15s; on a busy daemon the parse cache missed and this caused a full synchronous `readFileSync` + `JSON.parse` of every workflow's day file. `detectFailurePattern` only needs `status=failed + error` fields. **Moved to SQLite-first** because `run_events` has all needed fields (`workflow`, `item_id`, `run_id`, `event_ts`, `error`) and `idx_run_events_workflow_date (workflow, tracker_date, event_ms)` bounds the scan to today's rows in one query. `scanFailurePatterns` now accepts `(stateDb, projectionReady, dir)` params; `createDashboardServer` passes its live `stateDb` and `projectionReady` flag. JSONL `flatMap` kept as fallback when projection not ready.

- **2026-05-21: SQLite projection consistency hardened (3 defects).** The "SQLite is live truth; JSONL is audit/history — update both together" invariant had three holes; all fixed in `state/runtime.ts`, `state/db.ts`, and the `jsonl.ts` SIGINT handler:
  - **C2 — swallowed projection-write failures.** `apply*Live` in `state/runtime.ts` caught apply errors with a bare `log.warn`. Since `trackEvent` writes JSONL *first* then applies to SQLite, a thrown apply left the projection silently behind by one row. Fix: track *consecutive* apply failures per `(dir, date)` in a module Map, reset the counter on any success, elevate the log from `warn` to `error`, and after `REBUILD_FAILURE_THRESHOLD` (3) consecutive failures schedule a **deferred, guarded** `rebuildProjectionForDate` via `setImmediate` (so it never blocks the synchronous `trackEvent` hot path; a rebuild failure is caught and logged, never thrown). An in-flight rebuild for a `(dir, date)` is not re-triggered (`rebuildsInFlight` Set). The sync `apply*Live` calls still never throw — a transient SQLite lock must never abort a workflow. Design choice: deferred-rebuild over a synchronous retry because retrying inline would stall the workflow thread on the exact lock contention that caused the failure; deferred-rebuild over a monitoring subsystem because the project wants minimal, "fail loud over auto-correct" recovery, and the rebuild is itself the recovery. Test hook: `__resetProjectionFailureStateForTests()`.
  - **H1 — SIGINT handler updated JSONL but not SQLite.** The `process.on("SIGINT")` handler in `withTrackedWorkflow` writes the terminal `failed` tracker line + `error` log line directly via `appendFileSync` (it cannot await `trackEvent`/`appendLogEntry` before `process.exit`), which bypassed `applyTrackerEntryLive`/`applyLogEntryLive`. After every Ctrl+C, JSONL said `failed` but `runs.latest_status` stayed `running` — the dashboard showed killed runs stuck forever. Fix: new `applySigintTerminalToProjection(trackEntry, logEntry, source, dir)` in `state/runtime.ts`, called from the SIGINT path **after** the `appendFileSync` writes. It gets the cached DB handle via `openStateDb` (never opens a fresh DB), applies the same two entries synchronously (`node:sqlite` is sync; signal handlers run between event-loop ticks so there is no concurrent-transaction hazard), and swallows any DB error locally — JSONL-first, SQLite-second, startup rebuild as the backstop. No-op when the projection isn't ready.
  - **C1 — `isStateDbReady` cached `ready=true` for the whole process.** `readyCache` was only ever cleared by `closeStateDbForTests`; if `state.db` was deleted/corrupted/version-regressed after first open, `isStateDbReady` kept returning `true` and every projection write fell into the (now elevated) error path. Fix: cache the DB file's identity fingerprint (`inode + size + mtimeMs`) captured when readiness was confirmed; each `isStateDbReady` call does a cheap `statSync` and compares — if the file vanished or changed, the cache is invalidated and the next call re-runs the full `SELECT version` schema probe. Design choice: `statSync`-fingerprint over a TTL because it is precise (invalidates exactly when the file actually changes, not on a timer) and over a per-call `SELECT version` because `isStateDbReady` is on the `trackEvent` hot path — a `statSync` is far cheaper than opening a connection + querying. `closeStateDbForTests` still clears the cache, so the existing test hook is unaffected.
  - Tests: `tests/unit/tracker/state-runtime-consistency.test.ts` (C2 deferred-rebuild + below-threshold + counter-reset, H1 SIGINT apply + unready no-op) and two new cases in `state-db.test.ts` (C1 deleted-file + corrupted-file invalidation).
- **2026-05-20: Phase 6 — workflow runtime cleanup complete.** HTTP operator routes (`/api/cancel-queued`, `/api/retry`, `/api/retry-bulk`, `/api/delete-bulk`, `/api/task/force-stop`, `/api/cancel-active-bulk`) remain as stable paths but all funnel through `performWorkflowAction`. Legacy bulk factories (`buildRetryBulkHandler`, `buildDeleteBulkHandler`, `buildCancelActiveBulkHandler`) are test-only helpers, not route adapters. Architecture guards in `tests/unit/architecture/runtime-policy-coverage.test.ts` require every `defineWorkflow` export to declare `runtimePolicy` and validate action descriptor kind/scope/source. Dashboard controls gate on projection `actions` via `src/dashboard/lib/workflow-action-utils.ts`.
- **2026-05-21: Bulk retry/delete routes accept only the batch visible-view override.** `/api/retry-bulk` and `/api/delete-bulk` still default old callers to `source:"queue-panel"` + `scope:"group"`, but route parsing now forwards `source:"batch-view"` and `scope:"visible-view"` when explicitly sent by the dashboard batch footer. Unsupported source/scope strings intentionally fall back to the legacy defaults instead of exposing arbitrary workflow-action combinations.
- **2026-05-21: OCR discard is a central cancel variant.** `/api/cancel-queued` and `/api/task/force-stop` accept optional OCR discard context (`ocrSessionId`, OCR `runId`, `parentWorkflow`, `parentRunId`, `parentItemId`, `formType`, `reason`) and forward it to `performWorkflowAction`. A `cancel` with `ocrSessionId` bypasses normal task target resolution and calls `buildOcrDiscardHandler`, preserving parent-row mirror writes and delegated child deletion without requiring `/api/ocr/discard-prepare` as the dashboard component transport.
- **2026-05-21: Workflow action targets are workflow-explicit.** `WorkflowRuntimePolicy` action buckets hold static `WorkflowActionPolicy` entries only; resolved projections and `WorkflowActionRequest.targets` carry concrete target rows. Each target must include `workflowId` so delegated/tree actions can cross workflow boundaries without inheriting the request-level workflow. Hono route wrappers still default legacy request items to the request workflow, but new projection/client callers should send target-level `workflowId`.
- **2026-05-20: Phase 4 runtime policies must drive live action scope and context payloads, not only projections.** Oath Upload declares row cancel as `scope: "tree"` so queue controls must include descriptor scopes in `/api/cancel-queued` and `/api/task/force-stop`; the Hono wrappers then pass that scope to `performWorkflowAction`, where only explicit `tree` walks `runs.parent_run_id`. `buildJsonlEventsPayload` also expands workflows whose policy sets `rootRowPersistsThroughChildren`, so the Oath Upload tab includes OCR and oath-signature child rows under the same root context instead of relying on tests to manually pass cross-workflow rows.
- **2026-05-20: Central workflow action engine (Phase 2).** `src/tracker/dashboard/actions/perform-workflow-action.ts` is the single dispatcher for operator cancel / retry / delete / bump. It validates scope + source, resolves the blast radius (`resolve-targets.ts`), then routes to the **unchanged** low-level ops handlers (`ops/cancel.ts`, `ops/retry.ts`, `ops/delete.ts`, `ops/queue.ts`) and OCR `discard.ts`. Six HTTP routes (`/api/cancel-queued`, `/api/task/force-stop`, `/api/cancel-active-bulk`, `/api/retry`, `/api/retry-bulk`, `/api/delete-bulk`) are now thin wrappers: they translate their body into a `WorkflowActionRequest`, call `performWorkflowAction`, then map the `WorkflowActionResult` back to their legacy response shape (`toSingleActionResult` / `toForceStopActionResult` / `toBulkActionResult` in `routes/ops.ts`). Scope discipline: `row` / `group` / `visible-view` use the caller-provided targets **verbatim** — no hidden-row expansion, so a queue-panel action can't reach batch-view-only rows; only `tree` walks `runs.parent_run_id` descendants. A `cancel` with `ocrSessionId` set routes to file-scope OCR discard. `source: "daemon"`, `scope: "daemon"`, and `action: "stop-daemon"` are rejected — daemon stop is operational, not a workflow cancel. `WorkflowActionRequest.targets` carries `{ workflowId, id, runId?, date?, status? }`, not bare run ids: every low-level handler keys on the tracker item id, and resolving id from runId would need the SQLite `runs` projection (absent for freshly-queued tasks). The bulk factories (`buildCancelActiveBulkHandler`, `buildRetryBulkHandler`, `buildDeleteBulkHandler`) remain exported for unit tests only (Phase 6). Routes not in the migration list (`/api/cancel-running`, `/api/delete-entry`, `/api/queue/bump`) still call their handlers directly.
- **2026-05-20: Workflow runtime projections are additive adapters over queue surfaces.** `src/domain/workflow-runtime/projection.ts` wraps `buildTrackerQueueSurfaces` output into the future `WorkflowRunProjection` shape while queue rendering still uses the legacy surfaces directly. Keep Phase 1 behavior-preserving: group projections must not expose raw parent run ids as subtitles, and OCR EID/active-check fan-out children remain `delegation-member` rows rather than promoted batch delegations.
- **2026-05-18: Screenshot retention is lifecycle-tied via `runs.terminal_at`.** Migration 9 adds `runs.terminal_at` (NULL while still running, stamped on first terminal transition by `applyTrackerEntry`). `sweepStaleRunScreenshots` joins terminal-runs-older-than-30-days with `files` (`kind='screenshot'`) and deletes both, then a file-age backstop catches orphan PNGs and screenshots from stuck (non-terminal) runs that were nonetheless older than 30 days by filename ts. Wired into `createDashboardServer` as a one-shot startup call plus a 6h `setInterval` (`.unref()`). Sweep is no-op when the projection isn't ready; per-file delete failures are warned, never thrown — must not stall the dashboard.
- **2026-05-18: Hono JSON body parsing rejects non-object payloads.** `readJsonRequest` now accepts empty bodies as `{}` but rejects arrays, primitives, and `null` with a 400-shaped parse failure. Routes that call it directly should return parse failures as client errors, not 500s; downstream handler code can assume `body.foo` reads from a real object.
- **2026-05-18: JSONL tracker readers can opt into per-line validation.** `readJsonlCached` keeps parsed line metadata and accepts a validator so tracker-entry readers (`readEntries`, `readEntriesForDate`, `findLatestEntryForPredicate`) skip malformed/wrong-shape rows with file+line diagnostics instead of poisoning dashboard state. `findLatestEntryForPredicate` also accepts legacy `ts` rows and fills missing `workflow` from the searched workflow so oath-upload cross-day recovery still sees older JSONL. Log readers (`readLogEntries`, `readLogEntriesForDate`) now use `isLogEntry` in the same pattern — both `TODO(jsonl-guard)` markers removed. `isLogEntry` checks `workflow`, `itemId`, `level` (against the six-value literal union), `message`, and `ts`.
- **2026-05-18: SQLite projection read mappers must validate drift at the boundary.** `src/tracker/state/queries.ts` now guards `typed_data_json` with the actual `TypedValue` discriminants, treats a missing `schema_version` row as `schemaVersion: 0`, and drops unknown `latest_status` values from sidebar-count aggregation with `log.warn` diagnostics. Keep new SQLite read-side guards hand-written and local to the mapper; production tracker code must use `log.warn`, not `console.warn`, or the architecture test fails.
- **2026-05-14: Queue title metadata is the canonical row-title contract.** New queue title behavior should stamp `data.__queueTitle`, `data.__queueTitleKind`, and, for delegated work, `data.__queueRootTitle`. Keep `__name` only as compatibility/display fallback; do not add new workflow-specific title hacks in pre-emit hooks.
- **2026-05-19: Row archetype resolver keeps legacy fallbacks.** Even though new rows should stamp `data.archetype`, `resolveRowArchetype` must still recognize legacy `data.mode === "prepare"` as `batch-parent`, `taskRole === "utility"` as `passive-child`, and `requestRole === "delegation-dispatch"` as `dispatch`. Dashboard JSONL history and older tests still rely on these fallbacks, especially OCR prep rows.
- **2026-05-21: Multi-file prep grouping removed.** `/api/ocr/prepare` no longer accepts `originBatchRunId` / `originBatchSubject` (the 2026-05-18 grouping mechanism is gone). `writeOriginParentPending` always emits a standalone prep row with its own `runId` and no `parentRunId`. Uploading N PDFs creates N independent top-level prep rows — operator request: treat each PDF as a separate single run, not one batch task.
- **2026-05-14: OCR approval must pre-emit downstream child rows before daemon auth.** `/api/ocr/approve-batch` marks the OCR/prep parent approved synchronously, then fire-and-forgets daemon enqueue. Any downstream workflow it fans out to (oath-signature / emergency-contact) must receive an `onPreEmitPending` callback so child tracker rows appear before `ensureDaemonsAndEnqueue` waits on daemon spawn/auth. Without this, operators see the OCR row move but the target workflow's per-record rows stay invisible until after Duo/browser auth. Regression: `tests/unit/tracker/ocr-http.test.ts` (`buildOcrApproveHandler provides downstream pre-emit hook before daemon auth`).
- **2026-05-08: Migration 6 (drop run_events.raw_json) is a full table rewrite on first boot.** SQLite 3.35+'s `DROP COLUMN` rebuilds the table rather than mutating metadata in place. On a prod state DB with millions of `run_events` rows, the first `openStateDb()` after pulling Tier 3 will pause for tens of seconds while the rewrite runs — operators may see the dashboard "hang" on startup. Subsequent boots are unaffected. If the pause is unacceptable on a particular deployment, the workaround is to back up `.tracker/state.db`, delete it, and let the projection rebuild on next dashboard launch (cost: full JSONL replay, but background-free). Fresh DBs running migrations 1→6 in sequence pay the cost on a near-empty table and aren't affected.
- **2026-05-07: Tracker storage optimization landed (6-task fan-out).** Five concrete scaling smells fixed in lockstep:
  - **Sessions rotation**: writes go to `sessions-YYYY-MM-DD.jsonl` matching the timestamp's local date. Reads aggregate every dated file plus a legacy `sessions.jsonl` until it ages out via `cleanOldSessionFiles(maxAgeDays, dir)`. No one-time migration — the legacy file is read alongside dated files. Internal callers (`state/rebuild.ts`, `state/jsonl-source.ts`, screenshots' `groupedHandlerLegacy`, the SSE handler's JSONL fallback) all use `readSessionEvents(dir)` (rotation-aware) instead of `getSessionsFilePath(dir)` (today-only) when they need to surface historical events. Tests that wrote `sessions.jsonl` directly via `appendFileSync` had to switch to `emitSessionEvent` or `readSessionEvents` to keep working.
  - **`runEvents` hub topic from SQLite**: emitter queries indexed `session_events` (idx_session_events_run, idx_session_events_instance) when `deps.projectionReady && deps.stateDb`; otherwise falls back to `readSessionEvents` aggregation. The gate uses `deps.projectionReady && deps.stateDb` not `isStateDbReady(deps.dir)` — the deps flag is the *intentional* test seam; the dir-based check ignores it.
  - **/api/screenshots from SQLite files table**: grouped handler queries `queryScreenshotsForItem(db, {workflow, itemId})` first (idx_files_owner). When SQLite returns zero rows OR the projection isn't ready, falls back to `groupedHandlerLegacy` which now reads `readSessionEvents(dir)` (rotation-aware). Side fix: `applyScreenshotFiles` now joins through `runs` to set `workflow` + `item_id` on `files` rows (was a pre-existing gap that would have made `queryScreenshotsForItem` always return zero — found by Task 3's parity test).
  - **clean:tracker prunes SQLite**: `pruneStateDb(dir, cutoffDate)` deletes from `run_events`, `runs`, `items`, `logs` (TEXT `tracker_date`); `session_events` (INTEGER `ts_ms`); `files`, `task_attempts` (TEXT `created_at`); `worker_commands` (TEXT `requested_at` — NOT `created_at`). All in one `transaction(db, fn)` from the shim, then a best-effort `VACUUM` outside the transaction. Returns counts per table for the log line.
  - **parseCache LRU**: bounded at `PARSE_CACHE_MAX = 64` via Map insertion-order + delete-on-hit-then-re-set. Test-only `__resetParseCacheForTests` / `__getParseCacheSizeForTests` exposed. Long-lived dashboard sessions walking historical dates no longer leak.
  - **JSONL queue backend retired**: `legacyReadQueueState`, `queueBackend()`, and the `HRAUTO_QUEUE_BACKEND=jsonl` dispatch are gone. SQLite is the sole queue authority. `.queue.jsonl` audit-append preserved for `tail -f` debugging — readers must not consume it as state.
- **2026-05-07: `node:sqlite` returns null-prototype rows; spread `{...row}` before `assert.deepEqual`.** `node:sqlite`'s row objects don't have `Object.prototype` as their prototype, so `assert.deepStrictEqual(row, {...})` fails even when the contents match. Spread the row (`{...row}`) before comparing, or map an array of rows with `.map((r) => ({...r}))`. This caught two pre-existing tests (`tests/unit/tracker/state-jsonl-live-apply.test.ts`, `tests/unit/tracker/tasks/scheduler.test.ts`) when Node 26's stricter assertion surfaced the issue. New tests that compare SQLite rows must follow the same pattern.
- **2026-05-07: Node 26 hangs the test suite without `--test-force-exit`.** A few tests (`tests/unit/workflows/oath-upload/wait-ocr-approval.test.ts`, `tests/unit/tracker/watch-child-runs.test.ts`) leave polling intervals or watchers behind; under Node 26's process-based test isolation that prevents the test process from exiting after assertions complete. `scripts/run-tests.mjs` passes `--test-force-exit` to make the test runner kill the process when all tests have run, regardless of lingering handles. Without that flag, `npm run test` hangs indefinitely on Node 26.
- **2026-05-07: Cross-workflow counts cached in `/events` JSONL fallback.** *(Historical — pre `/events/hub`.)* `getCrossWorkflowCounts` wrapped the per-workflow `wfCounts` + `failureCounts` aggregation behind a 1s TTL so a 1 Hz stream did not re-read every workflow's JSONL on every tick. Today's **`entries`** hub emitter preserves the same idea inside `topics-emitters.ts` / entries payload builders. Reset via `__resetAllDashboardCachesForTests()` in `src/tracker/test-caches.ts`, or `__resetCrossWorkflowCountsCacheForTests()` for surgical tests. SQLite projection path remains uncached (O(query)).
- **2026-05-15: Dashboard cache test resets are centralized.** `tests/setup.ts` calls `__resetAllDashboardCachesForTests()` before each test so parse-cache, session-state, preflight-throttle, and cross-workflow-count TTLs cannot leak between unrelated suites. Keep individual reset exports for targeted cache tests, but add new dashboard-wide test caches to the aggregator in `src/tracker/test-caches.ts`.
- **2026-05-15: JSONL fallback should use SQLite-derived counts when available.** `buildJsonlEventsPayload` still replays JSONL for the cold-start fallback, but screenshot counts should come from `runs.screenshot_count` whenever the projection DB is ready. Only fall back to scanning `.screenshots/` when SQLite is unavailable. Shared historical probes belong in `findLatestEntryForPredicate` instead of workflow-local JSONL loops.
- **2026-05-21: JSONL fallback tests must pass real today separately from selected date.** `buildJsonlEventsPayload(workflow, date, today, dir)` treats `date === today` as the live/today stream and reads `readEntries(...)`; historical-date tests that write via `trackEventForDate` must pass a different `today` value or they will accidentally read the local current-day file and return no entries once the hard-coded date is in the past.
- **2026-04-28: Emergency-contact prep endpoints + restart sweep — Superseded.** Original module `src/tracker/emergency-contact-http.ts` and `/api/emergency-contact/{prepare,approve-batch,discard-prepare}` routes were folded into the shared `ocr` workflow. The current OCR routes live in `src/tracker/dashboard/hono/routes/ocr.ts` (`/api/ocr/{prepare,approve-batch,discard,reocr-whole-pdf,retry-page,force-research}`); the restart-sweep is now `sweepStuckOcrRows`.
- **2026-04-10: Dashboard logs empty despite JSONL having data** — `withTrackedWorkflow` generates `runId` but the log context (set by `withLogContext`) didn't include it. Log entries were written without `runId`. The dashboard's `runId` filter (`l.runId === runId`) rejected all logs (`undefined === "3885#1"` → false). Fix: `withTrackedWorkflow` now calls `setLogRunId(runId)` to inject the `runId` into the `AsyncLocalStorage` log context, so future log entries include `runId`. Server-side filter also uses `!l.runId || l.runId === runId` fallback for backwards compat with old entries.
- **2026-04-10: markStaleRunningEntries caused false failures** — `markStaleRunningEntries` was marking running entries as "failed" with a fake "Process interrupted — no heartbeat" message after 30s with no update. This was wrong: entries can legitimately be running for minutes while waiting for Duo MFA or long Kronos searches. Removed entirely. Replaced with SIGINT handler in `withTrackedWorkflow` that writes a proper `failed` entry synchronously on Ctrl+C.
- **2026-04-10: SSE entry enrichment** — Backend now enriches tracker entries with `firstLogTs`, `lastLogTs`, and `lastLogMessage` per (itemId, runId) pair. This lets the frontend show accurate start times, elapsed durations, and the latest log line without fetching full log streams.
- **2026-04-10: Workflow counts from backend** — Moved workflow dropdown counts to backend (`wfCounts` field in SSE payload) so they accurately reflect all workflows, not just the currently selected one.
- **2026-04-14: `readRunsForId` missed past-date runs** — was calling `readEntries(workflow)` which only reads today's JSONL. When viewing a past date in the dashboard, `/api/runs` returned an empty list and the RunSelector showed only the latest run. Fix: added optional `date` param; backend (`/api/runs`) and frontend (`LogPanel`) now forward the selected date so runs from that day's JSONL are returned.
- **2026-04-14: Preflight deleted fresh `sessions.jsonl` on every refresh** — `/api/preflight` checked if any `workflow_start` PID was alive and deleted the entire file if none were, wiping fake/mock demo data on refresh. Replaced with age-gated deletion (only if file untouched >24h) + dead-PID enrichment in `rebuildSessionState` (crashed workflows are marked `active: false` at read time — no file mutation). Preserves recent activity AND provides immediate crash-recovery UX.
- **2026-04-14: Async mutex broke sync reads-after-writes** — `trackEvent` and `appendLogEntry` wrapped their `appendFileSync` calls in `writeMutex.runExclusive(() => …)` and returned `void` without awaiting. The write became fire-and-forget, so tests (and any caller reading back immediately) saw no data. Removed the mutex entirely — `appendFileSync` is already atomic. Restored true synchronous semantics.
- **2026-04-18: Removed runner endpoints + child-process registry** — `src/tracker/runner.ts` and the `/api/workflows/:name/run`, `/api/workflows/:name/schema`, `/api/runs/:runId/cancel`, `/api/runs/active` route registrations + `buildSpawnHandler` / `buildCancelHandler` / `buildActiveRunsHandler` / `buildWorkflowSchemaHandler` factories were deleted from `dashboard.ts`. There is no generic "type JSON and spawn workflow" UI anymore; daemons and composable flows still start from CLI `npm run …`, and the dashboard uses targeted HTTP handlers (OCR prepare, emergency-contact prep, SharePoint download, etc.). The read-only `/api/runs?workflow=X&id=Y` endpoint (used by `RunSelector` to list past runs for an itemId) is preserved — backed by `readRunsForId`.
- **2026-04-19: Session events now carry runId.** `emitSessionEvent` reads from `AsyncLocalStorage` (via `getLogRunId()`) so events emitted during a tracked workflow item are attributable to that run. Events emitted outside a per-item `withLogContext` (batch-scope `Session.launch`) still carry `workflowInstance`, which is used for fallback attribution — see the 2026-04-23 lesson below.
- **2026-04-19: `step_change` dedupe.** When `markStep` fires both a `step` log entry and a `step_change` session event for the same `(workflow, runId, step)` within 50ms, the session event is suppressed. Prevents duplicate "advanced to step X" lines in the dashboard's Events tab.
- **2026-04-19: `createDashboardServer(opts)` factory.** Extracted from `startDashboard` so tests can spin up isolated servers on random ports with per-test tracker directories. `startDashboard` is now a thin wrapper that preserves the CLI singleton behavior.
- **2026-04-21: `computeStepDurations` anchors step 1 at the workflow start (pending ts).** Previously step 1's duration was measured from its own `running` event, which meant the pre-first-step gap (browser launch, session create, pending→first-running latency) was silently dropped — `sum(stepDurations)` didn't equal the global `useElapsed` counter on the dashboard. Now the earliest valid timestamp in the run (normally the `pending` event) anchors step 1's start, so subsequent steps tile the rest of the elapsed time exactly. The global timer value at the moment step N completes equals `sum(step1..stepN)`. Backfilled as 2 new unit tests in `tests/unit/tracker/dashboard.test.ts` ("absorbs the pending→first-running gap into step 1", "tiles elapsed time when no pending event is present"). Knock-on: `computeCacheStepAvgs` — which builds per-step historical averages — includes the absorbed gap in step 1's average for runs that emitted a pending event. That's more honest ("how long does step 1 typically take from workflow start") and matches what the step-pipeline chips already display.
- **2026-04-21: Batch-level instance + authTimings injection (all three batch modes).** `runWorkflowBatch` (sequential), `runWorkflowPool`, and `runWorkflowSharedContextPool` now all wrap their body in `withBatchLifecycle` (`src/core/batch-lifecycle.ts`). Every batch invocation emits exactly ONE `workflow_start` + one `workflow_end` instead of N. Each item's `withTrackedWorkflow` call receives `preAssignedInstance` (skipping its own `workflow_start/end`) and `authTimings` — an array of `{ systemId, startTs, endTs }` recorded by a `SessionObserver` during `Session.launch`. `runOneItem` injects a synthetic `running` tracker entry at `startTs` for each auth step BEFORE the handler runs, so per-item dashboard rows tile elapsed exactly with real per-system auth durations (ucpath 10–20s, crm 15–30s, etc.) rather than collapsing auth into the first handler step. `pool` mode uses one observer **per worker** (worker-scoped `authTimings[]` injected only into items that worker processes); `sequential` + `shared-context-pool` share one observer for the whole batch. SIGINT mid-batch fans out `failed` tracker rows for every un-terminated item + emits one `workflow_end(failed)`. Auth-failure on `Session.launch` fans out `failed` rows attributed to `auth:<firstSystem>` so the dashboard shows the right step.
- **2026-04-21: `generateInstanceName` self-heals dead-pid stale starts.** Workflow instance numbering used to lock forever if a process crashed between `workflow_start` and `workflow_end`. Now any `workflow_start` whose pid is dead AND is older than 60s is treated as "ended" for numbering purposes — the instance number gets recycled. 60s threshold lets legitimately-still-starting-up workflows (pre-first-`workflow_end`) keep their slot. `generateInstanceName` also takes an optional `dir` arg now, threaded through from `withTrackedWorkflow`, so test suites and tools can point it at isolated directories.
- **2026-04-21: runId format coexists.** Two runId formats live side-by-side in `.tracker/*.jsonl` and `sessions.jsonl`:
  - **UUID format (`randomUUID()`):** emitted by the kernel's batch/pool paths — `runWorkflowBatch` (`src/core/workflow.ts:445`) and `runWorkflowPool` (`src/core/pool.ts:36`) generate a UUID per item and thread it into `withTrackedWorkflow` via `preAssignedRunId` (`src/core/workflow.ts:164`).
  - **Legacy `{id}#N` format:** emitted by `withTrackedWorkflow` itself (`src/tracker/jsonl.ts:276`) whenever `preAssignedRunId` is absent — i.e. direct/test callers AND the kernel's single-run `runWorkflow` path. Note the subtlety: `runWorkflow` computes a local UUID at `src/core/workflow.ts:315` for its Stepper / screenshot emitter, but when it delegates to `withTrackedWorkflow` at `src/core/workflow.ts:408` it forwards `opts.preAssignedRunId` (the outer caller's value — usually `undefined`), not the local UUID. The Stepper / makeCtx / makeScreenshotFn use the local UUID; tracker JSONL entries and session events (which resolve `runId` from `getLogRunId()` / ALS — see `src/tracker/session-events.ts:73`) get the `{id}#N` value. So for single-run kernel workflows both tracker entries AND session events use `{id}#N`; only in-process Stepper-scoped artifacts see the local UUID.
  - **Dashboard read-time fallback:** a second, independent fallback lives in `src/tracker/dashboard.ts` (first use at line 801; also 922/1255/1267/1311/1395/1408): `e.runId || \`${e.id}#1\`` synthesizes a runId at read time for entries/logs that have none.
  - **Fallback (2026-04-23, current):** events lacking `runId` are attributed via `workflowInstance`, not pid. `filterEventsForRun` (implemented in `dashboard/session-state.ts`, re-exported from `dashboard.ts`) resolves the requested runId to a tracker entry's `data.instance`, then pulls in no-`runId` events whose `workflowInstance` matches. This replaced the earlier pid+time-window fallback — which had a latent bug for long-lived daemons (a daemon processing items back-to-back shared one pid across batches, leaking batch-scope events between items). Instance-scoping isolates each batch cleanly. Pre-2026-04-21 tracker entries without `data.instance` degrade to primary-only filtering.
- **2026-04-27: Tracker filenames use local date, not UTC.** `getLogPath` / `getLogFilePath` previously derived "today" via `new Date().toISOString().slice(0,10)` (UTC). Past local 5pm PDT, UTC has rolled to the next day, so reads/writes for "today" hit a different file than the dashboard's date picker was showing — `/api/retry`, `/api/run-with-data`, `/api/save-data`, `/api/cancel-queued`, and the orphan-sweep tracker emits all returned `no tracker entry found for id=X`. Fixed by an exported `dateLocal(d?: Date)` helper used in every "today"-shaped derivation across `jsonl.ts`, `dashboard.ts` (3 SSE endpoints + 2 cutoff-date computations), `session-events.ts`, `exports/spreadsheet.ts`, plus a frontend mirror at `src/dashboard/lib/utils.ts` consumed by `App.tsx`, `TopBar.tsx`, and `useEntries`. Existing UTC-named files keep their names — there's no migration; new writes go to local-named files. Tracker files now roll at local midnight.
- **2026-05-18: Orphan-sweep grace restored to 5 min after Oath OCR/EID race.** `scanOrphanedQueueItems` briefly used 0 ms grace based on an assumption that daemon lockfiles always exist before queue rows are visible. In practice, Oath Signature OCR can enqueue an `eid-lookup` child while a new daemon is still launching/authenticating; the sweep then marked the fresh child failed with "No alive daemon available", blocked the OCR parent, and the child later completed too late to recover the OCR row. Keep `ORPHAN_QUEUE_GRACE_MS = 5 * 60 * 1000` unless enqueue/spawn ordering is proven by tests across dashboard delegation and Duo startup.
- **2026-05-19: Legacy OCR discard rows can lack `data.mode`.** `isResolvedPrepEntry` treats `workflow === "ocr" && status === "failed" && step === "discarded"` as resolved before checking the batch-parent archetype. Older discard rows were emitted without `data.mode: "prepare"`; requiring the archetype makes them reappear in sidebar counts. This is intentionally narrower than daemon/queue cancellation, which should stay visible with preserved labels.
- **2026-04-27: Orphan-sweep grace = 5 min (was 90s).** `scanOrphanedQueueItems` was marking dashboard-initiated retries as "No alive daemon available" because the lockfile isn't written until `Session.launch` completes (after every Duo). For separations (4 sequential Duos) that takes 2–3 minutes — well past 90s — so the dashboard backend was killing its own in-flight spawn. New value matches `spawnDaemon`'s own deadline: inside the window the spawn is legitimately progressing; past it the spawn promise has rejected and the item is genuinely orphaned. Considered tracking active spawns in process-local state but rejected — only protects dashboard-initiated spawns, doesn't help CLI-initiated ones, two mechanisms for one job.
- **2026-04-29: Latest-run-per-`(workflow, id)` is the canonical dedup pattern.** Three endpoints now use it: `/api/search` (fixed earlier 2026-04-29 — was keying by `runId` and returning duplicate rows for retries), `/api/preview-inbox` (new, `buildPreviewInboxHandler`), and `/api/failures` (new, `buildFailuresHandler`). The pure shape: two-pass walk — first pass groups by `(id, runId)` keeping the latest tracker line per group, second pass groups by `id` keeping the highest-runId group. `computeFailureCounts` (pure helper, exported for tests) applies the same shape per-workflow for the `failureCounts` field on the **`entries` hub** payload (multiplexed SSE). Universal preview-row "ready for review" predicate: `data.mode === "prepare" && status === "done" && step !== "approved" && step !== "discarded"` — workflow-agnostic by design, so any future workflow that emits prepare-mode rows (currently only emergency-contact) joins the inbox without a code change.
- **2026-04-27: `/api/retry` and `/api/run-with-data` share `reEnqueueEntry`.** Two near-identical handlers in `dashboard-ops.ts` consolidated into one private helper that takes optional `prefilledData`. `buildRetryHandler` calls with `undefined`; `buildRunWithDataHandler` calls with the user's edits (which trigger the merge with `findLatestEntryData` for non-editable carry-over fields like separations' `rawTerminationType`). Two HTTP endpoints retained — frontend has two distinct UI affordances (RetryButton, EditDataTab); collapsing endpoints saves nothing and makes the API less self-documenting. Behavior with `prefilledData = undefined` is byte-identical to the pre-consolidation retry path.
- **2026-05-16: Screenshot pruning removed from dashboard startup + preflight.** Per the 2026-05-11 lesson (retention should be tied to run lifecycle, not dashboard cadence), both the `createDashboardServer` startup call and the 60s `/api/preflight` throttled call to `cleanOldScreenshots` were deleted. Use `npm run clean:tracker` for manual cleanup. Lifecycle-tied cleanup (delete N days after terminal state) is a future follow-up.
- **2026-05-16: `queryEntriesPayload` builds `historyByRun` from already-fetched event rows.** The function was issuing two full-table SELECTs against `run_events` per `/events` tick — the second to build step-duration history. Since `eventRows` (the first query's JOIN result) already contains `item_id`, `run_id`, `event_ts`, `status`, and `step`, `historyByRun` is now built by iterating `eventRows` in a Map loop. Drops each tick from 2 to 1 SELECT against `run_events`.
- **2026-05-16: `watchChildRuns` returns the terminal `TrackerEntry` in each `ChildOutcome`.** The optional `terminalEntry` field is populated by both the SQLite branch (synthetic entry) and the JSONL branch (parsed entry) when an item reaches terminal status. Callers no longer need a follow-up `findLatestEntryForPredicate` scan — `outcomes[0]?.terminalEntry` has the data. `wait-ocr-approval.ts` was the first caller converted. Test mocks that construct bare `ChildOutcome` objects can omit `terminalEntry` since the field is optional.
