# Dashboard

React SPA for real-time workflow monitoring: queue on the left, log/detail surface on the right.

## Stack

- React 19, Vite 8, Tailwind CSS v4, shadcn/ui primitives, lucide-react, sonner.
- HeroUI is only used for the date calendar and its stylesheet.
- Theme tokens live in `index.css`; fonts are Inter and JetBrains Mono.
- No framer-motion.

## UI editing rules

Follow these when adding or editing ANY dashboard component. Items marked **[guarded]** fail `tests/unit/architecture/frontend-tailwind-compliance.test.ts` — run `npm run test:architecture` before declaring UI work done.

- **Color = design tokens only. [guarded]** Never use Tailwind palette classes (`text-sky-500`, `bg-amber-400`, `border-slate-700`) or raw `#hex` / `rgb()` / `hsl()` literals in `.tsx`. Use the semantic tokens — `primary`, `secondary`, `muted`, `accent`, `destructive`, `info`, `success`, `warning`, `border`, `ring`, `foreground`/`background`, `sidebar*` — or the categorical log accents `log-cyan|log-teal|log-violet|log-slate`. House tint pattern: `bg-<token>/12–15` + solid `text-<token>` + `border-<token>/30` (see `shared/status-styles.ts`). Need a color the tokens don't cover? **Add a token** to `index.css` (light `:root` + `.dark` + the `@theme inline` `--color-*` map), don't inline it. The ONLY sanctioned raw hex is `#4ade80` / `#fbbf24`.
- **Icons = lucide, never glyphs.** Use `lucide-react` components — never unicode/emoji characters (`▲ ← ● ⚠ ✓ ✗`) as icons. An icon-only button needs an `aria-label` (or `title` / sr-only text); a decorative icon inside an already-labeled control gets `aria-hidden`.
- **Interactive = real elements.** Use `<button>` / `<a>`, not `<div onClick>`. If a non-button must be clickable, add `role`, `tabIndex={0}`, AND an `onKeyDown` (Enter/Space) plus `cursor-pointer`. Any `<button>` that is not a form submit gets `type="button"`.
- **Focus stays visible.** Don't `outline-none` without a replacement. The global base (`index.css`) gives native elements an `outline-ring/50` focus ring; if you override it, add `focus-visible:ring-2 focus-visible:ring-ring` — a background-tint-only focus state is too weak.
- **Async / live regions announce.** Streaming logs, live counts, validation, and status that change without a user action need `aria-live="polite"` (or `role="alert"` for blocking errors). Buttons firing async actions disable while pending.
- **Contrast + labels.** `text-muted-foreground` (and any `/NN` opacity on it) is for secondary/hint text only — primary titles and meaningful values use `text-foreground`. Every form control needs a `<label htmlFor>` or `aria-label`; a placeholder is not an accessible name.
- **Motion / layout. [guarded]** Animations use Tailwind utilities with `motion-safe:` / `motion-reduce:animate-none` (no `@keyframes`, no inline `animation:`); z-index stays on the `z-*` scale (no `z-[...]`); `<img>` needs `srcSet` + `sizes` + `loading`/`fetchPriority`; use `shrink-0`, not `flex-shrink-0`.

## Operator Text

Use the shared operator subject (`data.__subject`) as primary text for toasts, queue rows, delegation batches, and batch members. Raw run ids/session ids are fallback or debug detail only.

Dashboard controls mutate SQLite control state first and let workers observe commands. JSONL writes are audit/history. Do not add controls that only flip React state or indiscriminately kill Chromium processes.

## Backend

The SSE/API server is Hono under `src/tracker/dashboard/hono/`, created by `src/tracker/dashboard/server.ts`. Vite dev runs on `:5173` and proxies `/api/*` + `/events/*` to `:3838`; prod serves the built dashboard from the Hono server.

The operator server binds to loopback by default. Its access middleware validates Host/Origin, issues one per-process token from `/api/operator/session`, and requires that token on every mutation; `src/dashboard/lib/operator-auth.ts` installs the single fetch wrapper that supplies it AND exports `getOperatorSession()` for mutation paths that can't use `fetch` (RunModal's XHR upload progress). Phone Capture is a separate Hono listener on `port + 1` and exposes only token-scoped phone routes — **PDF / OCR uploads must NOT post there**; they go same-origin to `/api/ocr/prepare` (Vite proxies `/api` in dev). Non-loopback binding requires `HRAUTO_DASHBOARD_ALLOW_LAN=1`, a 16+ character `HRAUTO_DASHBOARD_LAN_PASSWORD`, and the LAN UI origin in `HRAUTO_DASHBOARD_ALLOWED_ORIGINS`; LAN access uses HTTP Basic user `operator` before the ephemeral operator token is issued. Never add wildcard CORS or register dashboard routes on the public Capture app.

Full API and event reference: `docs/engineering/dashboard-api-reference.md`.

## Workflow Metadata

Frontend labels, detail fields, steps, and display helpers come from the server-side workflow registry via `/api/workflow-definitions`. Do not add frontend-side workflow label/detail hardcoding.

For a new workflow:
- Declare `label`, `getName`, `getId`, and `detailFields` in `defineWorkflow`.
- Add log icon patterns only if new message patterns need icons.
- Run the dashboard and verify entries, steps, and detail fields populate from `ctx.updateData`.

## Build

```bash
npm run dev:dashboard
npm run build:dashboard
npm run dashboard
npm run dashboard:watch
npm run dashboard:prod
```

## Lessons Learned

- **2026-07-16: The i9 OCR review groups by PERSON, not by page — one section holds BOTH of a person's I-9 pages.** A scanned I-9 packet holds a Section 2 and a Section 1 page per person, usually NON-adjacent (live packet: Sanchez S2 = page 1, S1 = page 9), so the generic one-section-per-page layout scattered each person across the review. `OcrReviewPane` now branches its render-list memo on `cfg.formKind === "i9"` onto the pure `buildI9RenderList` (`components/ocr/i9-person-groups.ts`): each `formKind:"i9"` record → one `i9-person` entry with pages `[section2Page?, sourcePage]` (S2 first, from the backend's name-based pairing); an `orphanSection2` record → a one-page person section; a CLAIMED Section 2 record renders NO card of its own (its image lives in the person's section); unclaimed `unknown` pages keep per-page rendering so no page is hidden. Rendered by `PrepReviewPersonSection.tsx` (PrepReviewPair's grid, multi-page labeled left column, sticky card). **Approval-gate gotcha:** `requiredPreviewPages` must include EVERY page of every section (`i9SectionPages`) — gating only on `sourcePage` would let approval unblock before the Section 2 images rendered. Other form types are byte-identical (the generic branch is untouched). Pinned by `tests/unit/dashboard/ocr/i9-person-groups.test.ts` (8 cases).
- **2026-07-16: RunModal PDF uploads must post same-origin with the operator session header — never to `port + 1`.** After the 2026-07-15 Capture split, `port + 1` is phone-only; posting `/api/ocr/prepare` there returns 403 without CORS and the XHR surfaces a bare **"Network error"** (Run I-9 Check / every OCR upload). Fix: relative `submitUrl` (Vite proxies `/api` in dev) + `getOperatorSession()` on the XHR (the `fetch` wrapper does not cover XMLHttpRequest). Deleted `lib/upload-url.ts` (`resolveUploadBaseUrl` → `:3839`).

- **2026-07-15: A forwarded Capture URL must terminate on a different route graph, not a host-header filter over the dashboard app.** A shared listener still made every PII read and control route reachable if forwarding signals were absent or spoofed, and wildcard CORS amplified the exposure. The dashboard now binds loopback, validates Host/Origin, authenticates mutations with a per-process token, and sends ngrok to a dedicated phone-only listener. Keep Capture session tokens and operator tokens out of request logs; audit only redacted paths and statuses.

- **2026-06-30: Settings gained 3 new EDITABLE sections + 3 read-only REFERENCE panels ("what exists in the system").** Operator ask: surface more settings, some editable, some read-only "see what exists like row archetypes." **Editable (extend the existing override store):** the `OperatorSettings` contract (`src/domain/settings/types.ts`) + Zod schema (`src/tracker/settings/schema.ts`) gained `urls` (8 system entry URLs — empty = the production default literal in `config.ts`, set to target a test instance; validated as empty-or-http(s)), `capture`, `browserHealth`, `concurrency`, `daemon`, extended `timeouts` (ukg/duo×2/retry) + extended `ocr` (backoff×2 + validation retries). `merge`/`clone` got the new sections (the generic `diff` auto-handles them). **Two wiring patterns:** URLs + timeouts are read DIRECTLY by `config.ts` from `SETTINGS.*` (each daemon loads `config.ts` fresh, so no env needed) — `KUALI_SPACE_URL = SETTINGS.urls.kualiSpace || "<default>"`, `TIMEOUTS.ukgNavigation = SETTINGS.timeouts.ukgNavigationMs`. capture/browserHealth/daemon/ocr-backoff/concurrency are read by CORE modules via env, so each literal was converted to `numEnv("ENV", literal)` (new helper in `utils/env.ts`) and `applyOperatorSettingsEnv` (store.ts) populates the env ONLY for an operator-set field — so unset = the literal default = byte-identical behavior (the proven OCR-settings pattern). Converted sites: `session.ts` `CAPTURE`/`HEALTH_MONITOR_*`, `daemon.ts` `DEFAULT_IDLE_MS`/`DEFAULT_IDLE_REPOLL_MS`, `usage-tracker.ts` `BACKOFF_*`, `ocr/index.ts` `MAX_VALIDATION_RETRIES`, `old-kronos-reports/config.ts` `DEFAULT_WORKERS`. **Read-only reference (NO new endpoint):** content lives in `src/domain/settings/reference.ts` — client-safe + **drift-protected** by keying descriptors on the real domain unions (`Record<RowArchetype,…>`/`Record<QueueRowKind,…>`/`Record<BrowserHealthKind,…>`), so adding an enum member fails the build until documented. `ReferencePanels.tsx` renders 3 panels: **Row & queue model** (archetypes/shapes/kinds/input-subjects/statuses/trace-id), **Workflows & OCR** (the LIVE workflow index from `useWorkflows()` — code/category/archetype/systems/steps; `code`+`archetype` were already in the `/api/workflow-definitions` payload, just added to the client `WorkflowMetadata` type — plus OCR form specs/steps), **System reference** (browser-health verdicts + recovery tags, daemon phases/buckets, idle-refresh systems). `SettingsDialog` got a `kind: "settings"|"reference"` on each section, a "Reference" nav divider, and a content branch that renders `<ReferencePanel>` with no Save footer. Pinned by `store.test.ts` (new env knobs + url round-trip + bad-url 400). No component harness — verified by `typecheck:all` (only the pre-existing `control/ocr/index.js` error) + `test:architecture` (79) + `lint` (clean for these files) + targeted `test` (session/daemon/ocr/old-kronos 151, settings 20) + `build:dashboard` + an API round-trip (POST new sections → sparse persist, bad URL → 400, DELETE reverts) + a headless playwright-cli pass against real `dashboard:prod` (`.screenshots/settings-reference/0{1..8}-*.png`: System URLs, extended Performance, Audit capture, Recovery & daemon, and the 3 reference panels incl. the live workflow-index table).
- **2026-06-30: Workflow Editor — the Data Bank is now DRAG-AND-DROP into the pipeline (drop into a step lane = an "added" row, drop on the canvas = a standalone node), plus hand-drawn edges + Delete-key + inline op editing.** The palette used to be click-only: a click dropped a disconnected `actionNode` at a fixed offset (the "can't drag into the pipeline / no connectivity" complaint), and `nodesConnectable={false}`. **(1) DnD seam.** New pure module `graph/data-bank-dnd.ts` — `DATA_BANK_DRAG_MIME` (`application/x-databank-op`, a custom MIME so stray text/file drags never read as ops), `serializeOpDrag`/`parseOpDragPayload` (total — a foreign/malformed drag yields `null`, never throws), and `opToActionData` (op → `ActionNodeData`, mirrors `lane-build`'s `toLaneOp`). `DataBankPalette` op rows are now `draggable` with `onDragStart` (sets the MIME payload + pushes recent); click-to-add still works as a fallback. **(2) Drop routing (the load-bearing bit).** `GraphCanvas`'s wrapper has `onDragOver`/`onDrop`/`onDragLeave`. The drop target is found via the React Flow node DOM wrapper: `e.target.closest('.react-flow__node')` → `data-id` → `parseStepNodeId` (new export on `graph-build.ts`; `step:<x>`→`x`, else null). Drop ON a step lane → `onAddOpToStep(step, op)`; drop elsewhere → `addActionNode(op, screenToFlowPosition(...))` (a standalone node at the real cursor, not a fixed offset). The hovered step rides page→context `dropTargetStep` and highlights the lane (`ring-primary`). **(3) Added lane-ops live in PAGE state, not as RF nodes.** `WorkflowModifierPage` owns `addedOps: Record<step, AddedLaneOp[]>` (`AddedLaneOp = ActionNodeData & { addedId }`); `configModel` merges it into `StepNodeData.addedOps`, and `LaneShell` renders them as primary-tinted "added" rows (below the read-only mined ops) with a remove ✕ + a `+N added` header chip. They're edited from the **Step inspector's "Added operations" section** (`AddedOpEditor`: Fills-from / Scrapes-into / Note → `onUpdateAddedOp`), since added rows aren't selectable nodes. **Design intent only** (no runtime override — step ops are code-generated; this matches the dual-output model). **(4) Scaffold round-trip.** `mergeAddedOpsIntoModel` (design-spec, pure) folds added ops into the Generate-scaffold model as `action` nodes parented to their step (`parentId`=`step:<step>` → `parentGroup`); `designSpecToGraph` routes a `step:`-parented action node back into `overlay.addedOps` (a FREE action node still → `intentNodes`), and the page seeds `addedOps` from `designOverlay.addedOps` — so dropped ops survive Generate→reload. **(5) Connectivity.** `nodesConnectable` is on; `onConnect` adds an `EDGE_CUSTOM` edge. Custom edges are PRESERVED across the `configModel` re-sync by partitioning the edge state on `type===EDGE_CUSTOM` (a draft keystroke rebuilds the derived edges but must not wipe a hand-drawn one). Config nodes (row/step/coordinator/prep/member/opslane) are `deletable:false`; only placed action/intent nodes + custom edges delete via Backspace/Delete (RF skips input-focused elements, so typing in the inspector is safe). Drawn edges also **restore on reload**: `designSpecToGraph` returns `customEdges` (ONLY `custom`-typed — sequence/delegation/fold are re-derived by `overrideToGraph`, so restoring them would duplicate) and `buildInitialEdges` seeds them into `useEdgesState`'s initial value (the re-sync effect then preserves them). So the whole design surface round-trips consistently — dropped ops, standalone nodes, notes, AND edges all survive Generate→reload. **Pure seams pinned by** `tests/unit/dashboard/workflow-graph/{data-bank-dnd,design-spec,graph-build}.test.ts` (parse/serialize/opToActionData, `addedOps` routing + `mergeAddedOpsIntoModel` round-trip, custom-edge restore, `parseStepNodeId`). No component harness — verified by `typecheck:all` (clean; the lone `control/ocr/index.js` error is pre-existing), `test` (workflow-graph 45), `test:architecture` (79), `lint`, `build:dashboard`, and a headless playwright-cli **synthetic-DnD** pass against real `dashboard:prod` (`.screenshots/workflow-editor-dnd/0{1..5}-*.png`: palette draggable + hint, drop → lane shows "1 added" row + remove, inspector "Added operations (1)" with the edited `{newRowFlag}` flowing to the lane pill, free-canvas standalone node, Delete removes it while all 4 lanes survive). **Gotcha:** React Flow's connection (handle-drag) can't be faithfully driven by synthetic `PointerEvent`s headlessly — `onConnect` is verified by code + connectable handles rendering on both sides, not by an e2e-drawn edge.
- **2026-06-30: Settings is now a centered overlay (Dialog), and the Workflow Editor moved OUT of it into a full-page takeover — `SettingsPage.tsx` → `SettingsDialog.tsx`.** Operator rationale: the old Settings was a full-page view rendered BESIDE the always-on `WorkflowRail`, AND its 8th section embedded `WorkflowModifierPage` (which has its OWN `EditorSidebar`) — so the editor read as "sidebars on sidebars." Fix is two coupled moves. **(1) Settings → overlay.** `SettingsDialog.tsx` wraps the same content in the shared `ui/dialog.tsx` primitive (`size="xl"` + `className="flex h-[min(86vh,660px)] flex-col gap-0 overflow-hidden p-0"`), controlled by App's existing `showSettings` via `<Dialog open onOpenChange>`; Radix owns the focus trap + dimmed scrim + Esc. It renders as a GLOBAL sibling in App (always mounted, near `RunModal`), NOT a branch beside the rail — so the dashboard (rail/queue or Overview) stays mounted underneath and the gear just toggles `open`. **The draft persists across open/close** (the component never unmounts), so closing is non-destructive — Save commits, Revert discards, there is no close-confirm. Internals redesigned: a 212px section nav with a **settings search** (`sectionMatches` over label + per-section `keywords`), **per-section dirty dots** (`sectionDirty` maps a `SectionId` → the `OperatorSettings` group keys it edits), **grouped-card** content (local `Card` + `SectionHead` icon-tile), per-field **changed dots** (`InputField`/`ToggleField` gained an optional `changed` prop; `SettingsField` inputs went `max-w-md` → `w-full` to fill card-grid cells), and a sticky Save/Revert footer (settings-backed sections) or a "saved automatically" note (Notifications). The 8th nav entry is gone — "Workflow Editor →" is a **launch row** at the nav bottom that calls `onLaunchEditor`. **(2) Editor → full-page takeover.** New `components/workflow-modifier/WorkflowEditorScreen.tsx` = a slim header (`‹ Dashboard` back) + `<WorkflowModifierPage onDirtyChange/>`. App gained `showEditor` state; when true it renders ONLY the editor screen (TopBar + WorkflowRail + TerminalDrawer all hidden — true Option-A takeover) and **suppresses global keyboard shortcuts** (`if (showEditor) return` in the keydown effect, `showEditor` added to deps). `handleLaunchEditor` = close overlay + `setShowEditor(true)`; `handleLeaveEditor` (the back button) runs the existing `requestLeaveEditor()` dirty guard, then `setShowEditor(false)`. Because the editor is its own page (rail hidden) and Settings is a focus-trapped modal, the rail handlers (`handleWorkflowChange`/`handleShowOverview`) no longer need the editor guard — they were simplified (`handleWorkflowChange` is sync now). **Supersedes the 2026-06-29 Settings-page lesson's** "full-page view beside the rail" + "Workflow Editor is the last Settings section" claims (the backend `/api/settings` + `config/settings.json` + `applyOperatorSettingsToEnv` half is UNCHANGED). No component harness — verified by `typecheck` (clean; the lone `control/ocr/index.js` error is pre-existing, unrelated), `lint`, `test:architecture` (79, incl. `frontend-tailwind-compliance`), `build:dashboard`, and a headless playwright-cli pass against the REAL `dashboard:prod` on `:3939`: overlay over the dimmed dashboard with live `/api/settings` values, Notifications/Credentials sections, dirty state (field changed-dot + nav dirty-dot + Save-enabled + "Unsaved changes"), search "duo"→Features, editor launch → full-page (rail/topbar/drawer hidden, single sidebar), and back → dashboard restored (`.screenshots/settings-redesign/real-0{1..8}-*.png`).
- **2026-06-30: Titleless operation rows drop the header strip and read like a BATCH row — no header, count strip, then a member-name preview when collapsed.** `OperationRowUnified` (`queue-panel/operation-row-variants.tsx`) for a titleless person-anchor coordinator (`hasTitle === false`, i.e. `batchGroupTitle` returns `""` for a person-kind anchor — multi-person input-run operation shells / post-fan-out person operations). Three operator passes converged on the batch-row look: (1) the redundant member-name preview was dropped from the **header**; (2) the **entire header strip** (status icon + status badge + awaiting-review OCR jump + live subline) was removed — the whole header zone is now gated on `hasTitle` and renders ONLY for a titled (file-kind, e.g. `Oath_Packet_Batch.pdf`) coordinator, and the count strip's `border-t` is likewise gated on `hasTitle` so a titleless card's top edge is clean (no stray divider flush with the rounded border); (3) the **collapsed** state re-introduces the member-name preview but in **batch style** — below the count strip (`!expanded && previewKids.length > 0`), reusing `pickPreviewChildren(members, 3)` + a `MEMBER_PREVIEW_ICON` map mirroring `group-row-base`'s `STATUS_ICON`. So a titleless operation collapsed ≈ a batch person anchor; **expanding** swaps the preview for the full `EntryItem` member rows (the chevron toggles between the two). Each preview row is a real **`<button>`** (icon + name + EID), **hairline-divided** (`i > 0 && border-t border-border/40` — the container is a bordered `flex flex-col bg-card`, no `gap`), and **click = expand + go to that member**: `onClick` calls `onSelect(kid.id)` + `setPendingScrollId(kid.id)` + `setExpanded(true)`, and a `useEffect` gated on `[expanded, pendingScrollId]` scrolls the member into the scroll-capped list (`membersScrollRef.querySelector('[data-queue-entry-id=...]')` — EntryItem stamps that attr — then `scrollIntoView({ block: "nearest" })`, clearing the pending id). The effect (not the click handler) does the scroll because the member rows only mount after `expanded` flips. Titled (file) coordinators keep their header AND now also show the collapsed preview, so they read like a titled batch row too. Safe because titleless ⟺ person-kind ⟺ never awaiting-review (OCR operations are file-kind/titled). The footer (time · #run · trace id · actions) is unchanged. `operationHeaderHasTitle` stays (drives aria-label + footer subject). No new test (header/preview are JSX, not a pure seam; `operationHeaderHasTitle`'s tests still pass) — verified by `typecheck` (only the pre-existing `src/control/ocr/index.js` error), `test:architecture` (79), `lint`, `build:dashboard`, and a headless playwright-cli render of the `?view=ui-gallery` "titleless person anchor" operation variant (`.screenshots/titleless-operation/v2-{collapsed,expanded}.png`): collapsed = count strip + member-name preview (batch look), expanded = full member rows.
- **2026-06-29: Workflow editor — Workflows picker + Outline MERGED into one accordion sidebar (`EditorSidebar.tsx`), and the Data Bank palette redesigned + floated from the header. The canvas view controller is now LIFTED to the page.** Three coupled changes to `components/workflow-modifier/`. **(1) Merged sidebar.** `WorkflowPicker.tsx` + `graph/OutlineRail.tsx` are DELETED; `EditorSidebar.tsx` replaces both — one panel where picking a workflow expands its outline INLINE (accordion: `{stepCount} steps · {ops} · {systems}` → Queue row (a `naming` tag) → numbered Pipeline lanes with op counts → Delegation). **Icon-free** rows (the per-workflow lucide glyphs are gone; selected = `bg-accent` + `shadow-[inset_2px_0_0_var(--primary)]` + bold), numbered step CHIPS (not a `"N. "` text prefix), and the three canvas view-controls (Collapse all · Data flow · Fit) as TEXT buttons in the footer. **(2) Lifted view controller — the load-bearing architecture.** The OutlineRail used to live INSIDE `GraphCanvas` (which is keyed `${selected}:${loaded}` → REMOUNTS on every workflow switch); the picker was a stable page sibling. Merging them into one panel meant it can't live in the canvas (it'd flicker / scroll-reset on every click). So the per-workflow view state — `paletteOpen` / `dataFlowOn` / `collapsedIds` / `focusTarget {id,n}` / `fitNonce` — is LIFTED to `WorkflowModifierPage` and shared with both the (stable, page-level) `EditorSidebar` AND the (remounting) `GraphCanvas`, which is now CONTROLLED via the `CanvasViewState` prop bundle. The imperative bits stay in the canvas (they need `useReactFlow`): a nonce-guarded effect runs `focusNode` when `focusTarget.n` bumps (re-focusing the same lane re-centers), and a mount-skipping effect runs `fitView` when `fitNonce` bumps. The page resets `collapsedIds`/`focusTarget`/`dataFlowOn` on `selected` change (the canvas remounts; these are page-owned so they'd otherwise leak across workflows). The outline is derived PURELY in the page via `graph/outline-build.ts` `buildOutlineModel(base, draft, name, laneOps)` — same `overrideToGraph` + mined `laneOps` the canvas's `configModel` uses, so the sidebar matches the canvas lane-for-lane WITHOUT reading the live nodes (an unmapped mined step is an unnumbered extra lane; lane ids = the collapse-all set). **(3) Data bank.** Trigger moved from an in-canvas toggle to an icon button at the RIGHT of the `EditorSidebar` "Workflows" header; the palette (`graph/DataBankPalette.tsx`) floats over the canvas (React Flow `Panel` top-left) with a header × (`onClose`). Redesigned: a **By system / By action** grouping toggle, **kind-filter chips** (only kinds present in the catalog), a localStorage **Recently used** row (`useRecentOps.ts`), and rich op rows surfacing the op's **summary** (the mined human description — note it currently mixes Playwright locator code + prose; clean that at the BUILD/mining layer, not via a display heuristic), the vars it reads/writes (var pills), and its selector. Pure seam `graph/data-bank-palette.ts` gained `buildPaletteGroups` (query + kind filter, two views) / `availableKinds` / `resolveRecentOps`; `op-kind-visuals.ts` gained a tinted `chip` class per kind. **Untouched:** the pure projections (`graph-build`/`lane-build`/`design-spec`) + their tests (node types/ids/edge counts unchanged); `NodeInspector`, the lane nodes, and `LaneInteractionContext` (now fed from props). Pinned by `tests/unit/dashboard/workflow-graph/{data-bank-palette,outline-build}.test.ts` (13 cases). No component harness — verified by `typecheck` (only the pre-existing `settings/types.ts` error) + `test` (3259) + `test:architecture` (79) + `lint` + `build:dashboard` + a headless playwright-cli pass against `dashboard:prod`: merged sidebar with the CRM Doc Download outline dropdown, the floating data bank (826 ops / 10 systems, By-action grouping, 9 kind chips), outline→canvas focus (lane framed + others dimmed + inspector open), and Collapse-all / Data-flow / Fit (`.screenshots/databank-redesign/live-0{1..7}-*.png`).

- **2026-06-29: Settings page (`components/settings/`) — a navbar-gear full-page view for tweakable operator constants + the embedded Workflow Editor; the notification gear became the Settings gear and the bell got filter chips.** Three coupled changes. **(1) The Settings page.** `SettingsPage.tsx` (+ `SettingsField.tsx` primitives) is a full-page view (same `flex h-full flex-1` shell as `WorkflowModifierPage`) reached from a new `Settings` (lucide gear) button in `TopBar`'s `rightSlot` — App gained `showSettings` state + a view branch (`showSettings ? <SettingsPage/> : showOverview ? … : <ResizablePanelGroup>`), mirroring the Overview branch. A left section-nav drives 8 sections: General · Display · Performance · Features · Paths · **Notifications** (the desktop-notify toggles, RELOCATED from the old bell-header gear popover — App still OWNS `notifySettings`/localStorage; the page is just the UI via `notifySettings`/`onNotifySettingsChange` props) · **Credentials** (READ-ONLY `.env` presence — booleans only, never values) · **Workflow Editor** (renders `<WorkflowModifierPage onDirtyChange={onEditorDirtyChange}/>` — see #2). Only the 5 settings-backed sections show a Save/Revert footer; they edit a local `OperatorSettings` draft and POST `diffOperatorSettings(draft)` (a SPARSE override) to `/api/settings`. **Backend:** `config/settings.json` (gitignored operator state) read/written via `src/tracker/settings/{schema,store}.ts` (fail-soft read / fail-loud Zod write — the workflow-presentation override pattern) + the `/api/settings` GET/POST/DELETE route, registered in BOTH `hono/app.ts` AND `hono/manifest.ts` (the `dashboard-hono-retirement` guard asserts they match — the same pass also added the two pre-existing `/api/separations/{approve-eid,dismiss-eid}` routes that the EID-approval work left out of the manifest). `src/config.ts` reads the merged settings at module load — **precedence env > settings.json > code default**, so an empty file is behavior-neutral (every settings default mirrors a config literal); `applyOperatorSettingsToEnv()` populates cross-module env knobs (OCR/`DEBUG_SCREENSHOTS`/`HR_AUTOMATION_DUO_WEBAUTHN`/nav-retries/separations-window) ONLY for operator-set fields via `??=` (real `.env` always wins; dynamic defaults like OCR page concurrency = vision-pool size stay untouched). Shared client-safe contract in `src/domain/settings/types.ts`. **(2) Workflow Editor moved rail → Settings.** The rail "Workflow Editor" entry (added earlier the same day) is RETIRED — the editor is now the last Settings section. App dropped `showWorkflowModifier`/`handleShowEditor` and stops passing `editorActive`/`onShowEditor` to `WorkflowRail` (the rail's optional props stay, just unwired, so restoring the shortcut is one-line). `requestLeaveEditor` now simply guards on `editorDirty` (true only while the editor section is mounted; `WorkflowModifierPage` reports `false` on unmount) — covers leaving Settings with unsaved graph edits. **(3) Notification gear → filter chips.** `NotificationSettings.tsx` (the bell-header desktop-notify popover) was DELETED — its toggles live in Settings → Notifications now. The `headerSlot` prop is gone from `NotificationBell`/`TopBar`/App; the bell header instead renders adaptive **filter chips** (All · Errors · per-category Runs/OCR/Capture) built by the pure `navigation/notification-filters.ts` (`buildNotificationFilters`/`filterNotifications`, mirroring `screenshot-gallery.ts` — category = the `domain` half of the `domain.event` kind, so a new kind needs no code) with a self-heal effect that resets a vanished filter to All. No component harness — verified by `typecheck:all` + `test` (3259) + `test:architecture` (79) + `lint` + `build:dashboard` + a headless playwright-cli pass against `dashboard:prod` (`.screenshots/settings-page/0{1..6}-*.png`: navbar gear highlighted, all 8 sections, merged-default values, read-only Configured credential badges, the embedded editor's workflow picker, the bell with the gear removed). Pure seams pinned by `tests/unit/domain/settings/operator-settings.test.ts`, `tests/unit/tracker/settings/store.test.ts`, `tests/unit/dashboard/notification-filters.test.ts`.
- **2026-06-29 (SUPERSEDED by the Settings-page lesson above — the editor moved from the rail INTO the Settings page; the rail no longer has a Workflow Editor entry): Workflow Editor moved from a navbar button to a rail entry below "Dashboard"; quitting = pick another entry, guarded on unsaved edits.** The Workflow Modifier was a navbar `Workflow`-icon TOGGLE in `TopBar`'s `rightSlot` that, when on, HID the `WorkflowRail` and rendered `WorkflowModifierPage` full-page — "the single way in and out." It's now a `WorkflowRail` entry (`onShowEditor`/`editorActive` props, mirroring the existing `onShowOverview`/`overviewActive` "Dashboard"/Overview entry) sitting directly under "Dashboard" in the top "Overview" group. **The rail stays visible in editor mode** (the old `{!showWorkflowModifier && <WorkflowRail/>}` guard is gone; the page root already has `flex-1` so it fills beside the 200px rail), so **"quitting" = selecting any other rail entry** (Dashboard or a workflow) — there is NO dedicated close/X button, identical to how you leave Overview. The one thing Overview lacks is unsaved state: `WorkflowModifierPage` holds a `dirty` draft, so it now takes `onDirtyChange?(dirty)` (reported via an effect + `false` on unmount) which App lifts into `editorDirty`; `handleWorkflowChange` and the "Dashboard" handler (`handleShowOverview`) route through `requestLeaveEditor()` — `if (!showWorkflowModifier || !editorDirty) return true; else confirm({title:"Discard unsaved changes?", tone:"destructive", confirmLabel:"Discard", cancelLabel:"Keep editing"})` via the shared `useConfirm`/`ConfirmDialog`. ENTERING the editor (`handleShowEditor`) is unguarded (nothing to lose yet). The navbar button + its `Workflow` lucide import were removed (only `HelpCircle` stays in `rightSlot`); the generated design-scaffold brief string (`src/domain/workflow-design/brief.ts`) was renamed "Workflow configuration page" → "Workflow Editor page" for naming consistency. No component harness — verified by `typecheck` + `lint` + `test:architecture` (79) + `build:dashboard` + a headless playwright-cli pass against `dashboard:prod` on an isolated tracker dir: rail shows "Workflow Editor" `[active]` below "Dashboard", editor opens with the rail still visible, clean-quit switches with no dialog, and a dirtied step-label edit → click "Dashboard" pops the confirm with both **Keep editing** (stays, still dirty) and **Discard** (closes to Overview) branches working (`.screenshots/workflow-editor-rail/0{1,2,4}-*.png`).
- **2026-06-29 (generalized 2026-07-08): Identity-approval review — `EidApprovalBanner` + two derived status badges, now WORKFLOW-AGNOSTIC.** When an identity-resolving run resolves a DIFFERENT valid person than the input record it PAUSES instead of silently proceeding (the wrong-person fix — see `src/domain/identity-approval.ts`; separations was the first adopter, onboarding the second as of 2026-07-08). Dashboard side: (1) `EntryItem` `STATUS_CONFIG` has **`awaitingApproval`** (amber `bg-warning/12`, `UserRoundSearch`, "Awaiting Approval") and **`dismissed`** (muted, `UserRoundX`, "Dismissed"); both are produced by `identityApprovalStatusExtensions.derivedStatus` (`src/domain/identity-approval.ts`, re-exported by `separations-status.ts`, registered per-workflow in `domain/queue-row-status-index.ts` — separations + onboarding today) keyed on `data.eidApproval` (`pending`/`dismissed`). The run is mechanically `done`, so the derived badge replaces it — same pattern as person-lookup `notFound` / OCR `needsReview`; `EntryItem` never names the workflow. (2) `log-panel/EidApprovalBanner.tsx` renders at the top of the LogPanel whenever `entry.data.eidApproval==="pending"` (**no `entry.workflow` check** — the 2026-07-08 generalization; the `workflow` prop rides both requests), wired through `LogPanel`'s `failureBanner` slot (mutually exclusive — a paused run is `done`, not `failed`). It shows BOTH candidate identities side by side ("On the input record" + the resolved "Name match (proposed)", each with name/dept/payroll-title stamped at pause) with a per-candidate **Use this EID** (disabled when that candidate has no valid EID — e.g. onboarding's new-hire original), a manual 8-digit entry, and **Dismiss** — POSTing the generic `/api/eid-approval/{approve,dismiss}` via a local `fetch` + `toast`. No component harness — verified by `typecheck` + `frontend-tailwind-compliance` + seeded-fixture headless playwright-cli passes (separations `.screenshots/eid-approval/`; onboarding `.screenshots/eid-approval-onboarding/01-banner.png` — banner + "Awaiting Approval" badge + generic-route POST confirmed).
- **2026-06-29: Screenshots tab renders the new per-step audit trail (`kind: 'step'`) — `info`-toned "Step" badge + dedicated "Steps" filter chip.** The kernel now auto-captures one screenshot at the end of each successful workflow step (see `src/core/CLAUDE.md` 2026-06-29). Dashboard side: the screenshot `kind` union widened to include `'step'` (`shared/types.ts`, `hooks/useRunScreenshots.ts`); `ScreenshotTile`'s `KIND_BADGE` gained a `step` entry (`border-info/40 bg-info/15 text-info` — distinct from `form`'s primary tint and `error`'s destructive); `screenshot-gallery.ts` exports `STEPS_FILTER` and `buildScreenshotFilters` emits a default-tone "Steps" chip (after Errors, before per-system) when any step group exists, with `filterScreenshotGroups` narrowing to step groups (renamed from the retired `filterScreenshotTiles` in the 2026-06-30 folder-group migration — see `src/dashboard/components/CLAUDE.md`). All pure-logic — pinned by `screenshot-gallery.test.ts`. No component harness — verified by `build:dashboard` + a seeded-fixture headless playwright-cli render (`.screenshots/step-screenshots/0{1,2}-*.png`: Step badge distinct from Form, `Steps 4` chip filters to the 4 step tiles).
- **2026-06-29: `FailureBell` → `NotificationBell` — the navbar bell is now a notification CENTER fed by one canonical `notify()` pattern; toasts and the bell share a single model (`src/dashboard/lib/notifications.ts`).** Two layers. **(1) The pattern (the contract every inbox-worthy event follows).** `notify(spec)` is the single entry point: it records an `AppNotification` into a module-scope store the bell reads AND fires a toast through the `@/lib/notify` facade. **Rule:** route through `notify()` only events the operator would want to RECOVER if they missed the toast (terminal run outcomes, OCR awaiting-review, capture finished/failed/expired, a resolved cancel); pure transient ACKs ("Copied", "Retrying…", "Saved") stay on `toast.*` directly — that distinction IS the discipline. **Naming:** stable `domain.event` kind keys in `NOTIFY_KINDS` (`run.completed|failed|not_found|cancelled`, `ocr.awaiting_review`, `capture.photo_added|blurry|completed|failed|expired`); each kind → a default **severity** (`success|info|warning|error|message`, aligned to the toast types + neutral `message`) via `KIND_SEVERITY`, so a call site only overrides for a genuine exception. **Copy:** title = outcome (`subject + verb` — "Maria Lopez completed"), description = detail/why/next step. **Shape:** `{ id, kind, severity, title, description?, source(workflow)?, subject?, entityRef?, traceId?, dedupeKey?, ts, rerunnable? }`; `entityRef` = click-to-open, `dedupeKey` (default `<workflow>:<id>:<runId>:<kind>`) collapses repeats so a re-fired event updates its card instead of stacking. The lib owns identity/copy/store ONLY — colours live in the consumers (`<Toaster>`/`index.css` for toasts, `NotificationBell.tsx`'s `SEVERITY_CARD` token map for the center). **(2) The bell (`navigation/NotificationBell.tsx`, renamed from `FailureBell.tsx`).** Center = the live in-session store (`useNotificationFeed` → `useSyncExternalStore`) **merged** on-open with a `/api/failures` backfill (cross-workflow + historical failures, mapped via `failureRowToNotification`), deduped by key with the live record winning (`mergeNotificationLists`). Severity-iconed cards, newest-first; `run.failed` keeps the hover-revealed `Re-run` (same `useWorkflowActionDispatcher` retry transport); any card with an `entityRef` is click-to-open (`onOpen` → App `handleOpenNotification` switches workflow/date + selects). **Badge math avoids double-counting:** unseen failures (`Σ failureCounts − ack`, the original failure-bell behaviour, persisted `notification-bell-failures-ack`) PLUS unseen non-failure notifications (a `lastSeenAt` timestamp watermark, persisted `notification-bell-last-seen`); a live `run.failed` is excluded from the timestamp count so it isn't tallied twice. Badge tone = destructive when any unseen failure, else primary. **Wiring:** App's status-transition loop fires `run.completed`/`run.failed`/`run.not_found` + `ocr.awaiting_review` (the last with `toast:false` — review is steady-state, not a flash; the backgrounded-tab desktop notification still fires); `useCaptureToasts` fires the three terminal capture events (photo/blurry stay transient toasts); `useActionToasts` records the resolved-cancel card (`toast:false`, since App's loop skips cancelled). Pure seam pinned by `tests/unit/dashboard/notifications.test.ts` (18 cases: dedupe-on-record, live⊕backfill merge precedence, unread watermark, default dedupe key, kind→severity). No component harness — verified by `typecheck:all` + `test` (3188) + `test:architecture` (79) + `lint` + `build:dashboard` + a seeded-failures headless playwright-cli pass (bell badge `4`, "Notifications" header, 4 severity cards with Re-run in the a11y tree), `.screenshots/notification-bell/01-open.png`. **Supersedes the 2026-06-06 FailureBell lesson's naming** (the elevated-card visual design carried forward into the generic notification card).

- **2026-06-22: Operation coordinator log panel now shows its OWN step-pipeline timeline (Prepare → Review → Fan-out), consistent with every other workflow.** The coordinator's log panel used to special-case the `StepPipeline` away (`hideStepPipeline = isOperationCoordinator` in `LogPanel`) because the coordinator's stored `step` only ever reads `ocr-prep` then `approved` — the *member* steps (`navigation`/`fill-form`/`save`) belong to the fanned-out rows, not the coordinator, so feeding the registered steps would render an all-pending pipeline. Fix: `computeOperationPipelineView(entry, childEntries)` (`coordinator-logs.ts`, pure + exported) maps the coordinator onto a 3-stage pipeline derived from its **denormalized `data.ocrStatus`** (its `step` can't tell preparing from awaiting-review — both are `ocr-prep`): `running/preparing → prepare`, `awaiting-review → review`, `approved → fan-out`. Effective status (`operationCoordinatorEffectiveStatus`) drives completion — all members terminal → whole pipeline `done` (a failed MEMBER does NOT fail the coordinator, matching the operation-coordinator-chip contract). A real coordinator failure (OCR prep failed) or operator stop (cancel/discard) surfaces on the reached stage; we do NOT fabricate per-stage durations to recover the amber "cancelled" sentinel (no-fabrication rule) — the amber tone stays on the row badge. `LogPanel` un-hides the pipeline and passes `stepDurations={isOperationCoordinator ? undefined : runStepDurations}` (the coordinator's stored durations are keyed on `ocr-prep`/`approved`, which don't match the synthetic stages, so they'd be noise). The **detail grid stays hidden** for coordinators (a file/operation row has no person `detailFields` — showing empty cells would read as broken, not "consistent"). Pinned by `tests/unit/dashboard/coordinator-logs.test.ts` (7 `computeOperationPipelineView` cases). No component harness — verified by typecheck + full test (2887) + architecture (79) + lint + `build:dashboard`.
- **2026-06-22: Step-pipeline durations must prefer the LIVE SSE entry for the live run — same as status/step.** `LogPanel` derives `runStatus`/`runStep` with a live-vs-historical split: for the live run (`isViewingLiveRun`) it prefers the SSE `entry`, because `activeRun` (from `useRunsForMergedEntry`) is a ONE-SHOT `/api/runs` snapshot taken at row-open and never refetched as the run progresses (the hook keys on `workflow:id:runId:date`, none of which change mid-run). `runStepDurations` was the odd one out — `activeRun?.stepDurations ?? entry?.stepDurations` preferred the frozen snapshot, so chips advanced/turned green live (status/step are live) but the per-step **times** stayed stuck at open-time values until a remount (switch tab → back) re-ran the effect and refetched `/api/runs`. Fix: gate it on `isViewingLiveRun` identically. Durations update live without touching the entry hashes because `computeStepDurations` only writes a step's duration when the next step starts or the run goes terminal — both change `step`/`status`, which ARE in `entryRenderHash`/`buildEntriesHash`, so the SSE entry refreshes at exactly the moments durations change. (No component harness — verified by typecheck + `build:dashboard` + architecture 75/75 + lint.)
- **2026-06-25: `mergeDisplayItems` collapses consecutive identical events into one `×N` row (logging dedup overhaul, render-layer half).** The merged Logs/"all" stream now folds adjacent events sharing an identity (`eventCollapseKey` = `type` + `system` + `step` + `currentStep`) into a single `DisplayItem` whose `entry.count` is bumped; `EventLine` (`LogLine.tsx`) renders an `×N` badge (mirroring the regular log line's `x{count}`) when `count > 1`. The collapsed row keeps the FIRST occurrence's timestamp/fields so it sits at the run's start. The classic target is a burst of `idle_signal` touches for one system — this is the residual-noise + defense-in-depth layer: the true fixes are at emit time (single-sourced `idle_signal`, pid-scope attribution — see `src/tracker/CLAUDE.md` 2026-06-25), so a future emit regression degrades to one collapsed row instead of a wall. The dedicated **Events** category renders raw events and BYPASSES this merge (no collapse there). `RunEvent.count` is render-only — never present on the raw SSE event. Pinned by `tests/unit/dashboard/log-stream-merge.test.ts` (collapse + no-cross-system + interrupted-by-log cases).
- **2026-06-18: `daemon_phase` events are dropped from `mergeDisplayItems` (USR-002).** They fire on a ~1–2s heartbeat (102 per session vs 67 `item_start`) with no per-run detail, flooding the merged Logs/"all" view. Treatment mirrors `step_change`: filtered out of the merge via `MERGE_DROPPED_EVENTS = new Set(["step_change", "daemon_phase"])` in `LogStream.tsx`. They remain visible in the dedicated Events category (which bypasses the merge and renders `events` directly) and continue to drive session-card `daemonPhase` state. Pinned by `tests/unit/dashboard/log-stream-merge.test.ts` (two new USR-002 cases).
- **2026-06-25: "View Data" tab added to LogStream surface bar — data-provenance ledger.** New `"view-data"` surface key in `SURFACES` (icon: `Database`, between Preview and Edit Data), gated on a `viewDataAvailable` prop that `LogPanel` computes as `displayedLogs.some(isDataPointLog)` — so the tab only appears once a run recorded ≥1 data point. The data rides the existing structured-log channel: workflow handlers call `ctx.recordData({ direction, field, value, system, note })` (kernel primitive, auto-tags the current step), which emits an `event: "data:point"` log line carrying `dataDirection`/`dataField`/`dataLabel`/`dataValue`/`dataNote` (+ shared `system`/`step`). No new transport/DB/API — these fields ride `raw_json` through `mapLogRowToWire` like every other structured field, and the client `LogEntry` type was widened to carry them. `ViewDataPanel.tsx` filters + groups them via the pure `groupDataPointsByStep` (`src/domain/data-point.ts`, unit-tested) into a per-step timeline of **Read** (`log-cyan`/`ArrowDownToLine`) and **Write** (`log-teal`/`ArrowUpFromLine`) lanes — reusing the Extract/Fill category accents, color-not-only (icon + label). Scalable: any workflow opts in by calling `ctx.recordData` (separations instrumented first). `parseInitialTab("view-data")` routes to that surface. Pinned by `tests/unit/domain/data-point.test.ts`.
- **2026-06-25: "Edit Data" tab redesigned (form-grade) and scoped to separations only — parallels View Data's capability gate.** Two coupled changes to `log-panel/EditDataTab.tsx`. **(1) Scope.** Edit Data availability is unchanged structurally — `LogPanel` still gates it on `!isOperationCoordinator && detailFields.some(f => f.editable)` — but **emergency-contact's `editable: true` flags were removed** from its `detailFields` (it's an OCR-approval workflow, never an edit-and-resume target; `src/workflows/CLAUDE.md` "Opt-Ins" already documented "only separations is opted in"). With EC out, **separations is the only workflow with editable fields**, so the tab is separations-only the same way View Data is separations-only by `ctx.recordData` instrumentation — a *capability* gate, never an `entry.workflow === "…"` check. `editable` is read in exactly three places (registry normalize pass-through, the LogPanel gate, EditDataTab's `.filter(f => f.editable)`), so dropping EC's flags has no runtime-handler effect. **(2) Two new optional `DetailField` hints drive the redesigned form** (display grid ignores them; only Edit Data consumes them): `inputKind` (`text`|`id`|`date`) and `group` (section label). Threaded through `core/kernel/registry.ts` `normalizeDetailField` (defaults `text`/ungrouped dropped from the wire), the presentation-override `detailField` strictObject (`tracker/workflow-presentation/schema.ts`), and the dashboard wire type (`dashboard/lib/workflows-context.tsx`). Separations sets them on its 6 editable fields (Identity: name/eid · Dates: lastDayWorked/separationDate · Transaction: transactionNumber · Notes: comments). **The form** (still same fields, same `/api/{run-with-data,save-data,entry-data,find-prior-by-key}` flow, same `CopyFromPriorButton`): a constrained `max-w-2xl` column (kills the maximized-panel "1900px-wide inputs" ugliness); a framed "Override extracted values" callout; `groupEditableFields` lays fields into labeled sections (label + hairline), `date` fields render via a `DateField` (mono `MM/DD/YYYY` input + a `CalendarDays` `Popover` over the existing HeroUI `Calendar`, converting `MM/DD/YYYY` ⇄ the calendar's `YYYY-MM-DD` at the boundary), `id` fields are mono; per-field amber "changed" dot + `Undo2` Reset; on-blur validation (`validateEditField`: a non-empty `date` must be a real `MM/DD/YYYY`, an `id` can't contain whitespace) that BLOCKS Save+Run (`hasBlockingErrors`) while Reset stays enabled; a fixed two-zone action bar — *data-in* (Find prior, Refresh from logs) left, *data-out* (Reset · Save · **Run with these values** primary) right, with a `N changed` summary. Layout: within a section, `multiline` or a lone field spans full width; pairs sit half-width — so the two Dates pair on one row and Employee/EID pair. The container is a `flex h-full min-h-0 flex-col` (scroll body + fixed footer) inside LogStream's `edit-data` slot. New pure exports unit-tested in `tests/unit/dashboard/edit-data-tab.test.ts`: `mmddyyyyToYmd`/`ymdToMmddyyyy` (round-trip + impossible-day reject), `validateEditField`, `groupEditableFields`. No component harness — verified by `typecheck:all` + `test` (3159) + `test:architecture` (79) + `lint` + `build:dashboard` + a seeded-fixture headless playwright-cli pass (render / dirty / invalid-date-blocks-Save+Run / calendar-popover-opens-on-current-date), `.screenshots/dashboard-preview/edit-data-0{1,2,3,4}-*.png`.
- **2026-06-30: Log-panel readability overhaul + member rows are individually selectable; the "Delegated" tab is RETIRED.** Five coupled changes from an operator review of the operation-coordinator queue row + its log panel.
  - **(1) Member rows select the member (`queue-panel/operation-row-variants.tsx`).** Previously clicking ANY member row — a collapsed preview row OR an expanded `EntryItem` — selected the parent coordinator (the old `selectMain` callback routed every member click to `onSelect(parent.id)`), so an operator couldn't open one signer/contact's own logs. Now: collapsed preview rows are real `<button>`s whose `onClick` does `stopPropagation()` + `onSelect(kid.id)` + `setExpanded(true)`; expanded members pass `onSelect={onSelect}` (selects `member.id`) and the expanded-list wrapper `<div>` carries `onClick={e => e.stopPropagation()}` so the member click can't bubble to the card root and re-select the coordinator. The card ROOT (header for a titled coordinator, the footer/gaps for a titleless one) still selects the coordinator — that's how you reach its consolidated event-tracker logs. App-side selection already supported member ids (`App.tsx` `selectedEntry` resolves a member id via `entries.find`), so no App change was needed.
  - **(2) SESSION fold removed — kills the doubling (`coordinator-logs.ts`, `useCoordinatorAggregatedLogs.ts`, `LogPanel.tsx`).** An input-run coordinator's timeline used to fold daemon session-lifecycle events in as `source:"session"` log lines (the **SESSION** badge) via `mergeSessionLifecycleIntoCoordinatorLogs` — while those SAME events ALSO render as their own `EventLine`s in the merged "all" view. So every `workflow_start`/`browser_launch`/`auth`/`duo` appeared TWICE. The fold + the `"session"` `CoordinatorLogSource` + `SESSION_LIFECYCLE_EVENT_TYPES` are deleted; session events now render exactly once, as humanized event lines. `COORDINATOR_LOG_SOURCE_LABEL` keeps `Operation`/`OCR`/`Member` (those are NOT doubled — they come from the coordinator's own logs + OCR key events + member summaries).
  - **(3) `idle_signal` dropped from the merged view (`LogStream.tsx`).** Added to `MERGE_DROPPED_EVENTS` alongside `step_change`/`daemon_phase` — it's a daemon keep-warm heartbeat (per-system idle-refresh touch) with no per-run activity and dominated the timeline. Still visible in the dedicated **Events** category.
  - **(4) Event labels humanized + `workflow_start` names the workflow (`LogLine.tsx`).** `EVENT_LABEL` maps each `RunEvent["type"]` to a readable label ("Browser launched", "Authenticated", "Duo approved", …; unmapped → de-underscored type). `EventLine`'s detail fallback chain now ends in `event.workflowInstance`, so lifecycle events that carry no system/step (`workflow_start`/`session_create`/…) render the daemon instance — "Workflow started Separation 1" instead of a bare "workflow_start" (the "which workflow started" hookup).
  - **(5) Delegated tab REMOVED (supersedes the 2026-06-18 USR-001 lesson).** The `"delegated"` surface — `SURFACES` entry, `DelegatedChild`/`DelegatedSurface`/`delegatedStatusLabel`/`delegatedStatusClasses`, the `delegatedChildren`/`onOpenChild` props, the `parseInitialTab("delegated")` branch, and `LogPanel`'s `delegatedChildren` wiring — are all deleted, along with `tests/unit/dashboard/log-stream-delegated.test.ts`. Delegated children are surfaced inline (member rows) on the coordinator instead.
  - Backend half (member `item_start` on the coordinator timeline) lives in `src/tracker/CLAUDE.md` (2026-06-30). No component harness — verified by `typecheck` + `test` (tracker+dashboard 1207) + `test:architecture` (79) + `lint` + `build:dashboard` + a headless playwright-cli pass against real `dashboard:prod` on the operation-queue fixture: member preview rows are `View <name>` buttons that select the member (opening its own DOC ID detail), and the coordinator timeline reads as a clean humanized lifecycle ("Workflow started Separation 1" → "Browser launched Kuali" → … → "Item started 4361") with NO SESSION tags / NO idle_signal / NO Delegated tab (`.screenshots/log-member-fixes/0{3,5}-*.png`).

- **Lesson maintenance rule:** Keep current UI contracts here; move reference tables to `docs/engineering/dashboard-api-reference.md`.
- **2026-06-17: Terminal-drawer session summary uses a 3-way running / authenticating / idle split — never a 2-way running / idle split.** Daemons that are alive but haven't yet emitted a `daemon_phase` event are STILL AUTHENTICATING (4 serial Duo prompts, browser launch, login retries — each daemon enters the claim loop independently). Bucketing them as "idle" misleads operators into thinking capacity is available when it isn't. The signal is `daemonPhase` (set in `WorkflowInstanceState` from `daemon_phase` session events; daemon.ts only emits this once `setPhase('idle')` or `setPhase('keepalive')` fires — i.e. only AFTER auth completes and the claim loop starts). Bucketing rule in `bucketDaemonCounts` (TerminalDrawer.tsx, exported for unit tests): `itemInFlight` → running; `daemonPhase === 'idle'|'keepalive'` → idle; alive + no daemonPhase → authenticating; `crashedOnLaunch || finalStatus === 'failed'` → failed. Authenticating dot is amber (`bg-warning`) with pulse, matching the "in progress" tone. Pinned by `tests/unit/dashboard/daemon-bucket-counts.test.ts`.
- **2026-06-17: Operation coordinator row aria-label must use derived status label, not raw `entry.status` (ISS-001).** The `OperationRowUnified` button's `aria-label` was inline `${name} — ${cfg.label.toLowerCase()}` — correct as written but untestable. The fix extracted `resolveOperationStatusLabel(entry, ocr)` as a pure exported helper that wraps `headerStatus(entry, ocr).label`, making the contract directly testable without mounting React. The key contract: when `ocr.status === "awaiting-review"`, the label must be `"awaiting review"` (the `needsReview` derived state) — NOT `"running"` (the raw coordinator `entry.status`). A screen-reader user would otherwise hear a different state than sighted users see on the chip. Pinned by `tests/unit/dashboard/ocr-operation-aria-label.test.ts` (6 cases covering awaiting-review, no-ocr, ocr-preparing, done, failed, cancelled).
- **2026-06-12: Cancelled rows have their own surfaces — never bucketed under Failed (E2E-009/E2E-011).** A cancelled row is `failed`+`step=cancelled` (and a discarded operation mirror is `failed`+`step=discarded`); both are deliberate operator actions, not failures. Three surfaces enforce it: (1) `StatPills` gained a sixth **Cancelled** chip (warning tone, `grid-cols-6`) and `countEntriesByQueueStatus`/`entryMatchesStatusFilter` (queue-status.ts) bucket through the canonical `statusKeyForEntry` — filtering "Failed" excludes cancellations; (2) the failure bell + `/api/failures` (`tracker/dashboard/failures.ts`) skip `step === "cancelled" || step === "discarded"` rows (the OCR prep row itself was already excluded via `isResolvedPrepEntry`; the `discarded` exclusion covers the OPERATION row the discard mirrors onto); (3) a done OCR `verify` run with unverified records gains a warning `N/M verified` `secondaryTag` chip beside the green Done badge (`tracker/dashboard/ocr-status.ts`) — an unqualified Done on a 0/N report read as verified-ok. Pinned by `queue-row-status.test.ts` + `events-failure-counts.test.ts`.
- **2026-06-07: Session-card `StopPill` is PER-INSTANCE — stops one daemon, not the whole workflow.** It POSTs `/api/daemon/stop-instance` `{ workflow, instance }` (the card already carries its `instance` label; the backend resolves instance → `workflow_start.pid` → that daemon's port for a graceful `/stop { reassign: true }`). The workflow's other daemons keep running; the stopped daemon hands its in-flight item to a surviving peer, or fails it if it was the last one (see `src/core/CLAUDE.md` 2026-06-07). This **retires** the old workflow-scoped `StopPill` behavior: the `× stop all N` label, the `totalDaemonCount` prop, and the `useDaemons()` poll that fed the count are all **gone** from `WorkflowBox` (button is always `× stop`). The toast reflects the outcome (`reassignable` → "moves to another daemon" vs "this was the last daemon"). The queue toolbar's `StopAllButton` POSTs `/api/cancel-active-bulk` (`StopAllButton.tsx:40`) — a **per-item bulk CANCEL** (in-view pending/running items → orange Cancelled, **daemons SURVIVE**), NOT a daemon teardown. The workflow-scoped daemon teardown (`/api/daemon/stop`, in-flight items fail **RED**) has **no operator UI affordance** — it is the SIGINT/process-shutdown path only (corrected 2026-06-16, E2E ISS-005).
  - **F3 (2026-06-07): a lightweight destructive `useConfirm` is back for ONE case — `itemInFlight && !reassignable`.** Stopping a daemon mid-item is low-stakes ONLY when a peer can absorb the work; stopping the LAST daemon while it runs an item FAILS that live work, so it now pre-confirms ("Stop and fail item") instead of just a post-hoc toast. The full multi-daemon blast-radius dialog stays retired — this is the narrow last-daemon guard, not its revival. The `reassignable` flag is computed **client-side in `TerminalDrawer`** (`hasReassignablePeer(active, wf)`): another instance of the SAME `workflow` that is `pidAlive && active && !crashedOnLaunch`. It MUST be a real derived value — leaving `WorkflowBox`'s `reassignable={false}` default in place makes the confirm fire spuriously even when a healthy peer exists (over-confirm). This mirrors the axis the backend re-derives authoritatively at stop time, so the confirm only appears when there's genuinely no peer; the backend still makes the real decision.
- **2026-06-06: Log lines wrap (never clip) + top-aligned icons; the panel renders logs FAITHFULLY (fix verbosity at the source).** Two `LogLine.tsx` changes (regular + event rows): (1) the row flex container is `items-start` (was `items-center`) with `mt-[3px]` on the timestamp + icon, so a wrapped multi-line entry reads from the **first** line with the icon/time pinned to the top instead of floating in the vertical middle; (2) the message span gained `min-w-0` (alongside `break-words`) so a long line wraps cleanly inside the flex row instead of clipping at the panel edge — universal across all workflows. **The panel does NOT reverse-engineer or rewrite log content** (no display-layer JSON pretty-print/collapse): readability of a verbose log is a SOURCE concern — e.g. the OCR `done`-line raw-JSON dump was summarized in `per-page-pool.ts` (`src/services/ocr/CLAUDE.md`), not parsed back out in the UI. `formatLogMessageForDisplay` (`log-panel/log-display.ts`) stays minimal — it only strips a leading `[prefix]`.
- **2026-06-07: Operator-UX batch (13 changes).** Non-obvious contracts:
  - **Fonts:** `index.html` loads **IBM Plex Sans + IBM Plex Mono** (was Inter + JetBrains Mono). The `--font-*` tokens already declared IBM Plex first, so mono was silently falling back to system mono; loading the families realizes the intent (whole UI is now IBM Plex).
  - **Durations:** `useElapsed.ts` exports `formatSeconds(diff)` (shared by `useElapsed`/`formatDuration`) rolling up past **1h → "Xh Mm"** / **24h → "Xd Hh"**. Tests pinning the old `Xm Ys` form widen to `/\d+[dhms]( \d+[hms])?/`.
  - **Log panel:** `LogStream` gains a `failureBanner` slot (built in `LogPanel` from `entry.error` + `RetryButton`, suppressed for cancelled) + an internal **"Filter logs"** box (`visible` = category ∩ substring). Footer dropped the `Streaming · N entries` text (minimal live dot + sr-only) and **suppresses the `Standalone` chip** (only `from <Parent>`).
  - **Confirms:** every `window.confirm` → `shared/ConfirmDialog.tsx` + `shared/useConfirm.tsx` (promise `confirm()` + `confirmDialog` node). Migrated DeleteAllButton, BatchFooterActions, WorkflowBox stop-all, capture modal, OcrReviewPane remove-record.
  - **Drawer/date:** `TerminalDrawer` collapsed bar shows running/idle/failed dots; `viewingHistory` threads App → TerminalDrawer → `LiveIndicator` (Live → muted History pill). _(2026-06-17: the `TopBar` `Today` button + amber `History` chip were removed; the date navigator is now just `< [date] >` + calendar. The `LiveIndicator` History pill remains the off-today cue.)_
  - **Keyboard:** `QueuePanel` adds `Ctrl+Shift+R` (retry) / `x` (cancel) on the selected row; App adds `/` `[` `]` `g t` `?` `Esc`; `navigation/ShortcutsGuide.tsx` + a `?` navbar button. Retry is a Ctrl+Shift chord (not bare `r`) so it never collides with the browser's Cmd+R / Ctrl+R page refresh — both bare-`x` and the chord are guarded against unintended modifier combos.
  - **Resizer:** `shared/ColumnResizer.tsx` + `QueuePanel` `widthPx`; App persists `dashboard.queueWidth`.
  - **Notifications:** `lib/desktop-notifications.ts` (gated on `document.hidden`) fired from App's status-transition effect on failure + OCR awaiting-review edge; `navigation/NotificationSettings.tsx` popover. Scope = active workflow's entries.
  - **Overview/export:** `overview/OverviewPanel.tsx` rail entry built only from `wfCounts`+`failureCounts` (no invented splits); `log-panel/ExportMenu.tsx` (logs .txt / run .json / copy trace id, client-side). A multi-day trends chart was deliberately NOT shipped — no historical endpoint; faking data violates the no-fabrication rule. Add `/api/overview/trends` for real history.
  - **VerifyRecordView:** `unavailable` ("Unable to access") row now a warning-tinted bordered card (like the `found` cards, amber).
- **2026-06-06: FailureBell popover restyled to elevated "triage cards" with a wired Re-run; OCR key-pool indicator removed.** Two navbar changes. (1) `FailureBell.tsx` popover contents are now **elevated failure cards** (`rounded-lg border bg-card/40 shadow-sm`, hover lifts border+bg): a kicker row of a **destructive dot + workflow label** (`labelFor`) + time (`shortTime`), the **error as the focal line** (`text-foreground`, `line-clamp-2` — full text, not truncated to one line), then a footer of the **8-char short id** + a **hover-revealed `Re-run`** (`opacity-0 group-hover:opacity-100 focus-within:opacity-100`). The per-row **full UUID, `summary`, and the "failed" badge are gone** (`summary` was redundant with the workflow label; the whole panel is failures, so the badge was noise) — this retired the `statusBadgeClass` import. The **Re-run is real**: a local `RerunButton` calls the shared `useWorkflowActionDispatcher` (`transport:"retry"`, `fallbackTarget:{workflowId,id,runId,date}` → `POST /api/retry`), the same transport the queue-row `RetryButton` uses, with a sonner toast + pending spinner + `e.stopPropagation()` so it never triggers the card's open-the-row click. The error line is the open target (`onSelect(row)`). The **trigger button, unread badge, `localStorage` read-count, `/api/failures` fetch, and `aria-live` region are unchanged.** (An earlier same-day pass shipped a monospace "log tail" variant; replaced by these cards per design review.) (2) `ApiLimitsIndicator.tsx` (the OCR key-pool `G·6/6` pill) was **deleted** and unmounted from `TopBar` — the navbar right cluster is now just the bell + date nav. The backend `/api/ocr/key-status` route (`tracker/dashboard/hono/routes/ocr.ts` + `manifest.ts`, backed by `getOcrKeyStatuses`/`usage-tracker.ts`) is now **UI-orphaned but left intact** — `usage-tracker.ts` is core OCR rate-limit/rotation infra, not display-only; the route itself can be pruned if a fully clean removal is wanted. No tests referenced either component. Directions picked from a temporary `?view=panels-gallery` dev page (now removed). No component harness — verified by typecheck + lint + architecture guards (75/75) + live-dashboard Playwright (real bell rendering live OCR failures, hover-revealed Re-run wired to `/api/retry`, OCR pill confirmed gone).
- **2026-06-06: Log-panel filter bar split into two axes — segmented surfaces + an `All ▾` category dropdown (`LogStream.tsx`).** The old flat horizontal `FILTER_TABS` strip (All/Errors/Fill/Navigate/Extract/Debug/Events/Screenshots/Preview/Edit Data — one row mixing two unrelated axes) is gone. The bar is now ONE row: a **segmented control of surfaces** (`Logs · Screenshots · Preview · Edit Data`), then an **`All ▾` `DropdownMenu`** holding the seven log **categories**, with the **resize/maximize toggle pushed to the far right** (`ml-auto`). All three control groups are `h-8` (32px) so they tile with the queue toolbar's bottom border across the column gap. The dropdown renders **only under Logs** (hidden on Screenshots/Preview/Edit Data — categories are a lens on the log stream, not on other surfaces). **Events moved from a top-level tab to a category under Logs.** State is now two pieces — `surface` + `category` — replacing the single flat `filter` key; the exported pure `parseInitialTab(initialTab)` maps deep-links (`"preview"`→surface, `"events"`→`logs`+`events`, unknown→`logs`+`all`) onto the pair, so existing `defaultTab` deep-links (e.g. OCR row click → Preview) still work. `surface` self-heals to `logs` if the active surface stops being available (preview/edit-data gated by `previewAvailable`/`editDataAvailable`). Category dots use the categorical accent tokens (errors→`destructive`, fill→`log-teal`, navigate→`info`, extract→`log-cyan`, debug→`log-slate`, events→`log-violet`). The body filtering is unchanged in spirit: `all` = `mergeDisplayItems(nonDebugLogs, events)`, `events` = events only, `debug` = debug logs only, others = level-matched. The test-pinned exports `mergeDisplayItems`/`emptyStreamMessage`/`buildLogStreamItemKey` are byte-for-byte unchanged; `parseInitialTab` is newly exported and pinned in `tests/unit/dashboard/log-stream-empty-message.test.ts`. Virtualizer/auto-scroll/footer/preview-header slots untouched. No component harness — verified by typecheck + architecture guards (75/75) + live-dashboard Playwright screenshots.
- **2026-06-04: Input-run "settings gear" consolidated into `RunSettingsMenu`; `StepPresetMenu` removed.** The `InputRunPanel` gear is now a single `RunSettingsMenu` (`navigation/`) holding TWO ephemeral, per-page-load run settings: **Automation workers** (always shown) and **Run mode** presets (shown only when the workflow declares `presets`). It supersedes `StepPresetMenu` (deleted); the shared `StepPreset` type + `FULL_PRESET_ID` const moved to `src/dashboard/lib/run-settings.ts` (the new home for worker choices too — `WORKER_CHOICES` = Auto/1/2/4/6/8 + `workerChoiceToParam`). The gear shows the primary-accent + dot "active" treatment when EITHER setting is non-default. `InputRunPanel` POSTs `parallelWorkers` to `/api/enqueue` only when not Auto. `RunModal` carries the same setting as an inline **Automation workers** section gated by a new `RunModalSections.workers` flag (emergency-contact / ocr / oath-upload), self-hidden in oath-upload upload-only mode (single ticket, nothing to parallelize); it appends `parallelWorkers` to the upload `FormData` only when not Auto and only for the `/api/ocr/prepare` path. Backend contract + read-back: `src/workflows/ocr/CLAUDE.md` (2026-06-04 worker lesson) and `src/domain/run-options.ts`. No component test harness exists — verified by typecheck + `build:dashboard` + architecture guards.
- **Workflow definitions require eager registration.** `/api/workflow-definitions` reads the in-process `defineWorkflow` registry, so `src/tracker/dashboard/workflows.ts` must import every dashboard workflow barrel.
- **Queue surfaces and actions:** Extend classifier/projection layers and `GroupRowBase` wrappers for new row types. Dispatch row/bulk controls through `useWorkflowActionDispatcher` so scope/source reach `performWorkflowAction`.
- **2026-06-04: `operation` coordinator is one unified card (`OperationRow` → `operation-row-variants.tsx`).** Before approval (`data.ocrStatus === "awaiting-review"`), the header shows the blue **awaiting review** badge (same tokens as OCR `needsReview`) plus an info-tinted **Open OCR review** jump (`ArrowUpRight`) beside the badge — no middle OCR strip and no running subline. While OCR is still actively prepping (`ocrStatus` not yet `awaiting-review`), a compact work-zone strip can still show **OCR review** + primary jump. After approval, an inline expand/collapse member section renders fanned-out `EntryItem` rows. `onOpenOcrReview({ sessionId, runId })` must pass both ids so `App.handleOpenOcrReview` selects the OCR row and arms `reviewingPrepId` for the Preview gate.
- **2026-06-04: Batch rows are dedicated queue-ledger rows, not operation-style coordinator cards.** `DaemonBatchRow` renders through `BatchRowUnified` (`batch-row-variants.tsx`) as a compact queue container: **file** batches show a title-only header; **person** anchors omit the header entirely (preview + counts identify the batch). The whole row is clickable to drill into the dedicated batch queue — no separate Open-queue control, progress bar, subtitle under the title, avatar strip, or “+N more” footer. Member preview order is running → queued → failed → done (`orderBatchPreviewMembers`). Status tallies use `StatusCounts` only. Footer bulk actions unchanged; 0-member pre-fan-out stays `no rows yet` with anchor footer fallback.
- **2026-06-04: Batch-queue mode is one consolidated toolbar — the title/"Started …" header band was removed.** `BatchQueueToolbar` (the old `← / <title>` + `Started <time> · batch#<run4>` band) is gone (`batch-queue-view.tsx`). In batch mode `QueuePanel` renders a single `QueueSortToolbar` row: a new `leading` slot holds the **Back** button (`BatchQueueBackButton`) before the sort dropdown, and the `actions` slot holds the bulk actions (`queueBulkActionsSlot` → retry/stop/trash) followed by the screenshot-**preview** toggle (`BatchQueuePreviewButton`) — i.e. preview sits *after* the trash. The OCR **prep-review** jump (`BatchQueuePrepReviewButton`) renders only for prep anchors (`batchAnchorIsPrep`) and opens the review via `onOpenReview(batchQueueParentRunId)`. `buildSyntheticBatchQueueAnchor` + `resolvedBatchToolbarEntry` were dropped (the toolbar no longer needs an anchor entry).
- **2026-06-04: A batch card's footer id identifies the PARENT run, derived from `parentRunId` — members keep their own.** `buildProjectionFromQueueSurface` no longer borrows `members[0]`'s trace as the batch anchor subtitle (`deriveBatchAnchorSubtitle` in `projection.ts`): for a `batch` surface it composes `<sharedPrefix>-<runIdFragment(parentRunId)>` from the anchor's trace prefix + the parent runId4. With a real parent row this equals the parent's own trace (no-op); the fix matters for a cross-workflow delegated batch with NO parent row in panel (person-lookup / oath-signature fan-out under an OCR operation), which previously leaked the member's tail as the parent footer id. Members render their OWN trace (`resolveEntryId`), so parent footer and member rows now share the operation prefix yet differ in the final 4. Operation surfaces are unaffected (they carry a real `surface.parent`, so the subtitle is already the coordinator's own trace).
- **2026-06-04: Operation children are `operation-member`, the operation analogue of `batch-member`.** The OCR approve fan-out stamps `operation-member` (via the `rowShape` runtime option, gated on `isOperationCoordinatorWorkflow(operationWorkflow)`) so signer/contact rows nest inside the `operation` coordinator's expand section instead of becoming standalone rows or separate batch anchors. They still render as full `EntryItem` single rows (`operation-member` → `single` surface type in `entrySurfaceType`); the change is the shape/taxonomy + trace-id parity (coordinator footer = its own trace, members keep their tails). This also fixed a latent EC bug: emergency-contact (archetype `batch`) operation children were previously stamped `batch` → would have become separate anchors instead of nesting.
- **Unified footer contract (2026-06-01, status-gated):** Every flat queue footer (`EntryItem`, including batch members) renders one ordered button schema — `time · #run · id · ⟨spacer⟩ · elapsed/duration · ▲ bump · ↻ retry · × cancel · 🗑 delete`. Each button self-hides on its kernel action descriptor's `enabled` flag; **the projection sets `enabled` per status** via `rowActionEnabledForStatus` (`src/domain/workflow-runtime/projection.ts`), the single source of truth for the matrix: **running → ×** · **queued → ▲ ×** · **done/failed → ↻ 🗑** (a queued row has no delete — cancel it first, which makes it terminal, then 🗑 appears; cancelled is `failed`+`step=cancelled` → terminal → ↻ 🗑). The buttons (`BumpButton`, `RetryButton`, `RowCancelButton`, `DeleteButton` in `shared/`) still never branch on `entry.status` themselves — they only read descriptors — so the schema stays kernel-driven; status-gating lives in the projection, not the component. The default policy carries all four row actions (`DEFAULT_ROW_ACTIONS`, `default-policy.ts`); workflows inherit them. `RowCancelButton` routes by the cancel target's status (running → `status:"running"`, queued → queued handler, OCR-prep → discard) via the pure `buildRowCancelDispatchArgs` (`lib/row-cancel-request.ts`). **(2026-06-12, E2E-012/E2E-013):** the OCR-prep discard proxy (a) OVERRIDES the action descriptor's targets with the OCR run identity (`workflowId:"ocr"`, `id: ocrSessionId`, `runId: ocrRunId`) — the dispatcher prefers descriptor targets over `fallbackTarget`, and an operation row's descriptor carries the OPERATION runId, which 400'd the discard handler; (b) sends parent context (`parentWorkflow`/`parentRunId`/`parentItemId`) ONLY when the clicked row is the coordinator (`workflow !== "ocr"`) — for OCR-panel rows the server derives the real parent (`readParentRunId`/`readOperationWorkflow`), so the discard mirrors the operation row instead of the OCR row itself; (c) is gated on a still-discardable `data.ocrStatus` (`running|preparing|awaiting-review` or absent) so an approved operation's × falls through to the ordinary cancel. OCR review rows now carry `ocrSessionId`/`ocrRunId` in data (orchestrator re-stamp), so their × also routes through discard. Server-side, `cancelTarget` (perform-workflow-action.ts) treats the forwarded status as ADVISORY: a 409 wrong-state signal from either cancel handler falls through to the other one (E2E-008). Pinned by `row-cancel-request.test.ts` + `perform-workflow-action.test.ts`. This **replaces** the prior "always retry+delete, no status gating" rule and the dead `CancelRunningButton`/`QueueItemControls` (removed). The toolbar Stop + drawer `StopPill` remain the daemon-scoped stop; the footer × is the per-row cancel.
  - **Batch/group footers** (`DaemonBatchRow`/`DelegationRow` via `BatchFooterActions`/`ApprovalDelegationFooterActions`) keep their own bulk selection (retry-all / delete-all / cancel-all over members); they are not status-gated per the matrix above (a group spans mixed-status members). Don't bump a group.
- **2026-06-01: 0-member batch footers fall back to the anchor.** `DaemonBatchRow` derived footer time / elapsed / retry+delete from `memberEntries`, so a `batch` anchor with no members yet (e.g. the oath-signature PDF row during OCR approval — visible under the "all" filter via `queueGroupMatchesStatusFilter(null, []) === true`) rendered a degenerate footer (bare `#run`, no time/elapsed/actions). Fix: `DaemonBatchRow` takes an `anchorEntry` prop and uses `footerEntries = members.length ? members : [anchor]` for `firstTimestamp`, `elapsedEntries`, and `BatchFooterActions` targets; `members` stays empty so the count badge/progress bar still read 0/0. `QueuePanel` passes `anchorEntry={surface.parent}`. Surfaced by the dev UI gallery's ("?view=ui-gallery") "0 members (pre-fan-out)" variant — the gallery reuses the real component, so footer regressions show up there.
- **Prep and delegation cards:** OCR rows are `preview`; batch rows are real batch anchors. Multi-file upload is N independent preview runs, not one grouped parent card.
- **Status:** Queue row status semantics resolve from per-workflow **`statusExtensions`** (declared on `defineWorkflow`) via `src/domain/queue-row-status.ts` — the single source for `EntryItem`'s derived status (`notFound`, `needsReview`) and supplemental tag (person-lookup A/IA). `EntryItem` is workflow-agnostic: it calls `resolveQueueRowStatus(entry, { isDone })` and renders whatever comes back; the universal `cancelled` override + 5 base statuses stay in the component. Rule objects live client-bundle-safe in the domain/tracker layers (`person-lookup-status.ts`, `tracker/dashboard/ocr-status.ts`) and are registered for the client via the side-effect import `domain/queue-row-status-index.ts` (defineWorkflow never runs in the bundle). Never branch on `entry.workflow === "..."` for status in the dashboard.
- **Titles:** Queue row title/subtitle resolve from **kind** (`data.queueRowKind`: person/file/catalog) via `src/domain/queue-row-presentation.ts` — the single source for `resolveEntryName`/`resolveEntryId` (`shared/entry-display.ts`) and projection `batchGroupTitle`. person → resolved name (subtitle EID, else trace id); file → PDF filename; catalog → spec label; **person batch anchor → no title** (count badge + member preview identify it). Never promote technical ids to primary text; do not hardcode workflow titles in the dashboard.
- **2026-06-29: Workflow Modifier reworked from a 45-node op WALL into a step-LANE review canvas (`components/workflow-modifier/graph/`).** The old surface seeded a workflow's REAL mined ops as one free-floating `actionNode` per op (`workflow-ops-build.ts` → ~45 same-weight cards under 3 steps = an unnavigable wall, the operator's "not ideal to do anything"). Now each presentation step is a **LANE** (`stepNode` rewritten over a shared `LaneShell`) that **NESTS its ops as collapsible internal rows** with an internal-scroll cap (`max-h-[22rem]` + `nodrag nowheel`) so a 24-op lane is the same height as a 5-op one. New pieces: **`lane-build.ts`** (pure, replaces `workflow-ops-build.ts`) — `groupLaneOps(bank, baseStepOrder)` buckets mined ops under their matching presentation step (an unmapped mined step → a read-only `opsLaneNode`/`NODE_OPS_LANE` lane to the right, ignored by `graphToOverride`), and `buildDataFlowEdges(orderedLanes)` derives lane→lane **data-flow links** (a scrape's `{var}` → the FIRST later lane whose fill/select READS it; `{}`/case/`a|b`-alternative-insensitive; never within one lane; deduped) rendered as a dashed `log-cyan` `dataFlowEdge`/`EDGE_DATAFLOW` carrying the `{var}` as a midpoint pill, toggled off by default. **`OutlineRail.tsx`** is the always-on left navigator (Row → Steps w/ op counts → Delegation) + the three view controls (collapse/expand all · data-flow toggle · fit); clicking an entry **focuses** that lane = select + `fitView({nodes:[{id}]})` + **dim the rest** via `node.style.opacity` (focus/dim derived from a `focusedId` state, `duration:0` under `prefers-reduced-motion`). **Load-bearing seam discipline:** ops are merged into step nodes in the COMPONENT layer (`GraphCanvas` `configModel` memo), **NOT** in the pure `overrideToGraph` — `StepNodeData.ops` is optional + ignored by `graphToOverride`, and `stepIndex` (added for the "Step N" eyebrow) rides the projection — so `graph-build.test`/`design-spec.test` stay byte-identical (they pin node types/ids/edge-counts, NOT positions, so widening `STEP_DX` 240→384 + dropping the delegation branch below the now-tall lanes is free). **Lane collapse + focus are EPHEMERAL context state** (`lane-interaction.ts` `LaneInteractionContext`), deliberately OUT of `node.data` so a draft keystroke (which rebuilds `configModel`) can't reset a lane's collapsed/focused flag. The `DataBankPalette` is tucked behind an "Add node" toggle to keep the review canvas clean; the inspector gained a read-only `Operations (N)` summary on step + ops-lane nodes. **Scaffold tradeoff:** ops lanes are excluded from the lifted model and `configSlice(stepNode)` ignores `data.ops`, so "Generate scaffold" no longer auto-dumps all mined ops into the `.md` brief (it was noise; config + explicit intent/action nodes still serialize). The 2026-06-25 dual-output scaffold + the four pure projections below are otherwise UNCHANGED. Pinned by `tests/unit/dashboard/workflow-graph/lane-build.test.ts` (groupLaneOps + buildDataFlowEdges); `workflow-ops-build.test.ts` deleted with its module. No component harness — verified by `typecheck:all` + `test:architecture` (79) + `lint` + `build:dashboard` + live `dashboard:prod` Playwright (EC: lanes/spine/delegation; focus → frame + dim + inspector; data-flow toggle; Separations scaled to 11 steps / 87 ops / 4 systems; Kronos Search 24-op internal scroll + a real Job-Summary→Kronos data-flow link), `.screenshots/workflow-editor-mocks/wf-0{1..5}-*.png`. **Refinements (same day, operator review):** (1) **rail focus centers against the inspector** — `focusNode` now uses `getNode` + `setCenter` (not `fitView`) and pushes the target right by half the inspector footprint (`INSPECTOR_FOOTPRINT` ≈ the panel's `w-[22rem]` + gap, kept in sync by a comment) so the framed lane lands in the visible middle, NOT under the right overlay panel. (2) **The inspector is now a real review panel** (`NodeInspector` `StepReviewDetail`, shared by step + ops-lane): `Step N of M` eyebrow + step id, at-a-glance chips, a **Data flow** section (each `↓{inVar} from <op>` / `↑{outVar} into <op>`), **Operations (N)** in FULL detail (`OpDetailRow`: kind + system + the target selector/role/url mono + in/out var pills + the op's gotcha `note`), and **Notes**/**Source** from the data bank — the editable Label/Fold/Visibility controls moved into their own bottom "Presentation" section. This needed the per-step **`bankNote`/`bankSourceRef`** (data-bank `DataBankStep.note`/`sourceRef`) threaded onto `StepNodeData`/`OpsLaneNodeData` via a `stepMeta` map merged in `GraphCanvas` (same component-layer pattern as `ops` — pure projections untouched) + carried on `ExtraLane`. (3) **Data bank discoverability** — the canvas's "Add node" toggle is renamed **"Data bank"** with a `Database` icon (the palette it opens already self-labels "Data bank"). Verified live on Oath Upload (`wf-06-focus-inspector.png`).
- **2026-06-25: Workflow Modifier page rebuilt as an n8n-style node-graph editor (`components/workflow-modifier/graph/`) with a dual-output design-intent scaffold — the "Live Blueprint" stacked plates are RETIRED.** The page is now a pannable/zoomable `@xyflow/react` (React Flow v12) canvas of modular nodes wired together. **Dual output ("option c"):** editing produces TWO artifacts — (1) the **live config** (the existing sparse `WorkflowOverride`: naming/steps/delegation, written via `POST /api/workflow-presentation/:wf`, applied hot via `effectiveMetadata`, unchanged), AND (2) a **design-intent scaffold** (`config/workflow-design/<wf>.{json,md}`, git-tracked) capturing design intent beyond today's schema — new node kinds, layout, freeform look/behavior — for a future session to implement. **Node taxonomy:** config-backed nodes round-trip to the override — `rowNode` (title/subtitle/trace), `stepNode` (label/hide/fold, **order = node sequence**), `delegationCoordinatorNode` (label suffix), `prepNode` (prep title), `memberNode` (member title/subtitle); design-intent-only nodes feed the scaffold — `customNode` (violet dashed, carries freeform `intent`), `noteNode` (sticky), `groupNode` (labeled frame). **Edge families** drive semantics + styling: `sequenceEdge` (muted), `delegationEdge` (info), `foldEdge` (dashed warning, host⇢folded step), `customEdge` (dashed violet). **The four pure projections are the testable seam** (no component harness): `overrideToGraph(base, draft, name)` / `graphToOverride(model, name, baseSteps)` (mutual inverses, routed through `prune` so the override stays sparse), `graphToDesignSpec(model, wf, generatedAt)` / `designSpecToGraph(spec)` (whole-graph capture ↔ intent-node + position overlay), plus `renderDesignBrief(spec)` (deterministic `.md`). All live-preview via the SAME `resolveTitle/Subtitle/Trace` the server uses. **Component tree:** `WorkflowModifierPage` (picker + canvas + dual action bar: Revert/Save config + **Generate scaffold**), `GraphCanvas` (`ReactFlowProvider`, `colorMode="dark"`, custom `Background`/`Controls`/`MiniMap`, `NodePalette` Panel, live-model lifting for the scaffold), `NodeInspector` (right panel, reuses `SchemeHotspot`+`TokenTemplateInput`+blueprint sparse-setters for config nodes, freeform fields for intent nodes), `nodes/` + `edges/` registries (module-constant for perf), `graph-build.ts`/`design-spec.ts` projections. **REUSED unchanged:** `blueprint-helpers.ts`, `SchemeHotspot`, `TokenTemplateInput`, `WorkflowPicker`, `useWorkflowPresentation`. **DELETED** (superseded plates, no external importers/tests): `RowNamingPlate`, `StepPipelinePlate`, `StepChip`, `DelegationPlate`, `Plate`. **One new dep:** `@xyflow/react` — its CSS is imported in `index.css` **after** the tailwind import (Tailwind-v4 ordering rule; never from a `.tsx`), and its `--xy-*` palette is remapped to our design tokens there so the built-in chrome tracks the dark theme (no raw color, no `@keyframes` — architecture-guarded). **Backend:** new `workflow-design` Hono route group (`GET/POST/DELETE /api/workflow-design/:wf`) mirrors `workflow-presentation` — `src/tracker/workflow-design/{schema.ts (zod, fail-loud),store.ts}` + `paths.ts` helpers; POST stamps `generatedAt` server-side, validates hard, writes the JSON spec AND renders the `.md` brief. **The `.md` is GENERATED — never hand-edit; edit the graph and re-generate.** Pure projections + brief + zod pinned by `tests/unit/dashboard/workflow-graph/*` + `tests/unit/domain/workflow-design/brief.test.ts` + `tests/unit/tracker/workflow-design/schema.test.ts`. Verified by `typecheck:all` + `test:architecture` (79) + `build:dashboard` + `lint` + live-dashboard Playwright (config edit → live preview → sparse save; add custom node → fill intent → Generate scaffold → both files written).
- **2026-06-23: Top-level queue-row naming is now driven by effective presentation for stamped, non-delegated rows (Task 6.3).** `buildWorkflowRunProjection` (`src/domain/workflow-runtime/projection.ts`) gained a `topLevelNaming` term gated on `presentation` truthy (stamped — `resolveQueueRowPresentation` returns undefined without a `queueRowKind`) ∧ `!entry.parentRunId` (not a member) ∧ `!isPrepRow(entry)` (not a prep) ∧ `effectivePresentation?.naming` present. It resolves through `resolveQueueRowPresentation(entry, { naming, preferTraceIdSubtitle })` and sits in the precedence **before** `context.resolveEntryTitle`/`resolveEntrySubtitle`: `overrides.* ?? delegationTitle/Subtitle ?? topLevelNaming?.* ?? context.resolveEntry* ?? presentation?.* ?? fallback`. **Parity-safe by construction:** with the DEFAULT naming, `topLevelNaming` is byte-identical to `presentation` (6.1 Dimension A), and for a stamped row `context.resolveEntryTitle` already equals `presentation.title`, so an unconfigured workflow round-trips unchanged (6.1 + 6.2 + the 4.5 no-op projection test stay green); an operator's `naming.title`/`naming.subtitle` override — previously SHADOWED — now reaches the live `EntryItem`/operation row (both render `projection?.title ?? … ?? resolveEntryName`). The **out-of-projection** call sites in `shared/entry-display.ts` (`resolveEntryName`/`resolveEntryId`, used by search, App.tsx toasts, the log-panel header, `buildDisplayNameMap`, `BatchScreenshotsPanel`) deliberately **stay on kind dispatch** — they have no per-workflow `naming` metadata at the call site (spec-sanctioned), so only the projection-backed surfaces consume effective naming. Pinned by the top-level case in `tests/unit/tracker/workflow-presentation/override-changes-projection.test.ts`.
- **2026-06-02: Member/preview footer subtitle falls to the TRACE ID, not the EID.** The subtitle rule "EID if present, else trace id" is correct **only for a flat single** person row (its footer is the only place the EID shows — unchanged). On surfaces where the EID/name is already shown elsewhere — a **batch / preview group anchor** (the member-name preview in `group-row-base.tsx` shows each person's EID on the title-line right) — the footer subtitle must NOT repeat the EID; it falls through to the **trace id**. This is the `preferTraceIdSubtitle` option on `resolveQueueRowPresentation` (person kind only; `file`/`catalog` already yield the trace id), set by the group-anchor branch of `buildProjectionFromQueueSurface` (`src/domain/workflow-runtime/projection.ts`). Two bugs this fixed: (1) a one-member `alwaysBatchDelegatedMembers` batch (oath-signature / person-lookup) has **no parent row**, so the old `batchParent`-gated `subtitle: ... ? anchorProjection.subtitle : undefined` left the group footer **blank** — now the anchor falls back to `members[0]` and resolves the trace id; (2) `DelegationRow`'s footer fell back to `parent.data.__name` when `projection.subtitle` was undefined, leaking the literal `"OCR"` workflow label — removed (a stale pre-trace-id row now just shows no id). Pinned by `tests/unit/domain/workflow-runtime-projection.test.ts` (member-batch trace id, OCR file preview trace id ≠ "OCR", flat single keeps EID). Drilled-in member `EntryItem`s keep the EID subtitle (no EID on their own title line).
- **No title ordinals:** Session-local ordinals (`OATH 1`, `<label> · #1234`, `Roster 2`) are retired. Disambiguate by the footer's time + `#run`. `resolveDaemonBatchQueueTitle` → titleOverride, else a member's `pdfOriginalName`, else `""`. The terminal-drawer session card's title **always** drops the trailing ordinal (`WorkflowBox.displayInstance` strips `\s\d+$` — every instance, not just a lone ` 1`); instances stay numbered internally for start/end pairing but the number never reaches the title.
- **2026-06-01 (rev. 2026-06-04): Session-card subtitle = the running run's trace id, but ONLY while in flight.** `WorkflowBox`'s subtitle shows `WorkflowInstanceState.currentTraceId` — the frozen `data.__traceId` of the in-flight run, identical to that run's queue-row subtitle — **only when `itemInFlight` is true**. The daemon worker resolves the id with `findFrozenTraceId({workflow, runId, trackerDir})` at claim time and passes it as the `traceId` arg to `emitItemStart`; it rides the `item_start` session event's `traceId` field into `session-state.ts`, which sets `currentTraceId` on `item_start` and (like `currentItemId`) does not clear it on `item_complete`. **The card no longer DISPLAYS that retained id once the item finishes** — when not in flight (idle, keepalive, complete, failed, or a fresh authenticating daemon) the subtitle falls back to the phase subline from `deriveSessionCardCopy` (`idle — waiting for work`, `Run complete`, `Authenticating 1/2`, …) rather than leaving a stale trace id sitting in the description. (The earlier rev. kept the trace id visible after completion; that read as a confusing leftover id on an idle card, so display is now gated on `itemInFlight` — `currentTraceId` is still retained in state for any future correlation use.) The field must be carried in both the backend (`tracker/dashboard/session-state.ts`) and frontend (`shared/types.ts`) `WorkflowInstanceState` mirrors AND in `session-state-equal.ts` (`workflowEqual`) or the drawer won't re-render on a new trace id. The footer's bottom-right shows the current step (`footerStep`) — never a raw item id (see the step_change lesson below). UI gallery: the in-flight variant sets `currentTraceId` + `currentStep`; the "idle (had a run)" variant sets `currentTraceId` but is idle, exercising that the id is hidden.
- **2026-06-04: Session-card `currentStep` was silently broken by the `emitStepChange` dedup — fixed by always emitting + render-time dedup.** The session card's footer step text and micro step pipeline read `WorkflowInstanceState.currentStep`, which `rebuildSessionState` derives SOLELY from `step_change` session events. Those events were being **suppressed for every `ctx.step`** by a 50ms dedup in `emitStepChange` (`tracker/session-events.ts`): `Stepper.announce` writes a `step:start` log line (`Phase: X`) immediately *before* calling `emitStepChange`, so the "is there a recent matching step log?" guard always matched and dropped the event. Result: `currentStep` stayed `null` the whole run → the footer fell back to `currentItemId` (an opaque derived id, "random numbers like an id") and the pipeline never advanced. Fix: `emitStepChange` now **always emits** (the dedup machinery + its dead imports were removed); the duplicate-line concern it was solving — the log-panel **"all" tab** showing both the `Phase: X` log line AND a bare `step_change` event line — is now handled at render time in `mergeDisplayItems` (`log-panel/LogStream.tsx`), which drops `step_change` events from the merged stream (they stay in the dedicated **Events** tab and keep flowing to session state). The `WorkflowBox` footer also no longer falls back to `currentItemId` — never show an item id as the "step". Pinned by `tests/unit/tracker/step-change-emit.test.ts` (always-emits) + `tests/unit/dashboard/log-stream-merge.test.ts` (render dedup).
- **2026-06-04: `StepPipeline` cancellation no longer snaps the highlight back to step 0.** A cancelled run is `status="failed"` + `step="cancelled"` (the sentinel) — so `currentStep` no longer names the reached step. The old code's "failed + unrecognized step → step 0" fallback fired for the sentinel, lighting **Loading Roster** (red, with a live timer) even though the run was stopped at, say, **Awaiting Approval**. Fix: the step derivation is now the exported pure fn `computeStepViews(steps, currentStep, status, stepDurations)` (extracted from the component for testability). For a cancelled run it recovers the reached step from `stepDurations` — every COMPLETED step has a recorded duration (see `computeStepDurations` in `tracker/dashboard/run-timelines.ts`; the in-flight step at cancel time has none), so the cancellation point is the step just after the furthest timed step (clamped to the last step). That step is marked a new `cancelled` `StepStatus` rendered in the **`warning` (amber)** token — matching the row's "Cancelled" badge (`EntryItem` `STATUS_CONFIG.cancelled`) — not a misleading red `failed`. The genuine "failed with no/unknown step → step 0 red" fallback is preserved for real crashes. `AuthSuperChip`/`authGroupStatus` handle the cancelled status too (workflows whose steps include `auth:*`). Pinned by `tests/unit/dashboard/step-pipeline.cancelled.test.ts`. No live component harness — also verified by `build:dashboard` (warning opacity utilities emit) + architecture token guards.
- **SSE:** Real-time streams use one `/events/hub` EventSource. Listener errors must not kill other listeners, and projection-backed topics must resolve the current `state.db` handle each tick. **(2026-07-17)** Snapshot topics (`entries`, `sessions`) are change-gated — a payload is sent on subscribe and then only when its serialized form changes (never assume a 1 Hz tick client-side); `sseResponse` pings `:ka` every 30s and disconnects a consumer once 512 messages buffer, and a fetcher throw/reject inside any topic tick is contained to that tick (logged + retried), never an unhandled rejection.
- **2026-06-02: WorkflowRail badge counts are backend-authoritative.** `App.tsx` passes SSE `wfCounts` through `buildWorkflowRailEntryCounts`; do not override the active workflow with QueuePanel's local "ALL" count. The backend owns cross-workflow badge semantics via `countSidebarRowsFromTrackerHistory(asTracker)`, including latest-row dedupe, EID merge, top-level queue-surface collapse, and visible OCR prep rows. QueuePanel status strips may still compute local per-filter counts, but rail badges must track the `entries` payload contract.
- **Run launchers:** Operators start workflows through upload runs (`RunModal` + `RUN_MODAL_REGISTRY`) or input runs (`InputRunPanel` + `INPUT_RUN_REGISTRY`).
- **Input-bar row shape is count-based.** One parsed value is flat; multiple parsed values become one batch row.
- **2026-05-30: `buildDisplayNameMap` ordinal machinery removed (dead since queue row kind landed).** Stamped rows resolve title via `resolveQueueRowPresentation` (queue row kind), which wins inside `resolveEntryName` before the displayNames map is consulted — so the session-local "<base> <n>" numbering (the `ordinal` flag, the `totals` counting pass, the `counters` loop) was dead for every production row. The map now only emits bare base names for legacy/unstamped rows + the delegated-label inheritance walk. The one preserved nuance: a lone bare workflow-label fallback (e.g. a single "Separation" doc with no person name) is still omitted from the map so `resolveEntryName` falls through to its richer `__subject`. Behavior-neutral: scenario snapshots unchanged; the OATH/EMPL unit assertions dropped their ` 1` suffix (the map output, never seen in prod where OCR rows are kind `file`).
- **2026-05-30: Per-workflow status rules moved out of EntryItem into `statusExtensions`.** `EntryItem` no longer branches on `entry.workflow` for the A/IA tag or the `notFound`/`needsReview` derived statuses. The rules live in `WorkflowConfig.statusExtensions` (`derivedStatus` + `secondaryTag`), resolved generically by `resolveQueueRowStatus` (`src/domain/queue-row-status.ts`). Client registration is via the side-effect import `domain/queue-row-status-index.ts`. Behavior-neutral refactor — pinned by `tests/unit/domain/queue-row-status.test.ts`. Gotcha: `statusExtensions` is OPTIONAL, so no "every workflow must declare it" guard (that would be wrong).
- **2026-05-28: Retired workflows are filtered at the dashboard payload boundary.** `active-check` and `eid-lookup` tracker history can remain on disk, but `/events` workflow metadata and `/api/workflows` filter them out so the rail only exposes Person Lookup.
- **OCR review UI:** Preserve source-page ordering, retry/re-OCR page isolation, and `data-pair-index` instrumentation.
- **Testing gap:** There is no browser/component harness yet. Pure projection logic has unit tests; visual changes need lint, typecheck, dashboard build, and manual dashboard verification.
- **2026-05-31: Desktop Tailwind compliance is guarded by architecture tests.** `tests/unit/architecture/frontend-tailwind-compliance.test.ts` blocks dashboard regressions for custom keyframes/inline animation, arbitrary `z-[...]`, old `flex-shrink-0`, motion-unsafe Tailwind animations, `<img>` tags without `srcSet`/`sizes` plus explicit loading priority, and (2026-06-04) **raw color** — Tailwind palette classes (`text-sky-500`, `bg-amber-400`, …) and raw hex in `.tsx` components are rejected; use design tokens. The dashboard remains desktop-only; do not add mobile-only layout requirements unless the product scope changes.
- **2026-06-04: `.dark` retoned to a neutral near-black ladder, unified with the `--capture-*` system.** The dashboard's `.dark` block in `index.css` was warm-brown (hue ~15–32); it's now a neutral grayscale ladder at **hue 240, ~4% sat** matching the existing `--capture-*` "Editorial Calm" near-black surfaces — so the whole dashboard reads like the capture/run-modal reference (the run modal was already a hybrid: capture surfaces + warm `.dark` text/borders, now consistent). Surfaces: `--background` 240 4% 4% → `--card`/`--popover`/`--secondary`/`--muted`/`--accent` step up to ~13%; `--foreground` 240 5% 96%, `--muted-foreground` 240 4% 56%, `--border` 240 3% 14%. **`--primary` is now neutral near-white** (240 5% 90% / fg 240 6% 10%) — active nav, selected states, progress bars, and `bg-primary` CTAs render monochrome (no orange accent). `--ring`/`--focus` are a neutral mid-gray (240 4% 50%). **Status colors stay meaningful but muted** to sit on near-black: `--info` 211 62% 64%, `--success` 142 48% 55%, `--warning` 38 55% 58%, `--destructive` 0 58% 62%, plus retoned `--log-*` and a categorical `--chart-*` ramp (neutral→info→success→warning→violet). The `.dark .calendar` HeroUI overrides + HeroUI `--surface/--overlay/--default/--separator/--scrollbar` were retoned too. **Token NAMES, the `@theme inline` map, fonts, radius, and shadow tokens are unchanged** — this was a values-only edit, so no `.tsx` touched (all components consume tokens). Light `:root` is dead (`<html class="dark">` is hardcoded) and was left alone. Verified by `test:architecture` (75/75) + `build:dashboard` + a `?view=ui-gallery` Playwright screenshot (status hues stay legible). The `--capture-*` block remains the reference; `.dark` now mirrors it.
- **2026-06-04: Color is token-only; semantic + categorical tokens live in `index.css`.** A UI/UX audit found state/auth/log colors bypassing the theme via Tailwind palette classes and inline hex (terminal-drawer, log-panel, status badges) — invisible to the light/dark token system. Fix: added semantic state tokens **`info`/`success`/`warning`** (light `:root` + `.dark` + `@theme inline`, alongside existing `destructive`) and a small **categorical** accent set **`log-cyan`/`log-teal`/`log-violet`/`log-slate`** for the log/event legend, then migrated every component onto them (`text-info`, `bg-success/15`, `border-warning/40`, `text-log-violet`, …). Mapping: blue/sky→info, green/emerald→success, amber/yellow/orange→warning, red/rose→destructive; distinct log hues→`log-*`; neutrals→`foreground`/`muted-foreground`. The **only** sanctioned raw hex is `#4ade80`/`#fbbf24` (documented in `shared/status-styles.ts`). The audit also: gave `LogLine` event rows lucide icons (was unicode glyphs) + keyboard-accessible copy; swapped `⚠`/`←`/`●` glyphs for lucide/CSS; added `aria-label` to the search input and `aria-live` to the streaming-log + failure-bell count regions; added a real focus ring to the terminal-drawer toggle. Token additions live in `index.css` (light/dark + the `@theme inline` `--color-*` map that generates the utilities). Verified by typecheck + `build:dashboard` (utilities + opacity variants emit correctly) + architecture guards; no component test harness exists.
