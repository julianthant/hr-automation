// Person Match drives the UCPath HR-Tasks person search only; all URLs and
// timeouts come from the shared UCPath system module / src/config.ts. Nothing
// to narrow yet — this file exists to satisfy the workflow directory shape.
export const PERSON_MATCH_CONFIG = {} as const;
