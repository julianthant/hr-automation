---
phase: 01.1
slug: modular-codebase-restructure-with-shared-crm-and-ucpath-modules-and-test-organization
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-14
---

# Phase 01.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Node.js built-in `node:test` (v22.17.0) with tsx 4.21.0 loader |
| **Config file** | None (uses tsx CLI flags) |
| **Quick run command** | `npx tsx --test tests/**/*.test.ts` |
| **Full suite command** | `npx tsx --test tests/**/*.test.ts` |
| **Estimated runtime** | ~3 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npx tsc --noEmit && npx tsx --test tests/**/*.test.ts`
- **After every plan wave:** Run `npx tsc --noEmit && npx tsx --test tests/**/*.test.ts`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 5 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 01.1-01-01 | 01 | 1 | RESTRUCTURE-01 | unit | `npx tsx --test tests/**/*.test.ts` | Will exist after migration | ⬜ pending |
| 01.1-01-02 | 01 | 1 | RESTRUCTURE-02 | typecheck | `npx tsc --noEmit` | N/A (compiler check) | ⬜ pending |
| 01.1-01-03 | 01 | 1 | RESTRUCTURE-03 | typecheck | `npx tsc --noEmit -p tsconfig.test.json` | N/A (compiler check) | ⬜ pending |
| 01.1-01-04 | 01 | 1 | RESTRUCTURE-04 | smoke | `npx tsx src/cli.ts --help` | N/A (manual smoke) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `tests/unit/schema.test.ts` — migrated from `src/onboarding/schema.test.ts` with updated imports
- [ ] `tests/unit/env.test.ts` — migrated from `src/utils/env.test.ts` with updated imports
- [ ] `tests/unit/log.test.ts` — migrated from `src/utils/log.test.ts` with updated imports
- [ ] `tsconfig.test.json` — extends base, includes both `src/` and `tests/`
- [ ] `tests/e2e/` — empty directory placeholder

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| CLI extract command loads | RESTRUCTURE-04 | Requires runtime CLI invocation | Run `npx tsx src/cli.ts --help` and verify output |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
