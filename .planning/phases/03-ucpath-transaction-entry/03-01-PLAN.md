---
phase: 03-ucpath-transaction-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ucpath/types.ts
  - src/ucpath/action-plan.ts
  - src/ucpath/index.ts
  - tests/unit/action-plan.test.ts
  - tests/unit/transaction-types.test.ts
autonomous: true
requirements: [ENTR-05]

must_haves:
  truths:
    - "ActionPlan.preview() lists all planned steps to console without executing them"
    - "ActionPlan.execute() runs each step function in order"
    - "TransactionError carries the step name where failure occurred"
    - "TransactionResult reports success/failure with optional error and transactionId"
  artifacts:
    - path: "src/ucpath/types.ts"
      provides: "TransactionResult, TransactionError, PlannedAction types"
      exports: ["TransactionResult", "TransactionError", "PlannedAction"]
    - path: "src/ucpath/action-plan.ts"
      provides: "ActionPlan class with add/preview/execute methods"
      exports: ["ActionPlan"]
    - path: "src/ucpath/index.ts"
      provides: "Barrel exports for ucpath module"
    - path: "tests/unit/action-plan.test.ts"
      provides: "Unit tests for ActionPlan preview and execute"
    - path: "tests/unit/transaction-types.test.ts"
      provides: "Unit tests for TransactionError and TransactionResult"
  key_links:
    - from: "src/ucpath/action-plan.ts"
      to: "src/ucpath/types.ts"
      via: "imports PlannedAction type"
      pattern: "import.*PlannedAction.*types"
    - from: "src/ucpath/index.ts"
      to: "src/ucpath/action-plan.ts"
      via: "barrel re-export"
      pattern: "export.*action-plan"
---

<objective>
Create the UCPath module's type contracts and ActionPlan dry-run engine with unit tests.

Purpose: The ActionPlan class is the core of ENTR-05 (dry-run mode) and the foundation that Plan 02 builds the transaction workflow on top of. It is the only unit-testable surface in Phase 3 -- all PeopleSoft navigation is manual-only. Types must be defined first so Plan 02 can implement against concrete contracts.

Output: Typed ActionPlan class with preview/execute methods, TransactionResult/TransactionError types, and full unit test coverage.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ucpath-transaction-entry/03-RESEARCH.md

@src/ucpath/index.ts
@src/crm/types.ts
@src/utils/log.ts

<interfaces>
<!-- Existing types the executor needs -->

From src/crm/types.ts:
```typescript
export class ExtractionError extends Error {
  constructor(message: string, public readonly failedFields?: string[]) {
    super(message);
    this.name = "ExtractionError";
  }
}
```

From src/utils/log.ts:
```typescript
export const log = {
  step: (msg: string): void => { ... },
  success: (msg: string): void => { ... },
  waiting: (msg: string): void => { ... },
  error: (msg: string): void => { ... },
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: UCPath types and ActionPlan class with tests</name>
  <files>src/ucpath/types.ts, src/ucpath/action-plan.ts, tests/unit/transaction-types.test.ts, tests/unit/action-plan.test.ts</files>
  <behavior>
    - TransactionError extends Error with name "TransactionError" and optional step property
    - TransactionError.step carries the step name where failure occurred (e.g., "Navigate to Smart HR")
    - TransactionResult has success boolean, optional error string, optional transactionId string
    - ActionPlan.add(description, executeFn) increments step counter and stores the action
    - ActionPlan.preview() writes step list to console via log.step (format: "  1. description") without calling any execute function
    - ActionPlan.preview() prints header "=== DRY RUN: Transaction Preview ===" and footer "=== No changes made to UCPath ==="
    - ActionPlan.execute() calls each stored function in order, logging progress (format: "[1/N] description")
    - ActionPlan.execute() throws TransactionError with step name if any step function throws
    - ActionPlan with zero actions: preview() prints header/footer only, execute() completes without error
  </behavior>
  <action>
    Write tests FIRST (RED), then implement to pass (GREEN).

    **src/ucpath/types.ts:**
    Create TransactionResult interface (success: boolean, error?: string, transactionId?: string) and TransactionError class extending Error with optional step: string property (follows the ExtractionError pattern from src/crm/types.ts). Also export PlannedAction interface (step: number, description: string, execute: () => Promise<void>).

    **src/ucpath/action-plan.ts:**
    Create ActionPlan class with:
    - Private actions array of PlannedAction
    - Private stepCounter starting at 0
    - add(description: string, execute: () => Promise<void>): void -- increments counter, pushes to array
    - preview(): void -- prints header, each step as "  {step}. {description}", footer. Uses log.step() from src/utils/log.js. Does NOT call any execute function.
    - async execute(): Promise<void> -- iterates actions, logs "[{step}/{total}] {description}", calls execute(). On error, wraps in TransactionError with step description.

    Import log from "../utils/log.js". Import TransactionError from "./types.js".

    **tests/unit/transaction-types.test.ts:**
    Test TransactionError: name is "TransactionError", message set, step optional, step carries value when provided.

    **tests/unit/action-plan.test.ts:**
    Test ActionPlan: preview lists steps without executing, execute runs functions in order, execute wraps errors in TransactionError, empty plan preview/execute work.

    Use node:test and node:assert. Import via relative paths (../../src/ucpath/...).
  </action>
  <verify>
    <automated>cd C:/Users/Julian/Projects/hr-automation && npx tsx --test tests/unit/action-plan.test.ts tests/unit/transaction-types.test.ts</automated>
  </verify>
  <done>All ActionPlan and TransactionError tests pass. Types are exported and importable.</done>
</task>

<task type="auto">
  <name>Task 2: UCPath barrel exports</name>
  <files>src/ucpath/index.ts</files>
  <action>
    Replace the stub comment in src/ucpath/index.ts with barrel re-exports:
    - Export ActionPlan from "./action-plan.js"
    - Export TransactionResult, TransactionError, PlannedAction from "./types.js"

    This provides the public API that Plan 02 will import from.
  </action>
  <verify>
    <automated>cd C:/Users/Julian/Projects/hr-automation && npx tsc --noEmit -p tsconfig.test.json</automated>
  </verify>
  <done>src/ucpath/index.ts exports ActionPlan, TransactionResult, TransactionError, PlannedAction. TypeScript compiles with zero errors.</done>
</task>

</tasks>

<verification>
```bash
cd C:/Users/Julian/Projects/hr-automation && npm test && npm run typecheck:all
```
All existing tests (13) plus new tests pass. TypeScript compiles cleanly.
</verification>

<success_criteria>
- ActionPlan.preview() prints step list without executing any function
- ActionPlan.execute() runs steps in order and wraps errors in TransactionError
- TransactionError carries step context
- All unit tests pass
- TypeScript compiles with zero errors across both tsconfig files
</success_criteria>

<output>
After completion, create `.planning/phases/03-ucpath-transaction-entry/03-01-SUMMARY.md`
</output>
