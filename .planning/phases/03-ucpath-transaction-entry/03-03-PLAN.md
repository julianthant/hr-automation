---
phase: 03-ucpath-transaction-entry
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/ucpath/navigate.ts
  - src/ucpath/transaction.ts
  - src/auth/login.ts
autonomous: false
requirements: [ENTR-01, ENTR-02, ENTR-03, ENTR-04, ENTR-05]

must_haves:
  truths:
    - "Tool navigates from UCPath homepage to Smart HR Transactions page successfully"
    - "Tool selects UC_FULL_HIRE template from the template selector"
    - "Tool enters the effective date and PeopleSoft validates it (Tab key triggers server check)"
    - "Tool clicks Create Transaction and detects success or reports specific error"
    - "Dry-run mode shows correct action plan with real extracted data without modifying UCPath"
  artifacts:
    - path: "src/ucpath/navigate.ts"
      provides: "Working PeopleSoft navigation selectors (verified against live UCPath)"
    - path: "src/ucpath/transaction.ts"
      provides: "Working template selection, date entry, and create transaction selectors"
  key_links:
    - from: "src/ucpath/navigate.ts"
      to: "UCPath PeopleSoft UI"
      via: "Playwright selectors targeting live DOM elements"
      pattern: "SELECTOR.*verified"
    - from: "src/ucpath/transaction.ts"
      to: "UCPath PeopleSoft iframe"
      via: "frameLocator('#ptifrmtgtframe') for all form interactions"
      pattern: "getContentFrame"
---

<objective>
Discover and fix PeopleSoft selectors against live UCPath, then verify the full end-to-end transaction creation flow.

Purpose: All PeopleSoft selectors in Plan 02 are best-guess placeholders. This plan follows the proven Phase 1/Phase 2 pattern: run against the live system, observe what actually exists in the DOM, and iteratively fix selectors until the flow works. UCPath transactions cannot be undone, so verification must be careful.

Output: Working end-to-end create-transaction command verified against live UCPath.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ucpath-transaction-entry/03-RESEARCH.md
@.planning/phases/03-ucpath-transaction-entry/03-01-SUMMARY.md
@.planning/phases/03-ucpath-transaction-entry/03-02-SUMMARY.md

@src/ucpath/navigate.ts
@src/ucpath/transaction.ts
@src/cli.ts
@src/auth/login.ts
@src/debug-page.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Live selector discovery and iterative fixes</name>
  <files>src/ucpath/navigate.ts, src/ucpath/transaction.ts, src/auth/login.ts</files>
  <action>
    Run `npm run create-transaction:dry test@ucsd.edu` first to verify dry-run works with a real email (requires ACT CRM auth + Duo).

    Then run `npm run create-transaction test@ucsd.edu` for the live flow. This will launch browsers, authenticate, extract data, then attempt UCPath navigation and transaction creation.

    **Iterative discovery process (same pattern as Phase 1 Task 3 and Phase 2 Task 1):**

    1. Run the command and observe where it fails
    2. Take screenshots at failure points using the debug-page utility or add `await page.screenshot({ path: '.auth/debug-{step}.png', fullPage: true })` calls
    3. Inspect the DOM at each step -- look at actual element IDs, classes, text content, and iframe structure
    4. Fix the selector in the source file
    5. Re-run and check the next step
    6. Repeat until the full flow completes

    **Expected discovery areas (from RESEARCH.md Open Questions):**

    - **Smart HR Transactions URL:** Observe the URL after navigating to the Smart HR Transactions page. If a stable direct URL is found, update `SMART_HR_URL` constant in navigate.ts. If direct URL does not work, the menu fallback should be tested.

    - **Homepage navigation structure:** Determine whether "HR Tasks" is a homepage tile, NavBar item, or navigation collection. Fix the tile/menu selectors in the fallback path.

    - **PeopleSoft iframe:** Confirm `#ptifrmtgtframe` is the correct iframe ID for UCPath. If different, update getContentFrame().

    - **Template selector type:** Determine whether UC_FULL_HIRE dropdown is a native `<select>`, PeopleSoft lookup, or custom dropdown. Fix selectTemplate() accordingly.

    - **Effective date field ID:** Find the actual field ID/label for the effective date input. Fix enterEffectiveDate() locator.

    - **Create Transaction button:** Find the actual button element. Fix clickCreateTransaction() locator.

    - **Success/error detection:** After clicking Create Transaction, observe what the page shows. Fix the success/error detection in clickCreateTransaction().

    - **Processing spinner:** Observe whether PeopleSoft shows a spinner during navigation/form submission. Fix waitForPeopleSoftProcessing() selector if needed.

    **Mark all fixed selectors** with `// SELECTOR: verified against live UCPath {date}` replacing the old `// SELECTOR: best-guess` comments.

    **Important constraints:**
    - Per feedback_url_params.md: prefer URL param manipulation over UI clicking
    - Per feedback_auth_architecture.md: UCPath and ACT CRM use separate browsers
    - Per feedback_no_session_tracking.md: leave UCPath browser open after completion
    - Be cautious with Create Transaction -- UCPath transactions cannot be undone. If unsure about the final click, stop before it and screenshot for human review.
    - If any auth selector needs updating (login.ts), fix it too -- the SSO flow may have changed since Phase 1.
  </action>
  <verify>
    <automated>cd C:/Users/Julian/Projects/hr-automation && npm test && npm run typecheck:all</automated>
  </verify>
  <done>All PeopleSoft selectors in navigate.ts and transaction.ts updated to match live UCPath DOM. Command runs through navigation, template selection, date entry, and create transaction (or stops at a known point if UCPath blocks test transactions). Selectors marked as verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify end-to-end transaction creation</name>
  <action>
    Human verifies that both dry-run and live modes of the create-transaction command work correctly against the real systems.
  </action>
  <what-built>
    Full create-transaction flow against live UCPath:
    1. Dry-run mode: extracts employee data, shows action plan preview without touching UCPath
    2. Live mode: authenticates to both systems, extracts data, navigates UCPath Smart HR Transactions, selects UC_FULL_HIRE template, enters effective date, clicks Create Transaction
  </what-built>
  <how-to-verify>
    1. Run `npm run create-transaction:dry test@ucsd.edu` (replace with a real employee email)
       - Expected: ACT CRM auth + Duo, data extraction, then DRY RUN output showing action plan steps and effective date -- NO UCPath browser should open
       - Verify: No UCPath navigation occurred, effective date shown, no PII in output

    2. Run `npm run create-transaction test@ucsd.edu` (replace with a real employee email you want to create a transaction for)
       - Expected: ACT CRM auth + Duo + extraction, then UCPath auth + Duo, navigation to Smart HR Transactions, UC_FULL_HIRE selected, date entered, Create Transaction clicked
       - Verify: Transaction created successfully (or specific error message if UCPath rejects it)
       - Verify: UCPath browser left open after completion

    3. Verify no PII leaks in terminal output -- should only see field counts and effective date, never SSN/names/addresses
  </how-to-verify>
  <verify>Human visual verification of dry-run and live transaction creation</verify>
  <done>User confirms dry-run shows correct action plan without UCPath interaction, and live mode creates a transaction successfully (or reports a specific UCPath error). No PII leaks in output.</done>
  <resume-signal>Type "approved" if both dry-run and live modes work correctly, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
Phase 3 gate checks:
1. `npm run create-transaction:dry <email>` shows correct action plan with extracted data
2. `npm run create-transaction <email>` navigates UCPath, selects template, enters date, clicks Create Transaction
3. Confirmation or specific error reported after transaction attempt
4. `npm test && npm run typecheck:all` passes
</verification>

<success_criteria>
- All PeopleSoft selectors verified against live UCPath (no more best-guess placeholders)
- Dry-run mode works end-to-end: extract from ACT CRM, preview action plan, no UCPath touch
- Live mode works end-to-end: extract, authenticate to UCPath, navigate, create transaction
- Transaction creation confirmed successful OR specific error reported
- No PII in any terminal output
- UCPath browser left open after completion
- Human verified both modes
</success_criteria>

<output>
After completion, create `.planning/phases/03-ucpath-transaction-entry/03-03-SUMMARY.md`
</output>
