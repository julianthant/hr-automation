---
phase: 01-authentication-and-project-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - .gitignore
  - .env.example
  - src/utils/env.ts
  - src/utils/env.test.ts
  - src/utils/log.ts
  - src/utils/log.test.ts
  - src/browser/launch.ts
  - src/auth/types.ts
autonomous: true
requirements: [AUTH-01, AUTH-05]

must_haves:
  truths:
    - "npm install succeeds and Chromium is installed"
    - "TypeScript compiles with zero errors"
    - "Env validation rejects missing variables and accepts valid ones"
    - "Logger outputs colored status messages without PII"
    - "Browser launches in headed mode and creates a context with or without saved state"
  artifacts:
    - path: "package.json"
      provides: "Project manifest with dependencies and scripts"
      contains: "playwright"
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
    - path: "src/utils/env.ts"
      provides: "Env validation with fail-early exit"
      exports: ["validateEnv"]
    - path: "src/utils/log.ts"
      provides: "PII-safe colored console logger"
      exports: ["log"]
    - path: "src/browser/launch.ts"
      provides: "Browser launch + session state loading"
      exports: ["launchBrowser", "saveSession"]
    - path: "src/auth/types.ts"
      provides: "Shared type definitions for auth module"
      exports: ["AuthResult", "LoginOptions"]
  key_links:
    - from: "src/browser/launch.ts"
      to: "src/utils/log.ts"
      via: "import log for status messages"
      pattern: "import.*log.*from.*utils/log"
    - from: "src/browser/launch.ts"
      to: ".auth/state.json"
      via: "storageState load/save"
      pattern: "storageState"
---

<objective>
Scaffold the hr-automation project from scratch and build the core infrastructure modules that Plan 02 depends on: package.json with all dependencies, TypeScript config, env validation, PII-safe logger, browser launch/session module, and shared auth types.

Purpose: Plan 02 (auth flows + CLI) cannot be built without these foundations. This plan creates every module that Plan 02 imports.
Output: Working TypeScript project with tested utilities, browser launch capability, and type contracts for the auth module.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authentication-and-project-foundation/01-CONTEXT.md
@.planning/phases/01-authentication-and-project-foundation/01-RESEARCH.md

Existing files in project root:
- `.env` -- already has UCPATH_USER_ID and UCPATH_PASSWORD
- `.gitignore` -- exists but needs `.auth/` added
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project scaffolding and dependency installation</name>
  <files>package.json, tsconfig.json, .gitignore, .env.example, src/auth/types.ts</files>
  <action>
1. Initialize the Node.js project:
   - Run `npm init -y` to create package.json
   - Edit package.json to set: name "hr-automation", version "0.1.0", private true, type "module"
   - Add scripts:
     - "test-login": "tsx --env-file=.env src/cli.ts test-login"
     - "test-login:fresh": "tsx --env-file=.env src/cli.ts test-login --fresh"
     - "typecheck": "tsc --noEmit"
     - "test": "tsx --test src/**/*.test.ts"

2. Install dependencies:
   - `npm install playwright commander picocolors`
   - `npm install -D typescript tsx @types/node`
   - `npx playwright install chromium`

3. Create tsconfig.json per RESEARCH.md:
   - target: ES2022, module: ESNext, moduleResolution: bundler
   - strict: true, esModuleInterop: true, skipLibCheck: true
   - outDir: dist, rootDir: src, declaration: true
   - resolveJsonModule: true, verbatimModuleSyntax: true
   - include: ["src/**/*"]

4. Update .gitignore -- append these entries (keep existing content):
   - `.auth/` (session state -- per user decision)
   - `dist/` (build output)

5. Create .env.example (documentation only, no real values):
   ```
   UCPATH_USER_ID=your-ucsd-user-id
   UCPATH_PASSWORD=your-ucsd-password
   ```

6. Create src/auth/types.ts -- shared type contracts for the auth module:
   ```typescript
   export interface LoginOptions {
     fresh: boolean;  // --fresh flag: ignore saved session
   }

   export interface AuthResult {
     ucpiath: boolean;   // UCPath authenticated
     actCrm: boolean;    // ACT CRM authenticated
     sessionSaved: boolean; // Session persisted to .auth/
   }
   ```
   These types are the contract between Plan 01 and Plan 02. Plan 02 implements functions that accept LoginOptions and return AuthResult.
  </action>
  <verify>
    <automated>cd "C:/Users/Julian/Projects/hr-automation" && node -e "const pkg = require('./package.json'); console.assert(pkg.type === 'module'); console.assert(pkg.scripts['test-login']); console.log('package.json OK')" && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <done>package.json has all dependencies and scripts, tsconfig.json compiles cleanly, .gitignore blocks .auth/ and dist/, .env.example exists, src/auth/types.ts exports LoginOptions and AuthResult</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Env validation, PII-safe logger, and browser launch module</name>
  <files>src/utils/env.ts, src/utils/env.test.ts, src/utils/log.ts, src/utils/log.test.ts, src/browser/launch.ts</files>
  <behavior>
    - env.test.ts Test 1: validateEnv() throws/exits when UCPATH_USER_ID is missing
    - env.test.ts Test 2: validateEnv() throws/exits when UCPATH_PASSWORD is missing
    - env.test.ts Test 3: validateEnv() returns { userId, password } when both vars are set
    - log.test.ts Test 1: log.step() output does not contain any value from process.env
    - log.test.ts Test 2: log.success/error/waiting produce output (not silent)
  </behavior>
  <action>
1. Create src/utils/env.ts -- env validation module:
   - Export `validateEnv()` function that checks for UCPATH_USER_ID and UCPATH_PASSWORD in process.env
   - If any are missing: call log.error() listing the missing variable names, then call process.exit(1)
   - If all present: return `{ userId: string, password: string }`
   - Import log from ../utils/log for error messages
   - Reference .env.example in the error message: "Create a .env file with these variables. See .env.example"

2. Create src/utils/env.test.ts -- unit tests using node:test:
   - Use `describe`/`it` from `node:test`
   - Test missing UCPATH_USER_ID: temporarily delete from process.env, call validateEnv, assert it exits/throws. Use mock or capture process.exit.
   - Test missing UCPATH_PASSWORD: same approach
   - Test both present: set both env vars, assert return value matches expected shape
   - Use `beforeEach`/`afterEach` to save and restore original env values

3. Create src/utils/log.ts -- PII-safe colored logger per RESEARCH.md:
   - Import picocolors (import pc from "picocolors")
   - Export a `log` object with methods:
     - step(msg: string): blue "->" prefix
     - success(msg: string): green checkmark prefix
     - waiting(msg: string): yellow hourglass prefix
     - error(msg: string): red X prefix (to stderr via console.error)
   - CRITICAL: These functions accept a message string. They MUST NOT interpolate any env var, credential, email, or PII. The caller is responsible for passing safe messages like "Entering credentials..." (not "Entering credentials for juzaw...").

4. Create src/utils/log.test.ts -- unit tests:
   - Capture console.log/console.error output
   - Test that log.step("Entering credentials...") produces output containing "Entering credentials..."
   - Test that no log method output contains process.env.UCPATH_USER_ID or process.env.UCPATH_PASSWORD values (set dummy values, call log methods with safe messages, verify output does not contain dummy values)

5. Create src/browser/launch.ts -- browser launch + session management:
   - Import chromium from "playwright"
   - Import fs from "node:fs", path from "node:path"
   - Import log from "../utils/log"
   - Constants: AUTH_DIR = path.join(process.cwd(), ".auth"), STATE_FILE = path.join(AUTH_DIR, "state.json")
   - Export `launchBrowser(fresh: boolean = false)`: launches Chromium in headed mode (headless: false), creates context with storageState from STATE_FILE if it exists and fresh is false, creates page, returns { browser, context, page }. Log status: "Launching browser..." and "Loading saved session..." (if applicable).
   - Export `saveSession(context: BrowserContext)`: creates .auth/ directory (recursive), calls context.storageState({ path: STATE_FILE }), logs "Session saved".
   - Export `clearSession()`: removes STATE_FILE if it exists, logs "Cleared saved session".
   - Use proper TypeScript types from playwright: Browser, BrowserContext, Page
  </action>
  <verify>
    <automated>cd "C:/Users/Julian/Projects/hr-automation" && npx tsx --test src/utils/env.test.ts && npx tsx --test src/utils/log.test.ts && npx tsc --noEmit && echo "All tests pass, types clean"</automated>
  </verify>
  <done>validateEnv rejects missing vars and returns credentials when present (tests pass). Logger outputs colored messages without PII (tests pass). Browser launch module exports launchBrowser/saveSession/clearSession with proper types. TypeScript compiles with zero errors.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- TypeScript compiles with zero errors
2. `npx tsx --test src/**/*.test.ts` -- all unit tests pass
3. `node -e "const pkg = require('./package.json'); console.assert(pkg.scripts['test-login'])"` -- CLI script defined
4. `.gitignore` contains `.auth/` entry
5. `.env.example` exists with placeholder variable names
</verification>

<success_criteria>
- TypeScript project compiles cleanly with strict mode
- All npm dependencies installed (playwright, commander, picocolors, tsx, typescript)
- Chromium browser installed via Playwright
- Env validation tested: rejects missing vars, accepts valid vars
- Logger tested: colored output, no PII leakage
- Browser launch module ready: headed Chromium, storageState support
- Auth type contracts defined (LoginOptions, AuthResult) for Plan 02 to implement against
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-and-project-foundation/01-01-SUMMARY.md`
</output>
