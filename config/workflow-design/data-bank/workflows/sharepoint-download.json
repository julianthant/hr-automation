{
  "workflow": "sharepoint-download",
  "systems": [
    "sharepoint"
  ],
  "steps": [
    {
      "step": "auth:sharepoint",
      "label": "SharePoint Login",
      "system": "sharepoint",
      "sourceRef": "src/workflows/sharepoint-download/download.ts",
      "operations": [
        {
          "id": "sharepoint.landingUrl#navigate",
          "kind": "navigate",
          "system": "sharepoint",
          "label": "Navigate to SharePoint file URL",
          "url": "{url}",
          "inputVar": "{url}",
          "summary": "page.goto(landingUrl, { waitUntil: 'domcontentloaded', timeout: 30s }). URL read from pendingLandingUrl module-level slot set by the handler serial queue before each run."
        },
        {
          "id": "sharepoint.timeout3s#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait 3s for AAD redirect chain to start",
          "summary": "page.waitForTimeout(3000)"
        },
        {
          "id": "sharepoint.microsoft.emailInput#fill",
          "kind": "fill",
          "system": "sharepoint",
          "label": "Fill Microsoft AAD email address",
          "selectorFqn": "microsoft.emailInput",
          "inputVar": "{userId}@ucsd.edu",
          "note": "Conditional — skipped if microsoft.emailInput count === 0 (cached AAD session)"
        },
        {
          "id": "sharepoint.microsoft.nextButton#click",
          "kind": "click",
          "system": "sharepoint",
          "label": "Click Microsoft AAD Next button",
          "selectorFqn": "microsoft.nextButton",
          "note": "Only fires when emailInput was present; triggers AAD redirect chain to ADFS or Shibboleth"
        },
        {
          "id": "sharepoint.domcontentloadedPostMs#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait for DOM content loaded after Microsoft Next",
          "summary": "page.waitForLoadState('domcontentloaded', timeout: 10s) — in handleMicrosoftEmailStep, conditional on email step having run"
        },
        {
          "id": "sharepoint.ssoRedirect#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait for SSO redirect chain to settle",
          "summary": "page.waitForURL matching a5.ucsd.edu | ad-wfs-aws.ucsd.edu | /adfs/ls | duosecurity.com | sharepoint.com | office.com | login.microsoftonline.com/kmsi | .../login.srf, timeout: 30s"
        },
        {
          "id": "sharepoint.authBranch#control",
          "kind": "control",
          "system": "control",
          "label": "Branch: Shibboleth vs ADFS vs already-authed",
          "summary": "if url includes a5.ucsd.edu → Shibboleth path (ops 8-9); else if url includes ad-wfs-aws.ucsd.edu or /adfs/ls → ADFS path (ops 10-12); else skip SSO (cached trust — jump to Duo gate)"
        },
        {
          "id": "sharepoint.adfs.usernameInput#fill",
          "kind": "fill",
          "system": "sharepoint",
          "label": "Fill ADFS username (ADFS branch only)",
          "selectorFqn": "adfs.usernameInput",
          "inputVar": "{userId}@ucsd.edu",
          "note": "Only refilled if the field is currently empty — Microsoft usually pre-populates via ?username= URL param from AAD redirect"
        },
        {
          "id": "sharepoint.adfs.passwordInput#fill",
          "kind": "fill",
          "system": "sharepoint",
          "label": "Fill ADFS password (ADFS branch only)",
          "selectorFqn": "adfs.passwordInput",
          "inputVar": "{password}"
        },
        {
          "id": "sharepoint.adfs.submitButton#click",
          "kind": "click",
          "system": "sharepoint",
          "label": "Click ADFS Sign In button (ADFS branch only)",
          "selectorFqn": "adfs.submitButton"
        },
        {
          "id": "sharepoint.shibbolethSso#control",
          "kind": "control",
          "system": "control",
          "label": "Shibboleth SSO fill + submit (Shibboleth branch only)",
          "summary": "fillSsoCredentials(page) + clickSsoSubmit(page) — shared infra at src/infra/auth/sso-fields.ts. Shibboleth IdP (a5.ucsd.edu) is not the normal SharePoint route; ADFS (ad-wfs-aws.ucsd.edu) is. Shibboleth path kept as fallback."
        },
        {
          "id": "sharepoint.duoApproval#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Duo MFA approval gate",
          "summary": "requestDuoApproval (kernel path — requires instance, surfaces Duo chip in Sessions rail) or pollDuoApproval (CLI/standalone path); systemLabel='SharePoint'; successUrlMatch includes sharepoint.com|office.com|kmsi|login.srf; timeout: 180s. Throws if approval times out.",
          "note": "Conditional — skipped when page.url() already matches a success URL (warm-cookie fast-path)"
        },
        {
          "id": "sharepoint.networkIdleKmsi#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait for network idle before KMSI prompt probe",
          "summary": "page.waitForLoadState('networkidle', timeout: 15s)"
        },
        {
          "id": "sharepoint.kmsi.noButton#click",
          "kind": "click",
          "system": "sharepoint",
          "label": "Dismiss 'Stay signed in?' prompt (No)",
          "selectorFqn": "kmsi.noButton",
          "note": "Conditional — skipped if kmsi.noButton count === 0; failure swallowed (prompt may auto-dismiss)"
        },
        {
          "id": "sharepoint.domcontentloadedPostKmsi#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait for DOM content loaded after KMSI dismiss",
          "summary": "page.waitForLoadState('domcontentloaded', timeout: 10s) — in dismissStaySignedIn, conditional on KMSI button having been present"
        },
        {
          "id": "sharepoint.successUrl#assert",
          "kind": "assert",
          "system": "sharepoint",
          "label": "Assert final URL is a SharePoint / Office success URL",
          "summary": "Throws 'SharePoint authentication did not reach a success URL — final URL: ...' if page.url() does not include sharepoint.com | office.com | login.microsoftonline.com/kmsi | .../login.srf"
        }
      ]
    },
    {
      "step": "navigate",
      "label": "Verify post-auth SharePoint URL",
      "system": "sharepoint",
      "sourceRef": "src/workflows/sharepoint-download/workflow.ts",
      "note": "loginToSharePoint already navigated to the file URL as part of auth. This step only asserts the URL and registers the phase on the dashboard timeline. A hook for any future post-auth navigation (e.g. switching files mid-run).",
      "operations": [
        {
          "id": "sharepoint.postAuthUrl#assert",
          "kind": "assert",
          "system": "sharepoint",
          "label": "Assert page is on SharePoint or Office URL",
          "summary": "Reads page.url(); throws 'Expected SharePoint/Office URL after login, got: ...' if it does not include sharepoint.com or office.com"
        }
      ]
    },
    {
      "step": "download",
      "label": "Download Excel file via File menu",
      "system": "sharepoint",
      "sourceRef": "src/workflows/sharepoint-download/download.ts",
      "operations": [
        {
          "id": "sharepoint.networkIdleViewer#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait for Excel Online viewer to settle (network idle)",
          "summary": "page.waitForLoadState('networkidle', timeout: 30s)"
        },
        {
          "id": "sharepoint.timeout2s#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait 2s for WAC iframe JS to hydrate after networkidle",
          "summary": "page.waitForTimeout(2000)"
        },
        {
          "id": "sharepoint.excelOnline.fileButton#click",
          "kind": "click",
          "system": "sharepoint",
          "label": "Click File button in Excel ribbon (WacFrame_Excel_0 iframe)",
          "selectorFqn": "excelOnline.fileButton",
          "note": "All three menu ops (fileButton, createACopy, downloadACopy) are scoped to the WAC iframe via getExcelFrame(page). If fileButton count === 0, clickExcelDownloadMenu returns false and falls back to manual-download waiting prompt."
        },
        {
          "id": "sharepoint.timeout1s#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait 1s for File menu to open",
          "summary": "page.waitForTimeout(1000)"
        },
        {
          "id": "sharepoint.fileMenu.createACopy#click",
          "kind": "click",
          "system": "sharepoint",
          "label": "Hover 'Create a Copy' to reveal the download flyout",
          "selectorFqn": "fileMenu.createACopy",
          "note": "MUST use .hover(), not .click() — clicking fires the default 'Create a copy online' action which navigates away from the file. Returns false if not found."
        },
        {
          "id": "sharepoint.timeout800ms#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Wait 800ms for download flyout to appear after hover",
          "summary": "page.waitForTimeout(800)"
        },
        {
          "id": "sharepoint.fileMenu.downloadACopy#click",
          "kind": "click",
          "system": "sharepoint",
          "label": "Click 'Download a Copy' in the Create a Copy flyout",
          "selectorFqn": "fileMenu.downloadACopy",
          "note": "Returns false if not found. Click triggers the browser's native download event."
        },
        {
          "id": "sharepoint.autoClickFallback#control",
          "kind": "control",
          "system": "control",
          "label": "Auto-click fallback — prompt for manual download if click chain failed",
          "summary": "If clickExcelDownloadMenu returns false (any step not found): log.waiting('Could not auto-click — please click File → Create a Copy → Download a Copy manually (waiting up to Xs)'). The download event race still runs; operator can trigger manually within the timeout."
        },
        {
          "id": "sharepoint.downloadEvent#wait",
          "kind": "wait",
          "system": "sharepoint",
          "label": "Race: capture download event vs browser close vs timeout",
          "summary": "Promise.race([page.once('download'), page.once('close') / context.once('close'), setTimeout(downloadTimeoutSeconds * 1000)]). Default timeout: 300s (5 min)."
        },
        {
          "id": "sharepoint.downloadCapture#assert",
          "kind": "assert",
          "system": "sharepoint",
          "label": "Assert download was captured (not closed, not timed out)",
          "summary": "Throws 'Browser window was closed before the download completed' if race result==='closed'; throws 'No download captured within Xs' if result===null"
        },
        {
          "id": "sharepoint.saveAs#scrape",
          "kind": "scrape",
          "system": "sharepoint",
          "label": "Save downloaded file to outDir, derive filename, update tracker row",
          "outputVar": "{path, filename}",
          "summary": "Filename: if input.filenameBase set → '<filenameBase>-<YYYY-MM-DDTHH-MM-SS><ext>'; else '<YYYY-MM-DDTHH-MM-SS>-<sharepoint-suggested>'. download.saveAs(outPath). ctx.updateData({ filename, path: saved }) populates 'File' and 'Saved to' detailFields. Sets module-level lastDownloadResult for HTTP handler via _takeLastDownloadResult()."
        }
      ]
    }
  ],
  "notes": [
    "Auth is kernel-owned (systems[].login = sharepointLogin → loginToSharePoint). loginToSharePoint navigates to the file URL as part of auth, so the 'navigate' step only asserts the resulting URL — no additional navigation needed.",
    "URL injection pattern: pendingLandingUrl is a module-level variable in workflow.ts, set by the serial job queue (handler.ts) immediately before each run and cleared in finally. Safe only because the serial queue enforces one concurrent run at a time. If parallel runs are ever enabled, switch to a per-runId WeakMap.",
    "SSO branch: UCSD federates SharePoint through ADFS (ad-wfs-aws.ucsd.edu/adfs/ls/), NOT Shibboleth (a5.ucsd.edu). The Shibboleth path exists as a fallback but is not the normal SharePoint route (2026-04-22 lesson).",
    "Auto-click fallback: clickExcelDownloadMenu returns false on any element-not-found failure and logs a manual-download prompt. The download event race still runs — the step will succeed if the operator triggers the download manually within the timeout window.",
    "No dry-run boundary: this workflow always executes a real browser download. No fan-out; one run = one file.",
    "Concurrency: module-level serial queue in handler.ts serializes all runs across all registry ids. Concurrent fresh invocations queue behind the current job — prevents parallel Duo taps on the same phone. requestSharePointDownload({ mode: 'wait' }) joins the newest queued/current job.",
    "Fire-and-forget HTTP: POST /api/sharepoint-download/run returns 202 immediately. Dashboard surfaces progress via Sessions panel (browser chip + Duo chip + DONE/FAILED pill), LogPanel, and Queue row.",
    "inputSubject is 'selector' (catalog kind) — queue row title comes from the registry label (e.g. 'Onboarding Roster'), not a person name or EID. Trace id prefix is 'sp'."
  ]
}
