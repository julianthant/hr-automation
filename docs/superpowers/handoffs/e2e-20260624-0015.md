# E2E follow-up handoff — run 20260624-0015

## State of the world

**What was tested.** Full AI e2e of the hr-automation dashboard + kernel + daemons against an
**isolated tracker dir** (`generated/.e2e/tracker`):
- **Stub lane (Phases 0–9, `HRAUTO_E2E_STUBS=1`):** 7-variant matrix (~60 runs) through the real UI —
  enqueue/denorm/trace, cancel-queued, cooperative cancel of a signal-only wait, operation tree-discard,
  approve fan-outs (oath-sig single+multi, onbase stub contract, EC), fail-injection→Retry→replay, standalone
  no-approve, parallel workers (spawn-2/no-overspawn/2+2 reuse/stop-instance reassign), reload + counts +
  lifecycle. 4 read-only verifier agents + report.
- **Live lane (real CRM/UCPath/Kuali/Kronos, hands-off Duo):** onboarding dry-run + 9-doc separations dry-run.

**What passed.** 20 verified invariants (driver + verifiers). Core daemon/kernel machinery is solid:
VQ-003 single-terminal-write + 2026-06-04 no-deadlock hold, retry replays `original_input_json`, parallel-worker
spawn/reuse/reassign, 0 runs reaching >1 terminal surface across 799 lifecycle events, 0 invalid-line spam.
Live-lane SAFETY proven: **0 UCPath transactions, 0 Kuali finalizations**, `dryRun=true` on every task.

**Commits landed this session (master, not pushed):**
- `3c7e89c3` fix(run-modal): OnBase Run was a silent no-op — re-derive formType on open (ISS-001)
- `de221831` fix(queue): OnBase member subtitle shows the EID via ucpathId (ISS-003)

Both red→green pinned; `npm test` 2899 ✓, architecture 79 ✓, lint ✓, typecheck ✓, `/review-code` clean.

**Artifacts.** `generated/.e2e/runs/20260624-0015/`: `report.html` (before/after), `findings.json`,
`manifest.jsonl` (12 checkpoints), `issues.jsonl` (ledger: 6 findings, 20 passes, triages), `api/` (state dumps),
`dashboard.log` + `dashboard-live.log`. Screenshots in `.screenshots/e2e/20260624-0015/`. Daemon logs in
`generated/.e2e/tracker/daemons/` (the live onboarding + separations logs are the evidence for the live findings).

**Re-create the env.**
```bash
mkdir -p generated/.e2e/tracker
# STUB lane:
HRAUTO_TRACKER_DIR="$(pwd)/generated/.e2e/tracker" HRAUTO_E2E_STUBS=1 npm run dashboard   # :3838 + :5173
# LIVE lane (no stubs, hands-off Duo via .auth/duo-webauthn.json):
HRAUTO_TRACKER_DIR="$(pwd)/generated/.e2e/tracker" npm run dashboard
# place the fixture roster so OCR runs don't hit the forbidden SharePoint download:
cp tests/data/e2e-fixture-roster.xlsx generated/.e2e/tracker/{rosters,sharepoint}/
```
Drive the dashboard with the `playwright-cli` skill (headed). NB: the headed Chrome crashes intermittently under
a heavy queue — reopen the session; server state is unaffected.

## Errors that remain (deferred — one block per issue)

### ISS-006 — i9 (I9 Complete) login fails; blocked the onboarding live run  [HIGH]
- **Symptom.** Onboarding dry-run failed at the eager session-launch i9 auth: `Login page loaded | URL:
  https://stse.i9complete.com/` then `page.waitForURL: Timeout 15000ms exceeded` on **all 3 attempts**.
  Run failed before any handler step → **no mutation** (safe).
- **Repro.** Live-lane onboarding run for `dong7777125@gmail.com` (dry-run ON). Evidence:
  `generated/.e2e/tracker/daemons/onboarding-2026-06-24T09-18-12-801Z.log`.
- **Root cause (confidence: medium).** `src/systems/i9/login.ts:50` waits for the post-login hostname to include
  `wwwe.i9complete.com`, but the login page is now served from `stse.i9complete.com`. Likely an i9 app-domain
  migration (`wwwe`→`stse`), OR the login submit silently fails on the new page.
- **Fix needed.** Do NOT blind-change the URL. Drive the i9 login at `stse.i9complete.com` with playwright-cli,
  fill creds, click login, observe the ACTUAL post-login hostname, then update the `waitForURL` predicate
  (and `i9/selectors.ts` if the form changed). i9 is SSO (no Duo).
- **Verify.** Re-run the onboarding live dry-run; it must clear i9 auth and reach the `transaction` dry-run
  terminal (`data.status === "Dry Run Complete"`, no `onboarding-transaction-submitted` screenshot).

### ISS-005 — Hands-off Duo WebAuthn flakes on the FIRST auth attempt  [HIGH]
- **Symptom.** CRM auth attempt 1: `Duo WebAuthn armed` → `Duo WebAuthn factor not found at the prompt —
  falling back to manual Duo` → `Duo approval timed out` (~180s) → retry → **attempt 2 succeeded via WebAuthn**.
  All 4 separations systems + onboarding-UCPath authed first-try, so it's intermittent.
- **Repro.** Any live run; intermittent on the first attempt of a system. Evidence: same onboarding log.
- **Root cause (confidence: medium).** Recovers with the SAME key → NOT a dead credential. The 2026-06-17 Fix
  A/B/C (idle-guard / stale-CDP re-arm / Duo-queue) are in place, so this is a residual re-arm/timing edge in
  `armDuoWebAuthn`'s CDP probe at the very first prompt. Cost: a wasted ~180s manual-Duo timeout per flake.
- **Fix needed.** Build a reproducible multi-attempt live harness (`HR_AUTOMATION_DUO_WEBAUTHN=1 npm run
  test-login -- --systems crm`, looped) to catch the first-attempt "factor not found"; instrument
  `armDuoWebAuthn` / the factor-detection selector at the Duo prompt. If it degrades to chronic-reject,
  re-enroll (`npm run duo:webauthn:enroll` + `-- --security-key`, operator-only).
- **Verify.** N consecutive cold first-attempt auths with no "factor not found" / no manual fallback.

### ISS-004 — Stop-instance emits two workflow_end events with conflicting finalStatus  [LOW]
- **Symptom.** Per-instance stop (reassign) → backend synthesizes `workflow_end(finalStatus=failed)` while the
  daemon's own graceful shutdown emits `workflow_end(finalStatus=done)` ~64ms later.
- **Root cause (confidence: medium).** Teardown synthesis racing the daemon's own end — the recurring
  daemon-teardown-state-machine nest (`docs/engineering/daemon-teardown-state-machine.md`).
- **Fix needed.** Add a leg to `tests/delegation/daemon-teardown-soak.test.ts` asserting exactly one
  `workflow_end` per instance on stop-instance, then suppress the backend synthesis when the daemon emits its
  own end (mirrors the `endedInstances` guard). Low priority (cosmetic; last-write-wins renders correctly).

### ISS-B-DATA — separations dry-run terminal not demonstrable with the provided doc ids  [LOW, data]
- **Symptom.** All 9 provided doc ids (`4131…3917`) failed `not found in Action List` — not currently pending.
- **Fix needed (data, not code).** Run `tests/live/separations-collect.test.ts` to enumerate CURRENTLY-pending
  doc ids; re-run the separations input run (dry-run ON) with one of them and assert it halts at `Dry Run
  Complete` with `ucpath-transaction` + `kuali-finalization` SKIPPED. The terminal path is already
  deterministically covered by `tests/unit/workflows/separations/dry-run.test.ts`.

### Not a bug (rejected this session)
- **ISS-002** (i9-lookup=0 on verify): correct — `verify.ts` gates i9 on `officerSigned !== true`; the
  multi-oath verify read all records `officerSigned=true`.
- Synthetic-batch-anchor "orphan" (VQ-001) and the worker-split brief mismatch: working-as-designed.

## Execution plan for the next session (SEQUENTIAL)

1. **ISS-006 i9 login** — `general-purpose` (or `feature-dev:code-architect` if the i9 SSO flow changed
   structurally). Inputs: the onboarding daemon log + `src/systems/i9/{login,selectors}.ts`. Drive
   `stse.i9complete.com` live with playwright-cli, confirm the post-login hostname, fix `login.ts:50`. Done:
   onboarding live dry-run reaches the `transaction` dry-run terminal.
2. **ISS-B-DATA + a real separations dry-run terminal** — depends on a live env. Run
   `separations-collect` (read-only) to get a pending doc, then one dry-run separations run; assert the terminal
   + no mutation. `general-purpose`. (Independent of #1 — could run in parallel only if a second live Duo
   session is acceptable; otherwise serialize to avoid Duo-queue contention.)
3. **ISS-005 WebAuthn first-attempt flake** — `general-purpose`. Build the looped first-attempt harness, then
   instrument `armDuoWebAuthn`. Higher effort; only after #1/#2.
4. **ISS-004 workflow_end** — `general-purpose`. Pin in `daemon-teardown-soak`, then suppress the synthesis.
   Lowest priority.

These run **sequentially** (each is its own area but they share the single live Duo session / browser, and #3
depends on understanding #1's auth path). Worktree discipline only applies if a genuinely independent pair is
dispatched in parallel.

## Paste-ready prompt

> Resume the hr-automation e2e follow-up from `generated/.e2e/runs/20260624-0015/handoff.md`. The stub lane is
> done and ISS-001/ISS-003 are fixed+committed (3c7e89c3, de221831). Execute the deferred items SEQUENTIALLY
> with the named subagents, starting with **ISS-006 (i9 login at stse.i9complete.com — confirm the post-login
> hostname live, then fix `src/systems/i9/login.ts:50`)**. Re-create the env per the handoff. Use the
> `playwright-cli` skill for the live dashboard; hands-off Duo is on. Pin every fix red→green before closing it.
