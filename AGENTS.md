# AGENTS.md

This file exists for tooling compatibility (some agents look for `AGENTS.md`).

The authoritative agent instructions for this repo live in `CLAUDE.md` (project root)
and per-module `CLAUDE.md` files under `src/`. Please read those.
